Что предлагаю делать дальше

Я бы пошёл в таком порядке:

Шаг 1 — аппаратная карта

MCU
GPIO
TIM
ADC
DAC
USART
USB
I2C/SPI
EXTI
DMA
Flash

Шаг 2 — interrupt/vector map

Найти все функции, которые являются ISR, и установить:

TIM1 > ?
TIM16 > ?
TIM17 > ?
ADC > ?
USB > ?
USART > ?
EXTI > ?

Шаг 3 — глобальные структуры

Разобрать наиболее важные DAT_080xxxxx и построить:

struct ...
{
    ...
};

Вместо нынешних:

*(ushort *)(DAT_xxx + 0x2a)
*(byte *)(DAT_xxx + 0x14)

Шаг 4 — алгоритм nRLC

Вот это будет главным. Найдём, где:

формируется DAC;
задаётся частота;
измеряется сигнал;
вычисляются R/L/C;
работает autofreq;
используется window;
работают dpf/sin.

Шаг 5 — собрать новый проект STM32CubeIDE

И уже туда переносить восстановленные модули.