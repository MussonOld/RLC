# Карта портов GPIO и неочевидные аппаратные особенности

Источник: схема (в редакции/с аннотациями MussonOld) + `nRLC_STM32F303CBT6.NET`
(экспорт netlist в формате Protel, обозначение компонента `U15` в этой ревизии netlist).
Соответствие вывод↔порт сверено с официальным даташитом ST (DS9118,
Table 13, столбец LQFP48).

**МК в этой ревизии схемы: `STM32F303CCT6` (256КБ Flash), не CBT6.**
Подтверждено напрямую по списку компонентов netlist — совпадает с более ранней
находкой реверс-инжиниринга (оба известных образа прошивки превышают ёмкость
CBT6 в 128КБ). Карта периферии/распиновка идентична у CBT6 и CCT6 (один и тот же
кристалл, отличается только объём Flash), так что на уже проделанную работу по
ISR/периферии это никак не влияет.

## Полная таблица выводов (LQFP48)

| Вывод | Порт | Сеть | Назначение |
|---:|---|---|---|
| 5 | PF0-OSC_IN | OSC_IN | кварц HSE |
| 6 | PF1-OSC_OUT | OSC_OUT | кварц HSE |
| 7 | NRST | N\R\S\T\ | сброс |
| 8 | VSSA/VREF- | AGND | аналоговая земля |
| 9 | VDDA/VREF+ | An_3V3 | аналоговое питание |
| 10 | PA0 | — | **I_DAC** — выход ЦАП канала тока |
| 11 | PA1 | — | чтение кнопки **JOY_OK** (центр джойстика) |
| 12 | PA2 | JOY_C | кнопка джойстика C |
| 13 | PA3 | — | **не подключён** |
| 14 | PA4 | NetR18-1 | аналоговый тракт (цепь обратной связи PGA) |
| 15 | PA5 | NetR11-1 | аналоговый тракт |
| 16 | PA6 | NetC35-2 | аналоговый тракт |
| 17 | PA7 | — | **не подключён** |
| 18 | PB0 | ACLK | тактирование SPI на оба PGA113 (U10/U11) |
| 19 | PB1 | AMOSI | SPI MOSI на оба PGA113 |
| 20 | PB2 | I_CS | выбор кристалла PGA113, канал тока |
| 21 | PB10 | U_CS | выбор кристалла PGA113, канал напряжения |
| 22 | PB11 | NetBLE1-1 | к модулю Bluetooth (HM17/HM19) |
| 23 | VSS | GND | цифровая земля |
| 24 | VDD | Dig_3V3 | цифровое питание |
| 25 | PB12 | — | чтение кнопки **JOY_B** (см. раздел про джойстик/питание) |
| 26 | PB13 | AX_CS | выбор кристалла BMI160 (акселерометр), SPI |
| 27 | PB14 | — | аналоговое чтение **JOY_A / JOY_D** (ADC) — см. раздел про джойстик |
| 28 | PB15 | Display_CS | выбор кристалла дисплея |
| 29 | PA8 | ZP | пьезоизлучатель/спикер |
| 30 | PA9 | TX | USART1 TX |
| 31 | PA10 | RX | USART1 RX |
| 32 | PA11 | — | **не подключён** — встроенный USB (D-) не используется |
| 33 | PA12 | — | **не подключён** — встроенный USB (D+) не используется |
| 34 | PA13 | NetR48-2 | SWDIO |
| 35 | VSS | GND | цифровая земля |
| 36 | VDD | Dig_3V3 | цифровое питание |
| 37 | PA14 | NetR49-2 | SWCLK |
| 38 | PA15 | Vbus1 | детект USB VBUS (только вход — на плате есть физический разъём USB-C для зарядки, но МК не реализует USB device stack; см. выводы 32/33) |
| 39 | PB3 | NetR50-4 | SPI3 (подтверждено MussonOld) |
| 40 | PB4 | NetR50-3 | SPI3 |
| 41 | PB5 | NetR50-2 | SPI3 |
| 42 | PB6 | — | **не подключён** — I2C1_SCL не используется, свободен для переназначения |
| 43 | PB7 | — | **не подключён** — I2C1_SDA не используется, свободен для переназначения |
| 44 | BOOT0 | NetR51-1 | выбор режима загрузки — см. раздел про JOY_B |
| 45 | PB8 | BL | подсветка дисплея |
| 46 | PB9 | VD_ON | самоблокировка питания (см. раздел про включение питания) |
| 47 | VSS | GND | цифровая земля |
| 48 | VDD | Dig_3V3 | цифровое питание |

Выводы 13/17/32/33/42/43 (`PA3`, `PA7`, `PA11`, `PA12`, `PB6`, `PB7`) **вообще не
имеют подключения к какой-либо сети в netlist** — отсутствие подтверждено прямым
поиском, а не просто "нет содержательной подписи". Неиспользование I2C1 было
независимо подтверждено ранее через перекрёстные ссылки Ghidra по периферии
(ноль обращений к регистрам во всей прошивке) — теперь это же подтверждают уже
два независимых источника.

## Механизм включения питания

Два независимых аппаратных пути способны включить устройство из полностью
выключенного состояния:

1. **Долгое нажатие JOY_OK** — механически одновременно тянет вниз и `PA1`
   (чтобы прошивка могла прочитать кнопку), и `PWR_ON`. Низкий уровень на
   `PWR_ON` открывает `Q2` (P-канальный MOSFET), подавая VCC на остальную
   плату. Это работает даже при полностью обесточенном МК — чисто аппаратный
   триггер защёлки, прошивка тут ни при чём.
2. **INT1 акселерометра** — смешивается через диод `D3` с `VD_ON` на затворе
   `Q1` (N-канальный), который так же тянет `PWR_ON` вниз. Позволяет BMI160
   разбудить устройство (например, по движению) без участия прошивки или
   JOY_OK.

**Самоблокировка (self-latch):** после запуска прошивка обязана сама держать
`VD_ON` (= `PB9`) в активном состоянии, чтобы `Q1` (а значит и `Q2`/VCC)
оставались открытыми. Если прошивка ни разу не установит `VD_ON`, либо
перестанет его удерживать — плата снова выключится. Это вероятная причина
существования какой-то низкоуровневой периодической служебной задачи
(кандидат: часть работы `FUN_0800019c` / тела обработчика SysTick — пока не
подтверждено прямым чтением кода, отмечено здесь как зацепка для этого
анализа).

## Джойстик (5-позиционный, `SW1_Disp`)

| Кнопка | Читается через | Механизм |
|---|---|---|
| **JOY_OK** (центр) | `PA1` | короткое нажатие = обычная кнопка; **долгое нажатие** дополнительно запускает аппаратный путь включения питания (см. выше) |
| **JOY_B** (верх) | `PB12` | открывает `Q2_Disp`, который подаёт `VBAT` одновременно на `BOOT0` и `PB12`. `PB12` даёт прошивке прочитать это как обычную кнопку; удержание `BOOT0` в высоком уровне (если удерживать через сброс) переводит STM32 в аппаратный DFU-загрузчик. Одна и та же физическая кнопка выполняет двойную роль в зависимости от момента нажатия. |
| **JOY_C** | `PA2` | обычная цифровая кнопка |
| **JOY_A** / **JOY_D** | `PB14` (ADC, разделена с мониторингом батареи) | обе кнопки просаживают одну и ту же аналоговую линию (`BAT`), которая используется и для мониторинга напряжения батареи, но до разных уровней — подтверждено MussonOld: **JOY_A просаживает линию полностью в 0**, **JOY_D — лишь частично** (резистивный делитель, не прямое замыкание). Когда дойдём до чтения кода обработки этого ADC-канала, ожидается минимум 3 диапазона порогов: ~0 = JOY_A, промежуточный диапазон частичной просадки = JOY_D, нормальный/более высокий диапазон = чтение напряжения батареи (возможно, с дополнительными поддиапазонами для уровня заряда). Это конкретное предсказание для проверки, когда будет открыт код обработки этого канала — пока не сделано. |

## Что этот файл пока не закрывает

- Точные пороговые константы ADC, по которым код различает JOY_A / JOY_D /
  уровень батареи на канале PB14 — пока не найдены в прошивке.
- Действительно ли установка `VD_ON` происходит внутри `FUN_0800019c` (тело
  SysTick) или где-то ещё — правдоподобно, учитывая требование самоблокировки,
  но пока не подтверждено чтением кода.
- `NetR51-1` в разборе netlist оказался общим для `PB12` (вывод 25) и `BOOT0`
  (вывод 44) — стоит перепроверить визуально по самой схеме, если это станет
  важным, поскольку выглядит слегка необычно (один узел резисторной цепи
  касается и GPIO, и BOOT0, а не имеет каждый свою независимую подтяжку).
