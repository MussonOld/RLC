# GPIO_MAP.md

# STM32F303CCT6 — GPIO map

Карта восстановлена по схеме, netlist и реверсу firmware.

## GPIO

| Pin | GPIO | Сеть / сигнал | Назначение |
|---:|---|---|---|
| 10 | PA0 | — | **I_Measure** — аналоговый вход измерения тока, VOUT токового PGA113 |
| 11 | PA1 | — | **JOY_OK** — средняя кнопка |
| 12 | PA2 | JOY_C | **JOY_C** |
| 13 | PA3 | — | не используется |
| 14 | PA4 | — | **DAC_OUT** — реальный выход встроенного DAC |
| 15 | PA5 | — | **RANGE_SELECT** — переключение диапазонов |
| 16 | PA6 | — | **U_Measure** — аналоговый вход измерения напряжения |
| 17 | PA7 | — | не используется |
| 18 | PB0 | ACLK | тактовая линия собственного 3-проводного протокола PGA113 (не SPI) |
| 19 | PB1 | AMOSI | двунаправленная линия данных (DIO) того же протокола (не SPI) |
| 20 | PB2 | I_CS | выбор PGA113 токового канала |
| 21 | PB10 | U_CS | выбор PGA113 канала напряжения |
| 22 | PB11 | BLE_ON | управление Bluetooth |
| 25 | PB12 | Boot | **JOY_B** / состояние Q2_Disp |
| 26 | PB13 | AX_CS | выбор BM1160 |
| 27 | PB14 | BAT | **BAT + JOY_A + JOY_D**, ADC |
| 28 | PB15 | Display_CS | выбор дисплея |
| 29 | PA8 | ZP | пьезоизлучатель |
| 30 | PA9 | TX | USART1 TX |
| 31 | PA10 | RX | USART1 RX |
| 32 | PA11 | USB_DM | USB D−, только DFU |
| 33 | PA12 | USB_DP | USB D+, только DFU |
| 34 | PA13 | SWDIO | SWD |
| 37 | PA14 | SWCLK | SWD |
| 38 | PA15 | Vbat | контроль VBUS/питания |
| 39 | PB3 | CLK | **SPI3 SCK** — дисплей |
| 40 | PB4 | MISO | **SPI3 MISO** — дисплей |
| 41 | PB5 | MOSI | **SPI3 MOSI** — дисплей |
| 42 | PB6 | — | не используется |
| 43 | PB7 | — | не используется |
| 44 | BOOT0 | Boot | аппаратный выбор режима загрузки |
| 45 | PB8 | BL | подсветка дисплея |
| 46 | PB9 | VD_ON | удержание питания включённым |

## Важные аппаратные особенности

### Питание

**JOY_OK**: длинное нажатие открывает батарейный MOSFET Q2 и включает питание устройства.

**JOY_B**: открывает Q2_Disp. Vbat через Q2_Disp подаётся одновременно на BOOT0 и PB12. PB12 используется прошивкой для чтения состояния кнопки, а BOOT0 позволяет войти в аппаратный DFU-загрузчик при соответствующем состоянии во время reset.

### Джойстик

- **JOY_OK** — PA1.
- **JOY_B** — PB12.
- **JOY_C** — PA2.
- **JOY_A / JOY_D** — PB14, через резистивную лестницу.
- **BAT** — также PB14.

JOY_A просаживает BAT в ноль. JOY_D просаживает BAT частично.

Прошивка различает BAT / JOY_A / JOY_D по диапазону ADC-значения на PB14.

## Аналоговые сигналы

- **PA0 = I_Measure** — VOUT токового PGA113 → ADC.
- **PA4 = DAC_OUT** — реальный выход встроенного DAC STM32.
- **PA5 = RANGE_SELECT** — управление переключением диапазонов.
- **PA6 = U_Measure** — аналоговый вход измерения напряжения.
- **PB14 = BAT** — измерение батареи и резистивные кнопки JOY_A/JOY_D.

## Последовательные интерфейсы

### SPI3 — дисплей

**PB3/PB4/PB5 однозначно являются аппаратным SPI3:**

- PB3 — SCK
- PB4 — MISO
- PB5 — MOSI
- PB15 — Display_CS
- PC13 — Display_DC

### PGA113 / BM1160

PB0/PB1 **не являются выводами аппаратного SPI STM32F303CCT6**.

- PB0 — ACLK
- PB1 — AMOSI
- PB2 — I_CS
- PB10 — U_CS
- PB13 — AX_CS

**Важно: это не SPI, ни аппаратно, ни по протоколу.** У PGA113 (по
даташиту TI) не классический 4-проводный SPI с раздельными
MOSI/MISO, а собственный **3-проводный интерфейс**: `SCLK`, `CS` и один
**общий двунаправленный** вывод `DIO` (данные идут в обе стороны по одной
линии, полудуплекс) — это принципиально другой протокол, даже если внешне
похож на SPI по названию сигналов и по наличию тактовой линии. Имя
`AMOSI` в схеме условно (сохранилось по аналогии с SPI-терминологией), но
физически это и есть тот самый двунаправленный `DIO`. Именно поэтому
`PB0/PB1/PB2/PB10` не привязаны ни к одному альтернативному SPI-выводу
STM32F303CCT6 — прошивка обязана реализовывать протокол PGA113 программно
(bit-bang), а не через аппаратный SPI-периферию МК, даже в полудуплексном
(BIDIMODE) режиме.

Рассматривать как **программно управляемый интерфейс под собственный
3-проводный протокол PGA113 (SCLK + двунаправленный DIO + CS)**, не как
SPI-шину.

## USART1

- PA9 — TX
- PA10 — RX

## USB

- PA11 — USB_DM
- PA12 — USB_DP

USB используется **только для DFU-загрузки прошивки**.

## SWD

- PA13 — SWDIO
- PA14 — SWCLK

## Статус

Подтверждено:

- PA0 = I_Measure
- PA2 = JOY_C
- PA4 = DAC_OUT
- PA5 = RANGE_SELECT
- PA6 = U_Measure
- PB12 = JOY_B / Boot
- PB14 = BAT + JOY_A + JOY_D
- PB3/PB4/PB5 = SPI3
- PA11/PA12 = USB DFU
- PB0/PB1 = не SPI, программно управляемые линии

Точные ADC-каналы, пороги ADC, конфигурация DAC и программная реализация PB0/PB1 будут установлены по firmware.

## Примечание об исправлении (2026-09-05)

Более ранняя версия этого файла ошибочно указывала PA0 как "I_DAC" (выход
ЦАП) и PA5 как безымянный "SW". Исправлено MussonOld:

- **PA0** не может быть выходом DAC в принципе — подтверждено CubeMX (у
  STM32F303 конкретные фиксированные пины для каналов встроенного DAC, PA0
  среди них нет), и напрямую видно на листе аналоговой части схемы: PA0
  подключён к VOUT токового PGA113, то есть является **входом** ADC, а не
  выходом.
- **PA5** переименован из ничего не говорящего "SW" в **RANGE_SELECT** —
  управляет аналоговыми ключами SN74LVC1G3157, переключающими диапазоны
  измерения.

Обе поправки не затрагивают уже сделанный разбор конкретных функций в
`REVERSE_STATUS.md`/`REVERSE_EVIDENCE.md` (там работа шла на уровне
адресов регистров периферии, не сигнальных имён GPIO), но затрагивают
интерпретацию сигналов на уровне схемы — предыдущая версия карты пинов
считается устаревшей и заменена этим файлом целиком.

## Конфигурация GPIO по коду инициализации (FUN_08016274, обновлено 2026-09-05)

Прослежена **полная последовательность** (не только вызовы `HAL_GPIO_Init`,
но и вкраплённые между ними принудительные записи состояния через
`FUN_08005f06(port, pin_mask, value)` — аналог `HAL_GPIO_WritePin`, и чтения
через `FUN_08005efc(port, pin_mask)` — аналог `HAL_GPIO_ReadPin`) внутри
`FUN_08016274` (main).

### Кодировка `FUN_08005ccc` (уточнено)

`param_2` — структура `{Pin, Mode, Pull, Speed, Alternate}` (5 слов, как
`GPIO_InitTypeDef` в STM32 HAL). Важно: **`Mode & 3`** — это и есть
настоящее 2-битное значение регистра `MODER` (`00`=Input, `01`=Output,
`10`=Alternate Function, `11`=Analog); старший бит `0x10` — отдельный флаг
Open-Drain, не самостоятельный режим. То есть `Mode=0x11` — это не
"неизвестный режим 0x11", а `Output(01) + флаг Open-Drain`, `Mode=0x12` —
`AF(10) + Open-Drain`.

### Разрешённые адреса-указатели

| Символ | Адрес | Значение |
|---|---|---|
| `DAT_08016680` | `0x08016680` | `0x40021000` = **RCC** (используется для `RCC_AHBENR |= 0x20000\|0x40000\|0x80000` — включение тактирования GPIOA+GPIOB+GPIOC) |
| `DAT_08016684` (= `uVar18`) | `0x08016684` | `0x48000800` = **GPIOC** |
| `DAT_08016688` (= `uVar17`) | `0x08016688` | `0x48000400` = **GPIOB** |
| `DAT_0801668c` | `0x0801668c` | `0x20004650` — адрес в RAM, флаговая переменная (пишется как `*(iVar15+1)=1` по результату опроса кнопок при старте) |
| `DAT_08016690` | `0x08016690` | `0x10110000` = константа режима **`GPIO_MODE_IT_RISING`** (стандартное значение HAL) |

Прямых обращений к `&GPIOA_MODER` в этом коде нет через литерал-указатель —
Ghidra подписала его напрямую по SVD-метке, порт A используется без
косвенного `DAT_`.

### Полная таблица (порт, состояние до конфигурации, итоговая конфигурация)

| Порт.Пин | Принудительная запись до конфигурации | Итоговый Mode | Pull | Speed | Сигнал |
|---|---|---|---|---|---|
| PC13 | → 0 (LOW) | Output PP | NoPull | VeryHigh | не установлен |
| PA5 | → 0 (LOW) | *(конфигурируется позже отдельным вызовом, см. ниже)* | | | RANGE_SELECT |
| PB9 | (запись без явного 3-го аргумента — уточнить) | Output PP | NoPull | VeryHigh | VD_ON |
| PA1 | — | Input | PullUp | — | JOY_OK |
| PC15 | — | Input | PullDown | — | не установлен |
| *(опрос PC15+PA1 в цикле при старте — см. ниже)* | | | | | |
| PC13 | (см. выше, эта же настройка) | Output PP | NoPull | VeryHigh | — |
| PA9 | → ? (запись без явного 3-го аргумента — уточнить) | Output PP | NoPull | VeryHigh | TX |
| PB15 | — | Output PP | NoPull | VeryHigh | Display_CS |
| PB6 | — | Output PP | NoPull | VeryHigh | (физически NC) |
| PB7 | — | Output **Open-Drain** | NoPull | VeryHigh | (физически NC, вероятно резерв под I2C) |
| PA8 | — | Output PP | NoPull | VeryHigh | ZP |
| PA3 | **→ 1 (HIGH)** | Output PP | NoPull | VeryHigh | **TPS6300 EN — включается при старте** |
| PC14 | — | Input + **IT_RISING** (EXTI) | PullUp | — | **INT2 от BMI160** (подтверждено MussonOld) |
| PB11 | — | Output PP | NoPull | VeryHigh | BLE_ON |
| PB13 | **→ 0 (LOW)** | Output PP | NoPull | VeryHigh | AX_CS (CS активируется низким уровнем сразу при старте) |
| PB12 | — | Input | PullDown | — | JOY_B |
| **PA15** | — | **Input** | **NoPull** | — | Vbat/VBUS *(порт подтверждён — именно GPIOA, не спутать с соседними PC13/14/15 выше)* |
| PB0 | — | Output PP | NoPull | VeryHigh | ACLK (не SPI) |
| PB1 | — | Output PP | NoPull | VeryHigh | AMOSI/DIO (не SPI) |
| PA2 | — | Input | PullUp | — | JOY_C |
| PA5 | (см. выше, эта же настройка) | Output PP | NoPull | VeryHigh | RANGE_SELECT |
| PB2 | — | Output PP | NoPull | VeryHigh | I_CS |
| PB10 | — | Output PP | NoPull | VeryHigh | U_CS |

Все выводы настроены на скорость **VERY_HIGH**, кроме входов (для них Speed
не применяется).

### Важное уточнение по PC13/PC14/PC15 vs PA13/PA14/PA15

При первом проходе была путаница между этими двумя группами пинов, так как
конфигурации для pin13/14/15 на портах **A** и **C** расположены в коде
рядом и легко перепутать при беглом чтении. Разрешено окончательно по
аргументу порта (литералу `uVar18`/`DAT_08016684` = GPIOC против прямого
`&GPIOA_MODER`):

- **PC13, PC14, PC15** — отдельная группа на порту C (Output / EXTI-вход /
  Input+PullDown соответственно)
- **PA15** — отдельный, единственный вызов на порту A: `Input, NoPull`,
  не связан с конфигурацией PC13/14/15

### Открытые вопросы

1. **PA9 (сигнал TX) сконфигурирован как обычный GPIO Output, не Alternate
   Function.** Не проверено, переключается ли он в AF в другом месте кода,
   либо TX реализован программно.
2. **PB1 (DIO протокола PGA113) сконфигурирован здесь только как Output**,
   хотя протокол требует двунаправленной передачи — ожидается динамическое
   переключение режима в другом месте кода, не найдено.
3. **PC13/PC15** сконфигурированы, но их сигнальное назначение по схеме не
   установлено.
4. Два места принудительной записи (`PB9`, `PA9`) декомпилятор показал без
   явного третьего аргумента (`value`) — техническая неопределённость,
   требует уточнения через дизассемблер напрямую, не только декомпиляцию.
5. **Что физически на схеме подключено к PC13/14/15**, помимо уже
   подтверждённого PC14=INT2 BMI160 — предстоит установить (вероятный
   кандидат по более ранним схемам — заголовок с подписями
   INT1/INT2/Display_DC рядом с этой группой пинов).

### SPI3 (0x40003C00) — не используется

Прямой поиск (и по перекрёстным ссылкам Ghidra, и по сырым байтам образа)
показал **ноль обращений** к регистрам SPI3 (`0x40003C00`-`0x40003C10`)
во всей прошивке, несмотря на то что PB3/PB4/PB5 подтверждены на схеме как
физический SPI3 к дисплею. Вероятный вывод: дисплей управляется программно
(bit-bang), а не через аппаратный SPI3-периферию МК — тем же способом, что
и протокол PGA113 на PB0-PB2/PB10. При этом в разобранном блоке
`FUN_08016274` пины PB3/PB4/PB5 вообще не встречаются — их конфигурация
(если она есть) находится в другом месте кода, отдельно от общей
GPIO-инициализации main().

### PB6/PB7 — I2C, не задействован в проекте (CONFIRMED, MussonOld)

Несмотря на пометку "физически NC" выше, в CCM RAM (`0x1000xxxx`) найдена
функция (`0x10000B5C`), которая битбэнгом дёргает именно `PB6`/`PB7`
(низкий→высокий с задержками) через уже известный `FUN_08005f06`.
Проверено — эта функция нигде не вызывается (ни из флеша, ни из
остального кода CCM RAM), то есть на практике не выполняется.
**Подтверждено: это I2C (`PB7` сконфигурирован как Open-Drain), который в
этом проекте так и не был задействован.** Подробности —
`REVERSE_STATUS.md`, раздел 4.3.

### PA3 → TPS6300 EN — полная цепочка

- **Включение**: внутри `FUN_08016274` (main), `PA3 → HIGH` сразу после
  конфигурации PA8 (см. таблицу выше) — регулятор включается один раз при
  старте прошивки.
- **Выключение**: внутри `FUN_0800ee00` — `PA3 → LOW`, непосредственно
  после `PB9 (VD_ON) → LOW`. Судя по остальному содержимому этой функции
  (гашение через DMA, ожидание отпускания JOY_C, задержка 100мс) — это,
  по всем признакам, **процедура полного выключения питания**
  (вероятный обработчик команды `POWER_OFF` из списка командных строк).
  Вызывается из 6 мест, включая **обработчик TIM7** и main() — то есть
  TIM7, возможно, работает как таймер автовыключения по бездействию,
  продолжая логику countdown-таймера, найденную ранее в TIM6.
