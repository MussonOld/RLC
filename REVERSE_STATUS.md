# RLC Реверс-инжиниринг — Статус

Репозиторий: https://github.com/MussonOld/RLC
Источник: `nRLC_2_0_12_FREEWARE_ghidra_decompiled.txt` (декомпиляция Ghidra, 413 функций)
Дата последнего обновления: 2026-09-09 (гигиена статуса: §4.3/§10/§6.13/§6.17 приведены в соответствие с выводами §6.17–6.20 — снят устаревший "мёртвый код I2C" у 0x10000B5C, "канал 2" исправлен на "снимок" по §6.18, добавлены форвард-ссылки на отклонённые гипотезы; раздел 6.22: FUN_08000f52 дочитана полностью, запись Rs/Rp/L/C НЕ найдена нигде в функции, найден блок сглаживания в FUN_08009618, переосмыслены целевые смещения структуры)

## Текущий этап
**Этап 2 (векторы/IRQ/ISR верхнего уровня) — завершён.** Сейчас в работе
детальный разбор отдельных подсистем сверх этапа 2: полностью разобран
TIM7 (роль, реальный период — раздел 6.3-6.5), GPIO-карта вынесена в
отдельный файл `GPIO_MAP.md`, измерительный тракт по ADC4/PB14 разобран
до физического вывода (раздел 6.6). Текущая задача (раздел 13, пункт
21) — разбор крупной DSP-функции в CCM RAM (вероятное ядро корреляции
для измерения R/L/C): граница функции `thunk_EXT_FUN_100001b8`
**исправлена и расширена** до `0x100001b8..0x10000b27` (была ошибочно
неполной — `0x100008da..` оказался не соседней функцией, а продолжением
этой же функции внутри её тела, раздел 6.7), calling convention
установлена. Полная семантика (опорные таблицы, стадийный счётчик
`0x200049e8`, физические единицы) — ещё нет.

Цель:
`вектор -> IRQ -> обработчик -> периферия -> назначение ISR -> глобальные данные`

### Согласованный порядок анализа (2026-09-04)
Сначала якоря, а не угадывание функция за функцией: сначала устанавливаем,
кто и в каком контексте пишет в какие регистры периферии, и только потом
приписываем смысл отдельным функциям.

1. `Reset_Handler` по адресу `0x080023B8` — якорь. Ожидается: инициализация
   `.data`, очистка `.bss`, куда передаётся управление в `main`, какие
   функции инициализации периферии вызываются отсюда. **СДЕЛАНО 2026-09-04
   — см. раздел 2.** Результат: 4-инструкционный трамплин, вызывающий
   `FUN_0800ff68`, затем хвостовая цепочка через `0x08000188` ->
   `FUN_08002d48` -> `FUN_08016274` (подтверждённый main()). Явного
   inline-цикла `.data`/`.bss` в самом `Reset_Handler` не найдено — это
   должно происходить внутри одной из трёх вызываемых функций, пока не
   разобранных по отдельности.
2. `SysTick_Handler` по адресу `0x0800FDCC` — какой регистр/флаг трогает,
   есть ли программный тик/планировщик, реальный период. **СДЕЛАНО
   2026-09-04 — см. раздел 4.2.** Результат: разрешает вопрос про
   `FUN_0800019c`, ожидавший шага 3 — это тело SysTick, достигается через
   хвостовой вызов, а не обычный `bl`. Значение reload / реальный период
   всё ещё открыты.
3. Подтверждённые векторами ISR, в этом порядке (все адреса взяты
   напрямую из таблицы векторов, см. раздел 4.1):
   `DMA1_CH1 0x08000644`, `DMA1_CH3 0x08004010`, `DMA1_CH4 0x0800401C`,
   `DMA1_CH5 0x08004028`, `USB 0x080126E4`, `USART1 0x08010B5C`,
   `TIM6 0x0800237C`, `TIM7 0x0800FF78`, `DMA2_CH1 0x08004034`,
   `DMA2_CH3 0x08004040`. TIM6/TIM7 + DMA — приоритетная пара, вероятный
   источник реальной временной архитектуры.

### Явно НЕ делаем пока
- восстановление `main.c` по именам Ghidra;
- приписывание `FUN_0800019c` какого-либо назначения;
- вывод назначения DMA-канала только по номеру канала;
- слепой поиск значений PSC/ARR по всему бинарнику.

## Правила
- CONFIRMED = прямое подтверждение из кода/регистра/вектора/xref.
- PROBABLE = сильный вывод, но пока напрямую не доказанный.
- UNKNOWN = недостаточно доказательств; не гадать.
- Не переименовывать функции Ghidra окончательно без доказательств.
- Не писать финальный аппаратный/прикладной код до установления
  соответствующих связей.

## 1. МК / карта памяти

CONFIRMED: прошивка относится к классу STM32F3 (STM32F303), проверено
независимо по официальному `STM32F303.svd` от ST:

- Таблица векторов содержит ADC1_2, USB_HP_CAN_TX, TIM8, ADC3, ADC4 и семь
  индивидуально векторизованных каналов DMA1 (DMA1_CH1..DMA1_CH7).
- `0x50000000` (общие регистры ADC1_2) используется в прошивке напрямую.
- Начальный указатель стека — `0x20009968`, то есть ~39.4 КБ от начала
  SRAM (`0x20000000`) — согласуется с 40 КБ SRAM у STM32F303xB/C.
- Базовый адрес GPIOB `0x48000400` напрямую совпадает с адресом GPIOB у
  STM32F303 по официальному SVD.

Обнаруженные базовые адреса периферии (подписаны по STM32F303.svd):
- GPIO: `0x48000000` (GPIOA), `0x48000400` (GPIOB), `0x48000800` (GPIOC),
  `0x48000C00` (GPIOD), `0x48001000` (GPIOE)
- SYSCFG: `0x40010000` (включая `0x40010008`)
- USB: `0x40005C00`
- TIM1: `0x40012C00`
- TIM15: `0x40014000` (структура регистров подтверждена по SVD; прямых
  обращений в прошивке не найдено — см. примечание ниже)
- TIM16: `0x40014400` (использование подтверждено)
- TIM17: `0x40014800` (структура регистров подтверждена по SVD; прямых
  обращений в прошивке не найдено — см. примечание ниже)

Точный номер модели: **STM32F303CCT6 — CONFIRMED, подтверждено MussonOld
2026-09-06 по физической маркировке на корпусе снятого с прибора
(нерабочего) МК.** Согласуется и с косвенным доказательством по размеру
образа: (~185 КБ для `nRLC_2_0_12_FREEWARE.hex`) превышает 128 КБ Flash у
STM32F303CB, что соответствует 256 КБ Flash у CC — несмотря на то что на
шёлкографии схемы для этой платы указан CB (маркировка на схеме не
совпадает с реально распаянным чипом).

Примечание про TIM15/TIM17: перекрёстная сверка каждого подписанного по
SVD адреса регистра TIM15/TIM17 с реальными операндами инструкций в
прошивке дала ноль прямых совпадений для обоих таймеров. Это не
опровергает их использование (код может обращаться к ним через
параметризованный/косвенный драйвер — та же схема подтверждена для
GPIO_Init в `FUN_08005cac`), но атрибуцию "TIM15/16/17-related" для
`FUN_08008384`/`FUN_080101b4` понижена до PROBABLE в разделе 10, пока не
найдены прямые доказательства; использование TIM16 подтверждено отдельно, прямой ссылкой.

## 2. Старт / main — CONFIRMED 2026-09-04 (полностью прослеженная цепочка, не по шаблону)

Прослежено напрямую от `Reset_Handler` вперёд — каждый переход ниже —
это проверенная цель `blx`/`bl`/хвостового перехода, прочитанная из
реальных операндов инструкций и литерал-пула, а не выведенная по
близости адресов:

```
Reset_Handler (0x080023B8, всего 4 инструкции)
  |-- blx FUN_0800ff68        (0 вызываемых функций, не трогает ни одной
  |                             подписанной периферии — вероятно,
  |                             низкоуровневая настройка: тактирование/FPU/watchdog)
  `-- хвостовой переход (bx) -> трамплин @ 0x08000188
        |-- переустановка SP (ldr.w sp, [pc, #0xc])
        |-- bl FUN_08002d48   (2 вызываемых функции, периферия не
        |                       подписана — вероятно, инициализация
        |                       рантайма C: копирование .data / обнуление .bss)
        `-- хвостовой переход (bx, через литерал) -> FUN_08016274
              (84 вызываемых функции; трогает ADC4, CRC, DBGMCU, GPIOA, GPIOB,
               GPIOC, IWDG, RCC, SPI1, TIM1, TIM16, TIM2, TIM3 —
               это CONFIRMED как эквивалент application main(): широкая
               инициализация железа/периферии, установлено прямым
               прослеживанием цепочки вызовов от Reset_Handler, а не
               выводом по шаблону)
```

`FUN_08016274` выполняет инициализацию железа/GPIO, загрузку конфигурации
и настройку периферии, затем (пока не прослежено дальше) предположительно
входит в основной цикл приложения.

`FUN_0800019c` НЕ достижима через эту цепочку — ни одна из четырёх функций
выше её не вызывает и на неё не переходит. Это не разрешает вопрос, как
достигается `FUN_0800019c` (см. раздел 3), а лишь исключает объяснение
"часть обычного старта".

`FUN_08000608()` похожа на функцию основного цикла/конечного автомата
приложения, не ISR (адрес/роль перенесены из прежнего анализа; ещё не
перепроверены на 2.0.12 прямым прослеживанием).

## 3. Кандидат в ISR — статус пересмотрен 2026-09-04, РАЗРЕШЕНО 2026-09-04 (см. раздел 4.2)

**Разрешено: `FUN_0800019c` — это тело обработчика SysTick, достигается
через хвостовой переход `b.w` из `SysTick_Handler` — полное обоснование
см. в разделе 4.2.** Цепочка рассуждений ниже сохранена для истории (она
верно определила, что находка "ноль вызывающих" была недостаточным
доказательством мёртвого кода — что в итоге и подтвердилось).

`FUN_0800019c @ 0x0800019c` — оценивалась как сильный кандидат в
ISR/периодический обработчик на основании нуля прямых вызывающих через
`bl` плюс улик из тела функции (счётчики, поля времени/событий,
периодические аппаратные действия).

**Проверка таблицы векторов (в этой сессии): прямое опровержение ISR-через-вектор.**
Прочитана полная таблица векторов `nRLC_2_0_12_FREEWARE.hex` (первые 100
записей, IRQ -16..84), выполнен поиск `0x0800019c` — не найдено ни в одной
позиции вектора. Также выполнен поиск по всему образу прошивки буквального
32-битного значения `0x0800019c`/`0x0800019d` (с установленным Thumb-битом)
как сырых данных — ноль совпадений где-либо, то есть адрес не хранится и
ни в одной обычной таблице указателей на функции.

**Новая зацепка:** в прошивке обнаружено 14 инструкций Thumb-2 `TBB`/`TBH`
(компактные PC-относительные таблицы перехода, обычно компилируются из
`switch`) по адресам, включая `0x08002258`, `0x0800c7c2`,
`0x0800c862`, `0x0800c9a4`, `0x0800caa6`, `0x0800cd50`, `0x0800df0a`,
`0x0800fd34`, `0x08010714`, `0x08010736` (+ ещё 4, см. декомпиляцию). Это
наиболее вероятный способ достичь `FUN_0800019c` — и, возможно, командного
диспетчера за подтверждёнными командными строками из раздела 9 — раз поиск
по `bl`/сырым указателям исключает два более распространённых механизма.
Ни одна из 14 таблиц пока не декодирована, чтобы подтвердить или исключить
цель перехода на `0x0800019c`.

**Статус: статический вызывающий не найден ни одним из четырёх проверенных
механизмов (прямой вызов `bl`, таблица векторов, сырой указатель-данные,
таблица переходов TBB/TBH). Это НЕ доказательство того, что функция
мёртвая/неиспользуемая — остаётся несколько непроверенных механизмов
достижения (см. список ниже). Корректная формулировка: "`FUN_0800019c` не
доказана как ISR и пока не имеет статически обнаруженного вызывающего" —
а НЕ "`FUN_0800019c` — мёртвый код."** Улики из тела функции (счётчики,
поля времени/событий, периодические действия) по-прежнему в силе и не
исключают ISR; изначальная оценка "сильный кандидат" опиралась на
допущение (нет вызывающих ⇒ ISR), которое опровергает третий механизм
диспетчеризации (TBB/TBH), поэтому уровень уверенности понижается до
UNKNOWN в ожидании дальнейших доказательств, а не до "не используется."

**Результат декодирования TBB/TBH (в этой сессии): отрицательный.**
Декодированы все 14 таблиц TBB/TBH (границы определены по предшествующей
инструкции `cmp`/проверке границ, плюс щедрое повторное сканирование на
60 записей при обоих возможных вариантах выравнивания таблицы для
подстраховки) — ни одна из 14 таблиц не содержит записи, ведущей на
`0x0800019c`. В сочетании с поисками по таблице векторов и сырым данным
выше, **ни один из четырёх проверенных на сегодня механизмов достижения
(прямой вызов, таблица векторов, сырой указатель, таблица переходов
TBB/TBH) не объясняет, как вызывается эта функция.**

Оставшиеся гипотезы, ни одна пока не исследована:
- вызов через вычисляемый адрес в регистре (`BX`/`BLX Rn`), где регистр
  загружается не из статического литерала и не из уже проверенных таблиц;
- адрес формируется арифметически во время выполнения (база + вычисляемое
  смещение), а не читается из какой-либо фиксированной таблицы;
- `FUN_0800019c` — точка входа, достигаемая механизмом, отличным от
  обычного вызова — например, вторичная/цепная таблица векторов, передача
  управления из загрузчика или подобное — пока не проверено;
- граница функции, назначенная Ghidra, неточна — реальная, на которую
  реально ссылаются, точка входа может быть по другому адресу внутри или
  рядом с этим кодом, а `0x0800019c` — лишь удобная (но неверная) точка
  разбиения;
- мёртвый/неиспользуемый код (например, процедура из статически
  слинкованной библиотеки, реально никогда не вызываемая в этой сборке)
  — перечислена только для полноты; не имеет приоритета над остальными
  гипотезами выше и не должна восприниматься как рабочий вывод.

**Дополнение (та же сессия): исчерпывающий поиск по таблице векторов
завершён, по-прежнему отрицательно; отзыв ошибочной трактовки.**
Исходный поиск по таблице векторов (idx 0..99) пропустил последнюю запись
таблицы (idx 100 = IRQ84 = SPI4); перепроверено до idx 105 — по-прежнему
ноль совпадений для `0x0800019c`/`0x0800019d`. Реальная таблица векторов
занимает `idx 0..100` (адреса `0x08000000`-`0x08000190`), заканчивается
на IRQ84, сразу за которым следует небольшой литерал-пул (значения
`0x08016275` и `0x20009968` в idx 101/102 — это данные, не записи
вектора; это константы, читаемые последовательностью вычисляемого
перехода `ldr r0,[pc,#0]; bx r0` по адресу `0x08000190`-`0x08000192`,
ведущей на `0x08016275`). Более ранняя заметка в этом разделе,
описывавшая эту область как "мини Reset_Handler / возможно встроенный
вторичный образ" — была ошибочной трактовкой: это обычный хвост таблицы
векторов + литерал-пул + один вычисляемый переход, не связанный с
`FUN_0800019c`, а не отдельная встроенная структура. `FUN_0800019c`
начинается сразу после этого литерал-пула по адресу `0x0800019c`, что
выглядит случайным размещением, а не доказательством чего-либо.

Итог: гипотеза про таблицу векторов теперь подтверждена как исчерпывающе
мёртвая (не просто "вероятно" мёртвая). Три оставшиеся гипотезы выше без
изменений; путь через вычисляемый-адрес-датафлоу, вероятно, самый
перспективный следующий шаг, учитывая, что вычисляемый переход в стиле
`bx r0` только что был найден в одной функции раньше в образе — стоит
проверить, не ведут ли похожие паттерны в других местах прошивки именно
на `0x0800019c`.

Важно: НЕ определять её таймер исключительно по адресу/порядку функции.

## 4. Доказательства NVIC / IRQ

`FUN_0800602e(IRQ)` — **не подтверждён**; роль `NVIC_EnableIRQ`
подтверждена по адресу `0x0800604E` (`FUN_0800604e`) — см. раздел 10.

`FUN_0800604c(...)` — **не подтверждён**; роль `NVIC_SetPriority`
подтверждена по адресу `0x0800606C` (`FUN_0800606c`) — см. раздел 10.

### 4.1 Таблица векторов — CONFIRMED (прямое чтение таблицы векторов nRLC_2_0_12_FREEWARE.hex, сверено с официальной картой прерываний STM32F303.svd)

| IRQ | Обозначение STM32F303 | Адрес обработчика | Статус |
|---:|---|---|---|
| 11 | DMA1_CH1 | `0x08000644` | реализован; уникальный обработчик, разобран целиком — см. раздел 4.4 |
| 13 | DMA1_CH3 | `0x08004010` | реализован; общий диспетчер `FUN_080059ba` — см. раздел 4.4 |
| 14 | DMA1_CH4 | `0x0800401C` | реализован; общий диспетчер `FUN_080059ba` — см. раздел 4.4 |
| 15 | DMA1_CH5 | `0x08004028` | реализован; общий диспетчер `FUN_080059ba` — см. раздел 4.4 |
| 20 | USB_LP_CAN_RX0 | `0x080126E4` | реализован |
| 37 | USART1_EXTI25 | `0x08010B5C` | реализован |
| 40 | EXTI15_10 | `0x10000B28` | реализован; тело перенесено в CCM RAM (0x1000xxxx) |
| 54 | TIM6_DACUNDER | `0x0800237C` | реализован |
| 55 | TIM7 | `0x0800FF78` | реализован |
| 56 | DMA2_CH1 | `0x08004034` | реализован; общий диспетчер `FUN_080059ba` — см. раздел 4.4 |
| 57 | DMA2_CH2 | `0x080023D2` | реализован |
| 58 | DMA2_CH3 | `0x08004040` | реализован; общий диспетчер `FUN_080059ba` — см. раздел 4.4 |
| 59 | DMA2_CH4 | `0x080023D2` | **не реализован** — тот же адрес, что дефолтный/необработанный обработчик IRQ |
| 60 | DMA2_CH5 | `0x080023D2` | **не реализован** — тот же адрес, что дефолтный/необработанный обработчик IRQ |
| 61 | ADC4 | `0x080023D2` | **не реализован** — тот же адрес, что дефолтный/необработанный обработчик IRQ |

Примечание об исправлении: в более раннем проходе IRQ54-59 были
сопоставлены как DMA2_Channel1..DMA2_Channel4/ADC4 сразу вслед за IRQ40,
что ошибочно пропустило позиции вектора TIM6_DACUNDER (54) и TIM7 (55),
реально присутствующие в таблице векторов STM32F303. Таблица выше
заменяет то сопоставление; это прямое чтение таблицы векторов, сверенное
со списком прерываний SVD, а не реконструкция по памяти.

### 4.2 SysTick — РАЗРЕШЕНО 2026-09-04

`SysTick_Handler @ 0x0800FDCC` (подтверждено по таблице векторов idx=15,
исключение Cortex-M 15 = SysTick, значение `0x0800FDCD` совпадает точно):

```
push {r4, lr}
bl   FUN_08005f88      ; обычный вызов, ещё не разобран
pop  {r4, lr}
b.w  0x0800019c          ; ХВОСТОВОЙ ВЫЗОВ (branch, не bl) в FUN_0800019c
```

Это разрешает загадку `FUN_0800019c` из раздела 3: она достигается через
безусловный хвостовой переход (`b.w`), а не инструкцию `bl` — вот почему
каждый поиск вызывающих на основе `bl` в разделе 3 давал ноль результатов.
Это никогда не был мёртвый код; просто механизм достижения не входил в
число четырёх проверенных. **`FUN_0800019c` подтверждена (CONFIRMED) как
тело обработчика SysTick**, выполняется на каждый тик SysTick после того,
как `SysTick_Handler` сначала вызывает `FUN_08005f88`. Исходные улики из
тела функции в разделе 3 (счётчики, поля времени/событий, периодические
аппаратные действия) теперь объяснены, а не просто согласуются.

`FUN_08005f88` — ещё не разобрана; вероятный кандидат на счётчик
миллисекундного тика или подобный учёт рядом с SysTick, не подтверждено.

Всё ещё открыто: значение перезагрузки SysTick (`SYST_RVR`) / реальный
период тика пока не извлечены — нужны, чтобы перевести счётчики
`FUN_0800019c` в реальные единицы времени.

`FUN_08007770()` ранее числилась как "настраивает SysTick" — адрес
**не проверен и не подтверждён**; не полагаться на неё до перепроверки.

## 5. USB — ISR РАЗРЕШЕНА 2026-09-06

Периферия `0x40005C00`: очень высокая уверенность, USB.

IRQ `20 (0x14)`: связь с высокой уверенностью.

### 5.1 Точка входа ISR — CONFIRMED (прямое дизассемблирование Thumb-2, Capstone)

Обработчик из таблицы векторов по адресу `0x080126E4` — не отдельная
функция Ghidra (она попадает в промежуток между `FUN_080126d0` и
`FUN_080126f0`, которые Ghidra извлекла как отдельные крошечные функции,
пропустив код между ними). Прямое дизассемблирование байт по этому
адресу даёт:

```
0x080126e4:  ldr   r0, [pc, #4]      ; r0 = *(0x080126ec) = 0x200072b4
0x080126e6:  b.w   0x08006328        ; ХВОСТОВОЙ ВЫЗОВ, не bl
```

То есть реальная ISR — это двухинструкционный трамплин: загружает
указатель на статический объект в SRAM (`0x200072b4`, вероятно
дескриптор/handle USB-периферии) в `r0` и хвостовым переходом передаёт
управление в `FUN_08006328(r0)`. Этот же паттерн (литерал-пул + `b.w`)
уже был confirmed для `SysTick_Handler` -> `FUN_0800019c` (раздел 4.2) —
похоже, это общая идиома компилятора/HAL в этой прошивке для тонких ISR
над общим обработчиком.

### 5.2 `FUN_08006328` — CONFIRMED как основной обработчик событий USB (аналог `HAL_PCD_IRQHandler`)

Тело функции (`param_1` = указатель на дескриптор USB, тот самый
`0x200072b4`) — последовательность из 8 проверок бит через
`FUN_080126f0(*param_1)` (подтверждённый геттер, читает 16-битное поле по
смещению `0x44` от `*param_1` — см. `FUN_080126f0`/`FUN_080126d0` выше),
с масками, **точно совпадающими с раскладкой регистра `USB_ISTR` USB
FS Device controller у STM32F3** (не USB OTG):

| Маска | Бит `USB_ISTR` | Действие в `FUN_08006328` |
|---|---|---|
| `0x8000` | CTR (успешная передача) | вызывает `FUN_0800e754(param_1)` |
| `0x400` | RESET | сбрасывает флаг, вызывает `FUN_0800659c`, при первом входе (флаг по смещению `0xaa`) вызывает `FUN_08012728` |
| `0x4000` | PMAOVR | только сброс флага |
| `0x2000` | ERR | только сброс флага |
| `0x1000` | WKUP | сброс флага + `FUN_080065c2` |
| `0x800` | SUSP | сброс флага + `FUN_0800663c` |
| `0x200` | SOF | сброс флага + `FUN_080065ca` |
| `0x100` | ESOF | сброс флага, `return` |

Это классическая структура `HAL_PCD_IRQHandler()`: опрос `ISTR`,
пооче­рёдная обработка и очистка каждого установленного бита. Смещение
`0x40` от `*param_1` (используется в `FUN_08006328` и `FUN_080126d0`)
согласуется с регистром `USB_CNTR` (управление/маски прерываний) в том
же дескрипторе, `0x44` — с `USB_ISTR`.

**Вывод: USB-периферия работает в режиме Device (не Host), использует
встроенный USB FS controller STM32F3 (не OTG), обработчик событий —
`FUN_08006328`, вызывается из ISR по адресу `0x080126E4` через
хвостовой трамплин.** Дочерние обработчики по каждому биту
(`FUN_0800e754` — CTR/endpoint transfer, `FUN_0800659c`/`FUN_08012728`
— RESET/enumeration, `FUN_080065c2/0x663c/0x65ca` — WKUP/SUSP/SOF) пока
не разобраны по отдельности — это следующий уровень детализации, не
обязательный для завершения текущего этапа (карта векторов/IRQ верхнего
уровня).

### 4.3 CCM RAM (`0x1000xxxx`) — механизм инициализации найден, `EXTI15_10` и `thunk_EXT_FUN_10000bc0` разобраны по байтам — CONFIRMED 2026-09-06

Таблица копирования из Reset-цепочки (используется функцией, вызываемой из
`FUN_08002d48` через литерал-таблицу по адресу `0x0802cfec..0x0802d01c`, 3
записи вида `{src, dst, len, func}`) содержит:

```
src=0x0802D01C dst=0x20000000 len=0x68C8 func=0x08002D6C   ; .data -> SRAM
src=0x0802D7DC dst=0x10000000 len=0x0C18 func=0x08013690   ; -> CCM RAM
src=0x0802D7DC dst=0x200068C8 len=0x30A0 func=0x080136A0   ; .bss zero-fill
```

`FUN_08013690` — простой почётное копирование словами (`ldm r0!,{r3};
stm r1!,{r3}` в цикле), без сжатия. Значит содержимое CCM RAM в момент
старта — это **прямая byte-in-byte копия** флеша `0x0802D7DC..0x0802D7DC+0xC18`,
и его можно читать прямо из `.hex`, не выполняя код.

Извлечены и дизассемблированы (Capstone, Thumb-2) по вычисленному
смещению `flash = 0x0802D7DC + (ram - 0x10000000)`:

**`EXTI15_10` @ `0x10000B28`:**
```
ldr  r0, [pc, #0x28]      ; r0 = указатель на регистр EXTI_PR (литерал)
ldr  r1, [r0]
tst.w r1, #0x8000          ; бит 15 (EXTI line 15)
itt ne
movne.w r1, #0x8000
strne r1, [r0]              ; если установлен — сбросить (write-1-to-clear), без иной обработки
ldr  r1, [r0]
tst.w r1, #0x4000           ; бит 14 (EXTI line 14 = PC14 = INT2 BMI160, см. раздел 6.1)
it eq
bxeq lr                     ; если бит14 не установлен — выйти, ничего не делать
mov.w r1, #0x4000
str  r1, [r0]                ; сбросить бит14
ldr  r0, [pc, #0xc]           ; указатель на структуру состояния
ldrb r1, [r0, #6]
adds r1, r1, #1
strb r1, [r0, #6]              ; инкремент однобайтового счётчика по смещению 0x6 от структуры
bx   lr
```
**Вывод (CONFIRMED):** обработчик реагирует только на линию EXTI14
(`PC14`/`INT2 BMI160`) — увеличивает счётчик событий по смещению `0x6` в
некой глобальной структуре (вероятно, счётчик готовых выборок
акселерометра, читается где-то ещё, не прослежено). Линия EXTI15
сбрасывается, но не обрабатывается — вероятно, конфигурируется по
умолчанию HAL-библиотекой на неиспользуемом пине и просто гасится, чтобы
не зависать в IRQ.

**`thunk_EXT_FUN_10000bc0(ms)` @ `0x10000BC0`:**
```
ldr    r1, [pc, #0x28]        ; r1 = указатель на структуру состояния бипера
ldrb   r2, [r1]                ; r2 = байт-флаг "занят/активен"
cmp    r2, #1
itte   eq
ldrheq r2, [r1, #2]             ; если флаг==1: r2 = текущий остаток длительности (halfword по смещ. 2)
cmpeq  r2, #0
bxne   lr                        ; если флаг!=1 ИЛИ остаток!=0 — уже пищит, новый запрос игнорируется
mov.w  r2, #0x32                  ; r2 = 50
ldr    r3, [pc, #0x1c]
str    r2, [r3]                    ; записать 50 в другой регистр/переменную (вероятно период/скважность тона)
movw   r2, #0xcccd                  ; классическая reciprocal-константа для деления на 10
mul    r0, r0, r2                    ; r0 (параметр ms) * 0xCCCD
lsr.w  r0, r0, #0x13                  ; >> 19  =>  ms / 10  (оптимизированное компилятором целочисленное деление)
strh   r0, [r1, #2]                    ; сохранить как "оставшуюся длительность в тиках по 10мс"
ldr    r0, [pc, #0xc]
b.w    #0x10000c0c                      ; хвостовой переход — запуск таймера/тона
```
**Вывод (CONFIRMED, повышено с PROBABLE):** это неблокирующий драйвер
пищалки/зуммера — принимает длительность в миллисекундах, переводит её в
тики по 10мс (`ms/10`) и запускает генерацию тона; повторный вызов, пока
предыдущий сигнал ещё звучит, игнорируется. Физический вывод — `PA8`/`ZP`
(пьезоизлучатель, см. `GPIO_MAP.md`). Логика деления на 10 (`0xCCCD`,
`>>19`) — стандартная compiler-generated идиома, не ручной код, что
подтверждает: это компилированный Си/HAL-код, перенесённый в CCM RAM ради
скорости (типично для ISR-контекста, где это выполняется).

**Функция @ `0x10000B5C` разобрана по байтам — CONFIRMED, 2026-09-06;
статус вызова ПЕРЕСМОТРЕН 2026-09-08/09 (см. разделы 6.19/6.20 —
"нигде не вызывается" ниже ОПРОВЕРГНУТО, 20+ реальных вызовов найдены
из `FUN_08000f52` через двухинструкционный вениир `0x08002de0`, не
через прямой литерал-указатель, поэтому поиск ниже его и не находил).
Сразу за `EXTI15_10` в том же CCM-блоке находится отдельная
функция:
```c
void FUN_10000b5c(void)
{
    FUN_08005f06(GPIOB, 0x40, 0);   // PB6 = LOW
    delay(~256 итераций busy-wait);
    FUN_08005f06(GPIOB, 0x80, 0);    // PB7 = LOW
    delay;
    FUN_08005f06(GPIOB, 0x40, 1);     // PB6 = HIGH
    delay;
    FUN_08005f06(GPIOB, 0x80, 1);      // PB7 = HIGH
    delay;
}
```
Порт подтверждён чтением литерала — `0x48000400` = GPIOB. Изначальная
гипотеза (протокол PGA113) **не подтвердилась**: реальные линии протокола
PGA113 — `PB0`/`PB1`/`PB2`/`PB10` (см. `GPIO_MAP.md`), а не `PB6`/`PB7`.
По `GPIO_MAP.md`, `PB6` и `PB7` на этой ревизии платы **физически не
подключены** (NC), при этом `PB7` сконфигурирован как Open-Drain — это
типичная конфигурация для I2C, что согласуется с более ранней пометкой в
`GPIO_MAP.md` "вероятно резерв под I2C".

Поиск вызывающего кода **в этой сессии (2026-09-06)** дал ноль
результатов двумя способами: (1) побайтовый поиск литерала
`0x10000B5C`/`...5D` (с флагом Thumb) по всему `.hex`; (2) полный
дизассемблированный проход по блоку CCM RAM на предмет `bl`/`b.w` с
этим адресом как целью. **Это ОШИБОЧНЫЙ вывод, найдена причина:**
реальные вызовы идут через linker veneer-таблицу `0x08002de0`
(`movw/movt ip,...; bx ip`), не через прямой литерал-указатель на
`0x10000B5C` — оба использованных здесь метода поиска такой вызов
принципиально не находят. См. раздел 6.19 (20+ вызовов из
`FUN_08000f52`) и раздел 6.17 (сама veneer-таблица).

**Вывод (ПЕРЕСМОТРЕН, см. 6.19/6.20 — актуальная формулировка):** это
НЕ мёртвый код — функция выполняется регулярно (20+ раз за цикл
`FUN_08000f52`). Байткод (GPIOB `PB6`/`PB7` push-pull/open-drain
toggle) не изменился и остаётся CONFIRMED. Но на данной ревизии платы
`PB6`/`PB7` физически не разведены (NC, подтверждено MussonOld) —
код электрически инертен (не влияет на функциональность), хотя и
исполняется. Атрибуция "битбэнг-инициализация I2C" — PROBABLE, не
CONFIRMED (альтернативная гипотеза "строб/защёлка дисплея" рассмотрена
и отклонена в разделе 6.20 — open-drain нетипичен для CS/DC).

### 4.4 Обработчики DMA (IRQ 11/13/14/15/56/58) — CONFIRMED, 2026-09-06

Раздел 13, пункт 3 ("сопоставить адреса обработчиков с функциями Ghidra")
— продолжение для оставшихся IRQ из таблицы 4.1.

**Пять из шести — общий HAL-подобный диспетчер.** Прямое дизассемблирование
показывает, что `DMA1_CH3` (`0x08004010`), `DMA1_CH4` (`0x0800401C`),
`DMA1_CH5` (`0x08004028`), `DMA2_CH1` (`0x08004034`), `DMA2_CH3`
(`0x08004040`) — все пять используют **один и тот же** двухинструкционный
трамплин (`ldr r0,[pc,#4]; b.w 0x080059ba`), с уникальным для каждого
канала указателем-хэндлом в SRAM (`0x20006d04`, `0x20006d48`,
`0x20006d8c`, `0x20006c68`, `0x20006cc0` соответственно). `FUN_080059ba`
разобрана — это классический **`HAL_DMA_IRQHandler`**: проверяет флаги
`TC`/`HT`/`TE` в структуре хэндла (со сдвигом на позицию канала), сбрасывает
соответствующий бит и вызывает один из трёх callback-указателей хэндла
(`XferCpltCallback`/`XferHalfCpltCallback`/`XferErrorCallback` по
структуре, смещения `0xc`/`0xa`/`0xb`). Сами callback'и для каждого из 5
хэндлов не прослежены — следующий уровень детализации, не обязателен для
завершения текущего этапа.

**`DMA1_CH1` (IRQ 11, `0x08000644`) — единственный, НЕ использующий общий
диспетчер, разобран целиком:**

```
push {r4,r5,r6,lr}
r5 = 0x40020000                 ; DMA1 (базовый адрес подтверждён по SVD)
r4 = 0x200048a0                 ; указатель на буфер/структуру, не идентифицирована
if (DMA1_ISR & 2) {             ; TCIF1 — transfer complete, канал 1
    DMA1_IFCR = 2               ; сброс флага (write-1-to-clear)
    r2 = *(halfword*)(r4+2)
    r1 = r2                      ; ветка TC: r1 == r2 (см. ниже)
    r0 = 0x20007840              ; константа/объект — общий для обеих веток
    bl thunk_EXT_FUN_100001b8    ; см. ниже
}
if (DMA1_ISR & 4) {             ; HTIF1 — half-transfer, канал 1
    DMA1_IFCR = 4
    r2 = *(halfword*)(r4+2)
    r1 = 0                       ; ветка HT: r1 принудительно 0, r1 != r2 здесь
    r0 = 0x20007840
    b.w thunk_EXT_FUN_100001b8   ; хвостовой вызов
}
pop {r4,r5,r6,pc}
```

**ИСПРАВЛЕНИЕ 2026-09-08:** формулировка выше (`r1 = *(halfword*)(r4+2)`
одинаково для обеих веток) была неточной — прямая повторная сверка по
реальным инструкциям (не по пересказу) показала, что `r1` и `r2`
различаются между ветками TC/HT: в TC `r1` копируется из `r2` (то есть
`r1 == r2`), в HT `r1` явно обнуляется, а `r2` остаётся равен halfword-
значению `*(r4+2)` в обеих ветках. Это меняет вызов `thunk_EXT_FUN_100001b8`
с двухаргументного (`r0`,`r1`) на трёхаргументный (`r0`,`r1`,`r2`) —
регистр `r2` ранее не был замечен как значащий параметр. Полный разбор
последствий — раздел 6.7.

Проверка ошибки (`TEIF1`, бит 3) отсутствует — обработчик реагирует
только на `TC`/`HT`. Такая пара (обработка и половины, и полного
транспонирования одним и тем же путём с тем же указателем-объектом)
—典型 паттерн **непрерывного (circular) DMA-захвата с двойной
буферизацией**, что для устройства измерения RLC весьма вероятно связано
с потоковым чтением АЦП или потоковой генерацией сигнала ЦАП.

**Источник данных (добавлено 2026-09-08):**
`DMA1_CH1` = `ADC1_DR` (`0x50000040`), `CMAR1` = `0x200058E8` — **CONFIRMED**.
`DMA2_CH1` = `ADC2_DR` (`0x50000140`), `CMAR1` = `0x20005758` — **CONFIRMED**.
Канал ADC1 rank1 = Channel 1 = PA0 (I_Measure) — **CONFIRMED**.
Параллельно ADC2 rank1 = Channel 3 = PA6 (U_Measure) — **CONFIRMED**.
Семантика буферов сэмплов и роль как *единственного* acquisition path —
PROBABLE. Полный разбор — раздел 6.14.

**`thunk_EXT_FUN_100001b8` (`0x08002dcc` -> CCM RAM `0x100001b8`) — новая
крупная находка, CONFIRMED структура входа/тип, семантика PROBABLE:**
Тело в CCM RAM (та же техника извлечения, что в разделе 4.3: копия из
флеша по формуле `flash = 0x0802D7DC + (ram - 0x10000000)`) — это **не
маленький тонкий thunk**, а большая функция: только в первых ~1350 байтах
(340+ инструкций) уже насчитано **42 инструкции `SMLAD`** (signed
multiply-accumulate dual — ядро DSP-расширения Cortex-M4). Видна работа с
таблицей, индексируемой через `lsl #2` (характерно для таблицы отсчётов
синуса/косинуса), и накопление через `smlad` в паре аккумуляторов.

**Обновление 2026-09-08: полная граница функции найдена — CONFIRMED, см.
раздел 6.7.** Функция занимает `0x100001b8..0x100008d9` (0x722 = 1826
байт), стек-фрейм (push/sub sp при входе и add sp/pop на обоих выходах)
сходится побайтово. Сразу после неё, вплотную (без зазора), начинается
следующая функция `0x100008da..` — её тело и границы в этой сессии не
разбирались, это отдельная открытая задача (см. раздел 13).

**PROBABLE, крупная находка для раздела 11 ("расчёт R/L/C",
"временная диаграмма измерения"):** это весьма вероятно ядро алгоритма
измерения — синхронное детектирование/корреляция буфера АЦП с опорными
синус/косинус-таблицами (типичный метод для LCR-метров: перемножение
выборок с опорными сигналами и накопление даёт амплитуду/фазу
импеданса). **Не CONFIRMED** — сама формула, входные/выходные данные и
физические единицы не восстановлены; полный семантический разбор
продолжается в разделе 6.7.

`FUN_08005cac()` — низкоуровневая процедура настройки GPIO (адрес
не подтверждён; см. раздел 10).

Настраивает режим/вход/выход/подтяжку/альтернативную функцию и состояние,
связанное с EXTI, обращается к SYSCFG.

Обнаружено пять базовых адресов GPIO: GPIOA..GPIOE на устройстве STM32F303
(подтверждено по SVD, см. раздел 1 выше).

Точная распиновка МК и имена прикладных сигналов: UNKNOWN (см. отдельный
файл `GPIO_MAP.md` — там теперь есть подтверждённая таблица распиновки по
схеме и netlist).

### 6.1 Конфигурация GPIO внутри main() — CONFIRMED 2026-09-05, дополнено 2026-09-05

**Обновление:** первый проход по этому разделу (см. ниже) содержал одну
ошибку и один пробел, оба исправлены — полная актуальная версия таблицы
теперь только в `GPIO_MAP.md` (не дублируется здесь, чтобы не разойтись):
- ошибка: пины 13/14/15 порта C (`PC13`/`PC14`/`PC15`) были на первый
  взгляд перепутаны с портом A из-за соседства в коде — разрешено по
  аргументу порта (`uVar18`=`DAT_08016684`=GPIOC против прямого
  `&GPIOA_MODER`); настоящий `PA15` конфигурируется отдельным вызовом как
  `Input, NoPull`;
- пробел: автоматический разбор изначально не учитывал вкраплённые
  принудительные записи состояния (`FUN_08005f06`) между вызовами
  `HAL_GPIO_Init`-подобной функции — из-за этого были упущены важные
  детали (PA3 принудительно устанавливается в HIGH перед конфигурацией —
  включение TPS6300 при старте; PB13 принудительно в LOW — CS BMI160
  активен сразу при старте; аналогично для PC13 и, предположительно, PA9).

Все адреса-указатели (`DAT_08016680`=RCC, `DAT_08016684`=GPIOC,
`DAT_08016688`=GPIOB, `DAT_08016690`=`GPIO_MODE_IT_RISING`) разрешены.

**CONFIRMED:** PB3/PB4/PB5 → **SPI1** (дисплей), PB15 → Display_CS.
`FUN_08007298` (MSP-init SPI1, условие `*param_1 == &SPI1_CR1`) вызывает
`FUN_08005ccc(GPIOB_base, &init_struct)` с Pin-маской `0x38` (биты 3/4/5),
Mode = AF_PP, Alternate = `5` (AF5 = SPI1 на STM32F303). Эта конфигурация
происходит отдельно от общего GPIO-блока main() (`FUN_08016274`), поэтому
не была замечена при первом проходе по разделу 6.1. Netlist обновлён по
этому результату. Протокол PGA113 (PB0-PB2/PB10) остаётся отдельным
bit-bang-случаем, вывод по нему не меняется.

Также найдена полная цепочка **PA3 → TPS6300 EN**: включается в
`FUN_08016274` (main) при старте, выключается в `FUN_0800ee00` вместе с
`PB9`/`VD_ON` — эта функция похожа на процедуру полного выключения
питания (см. новый раздел 6.2 ниже).

`FUN_08005ccc @ 0x08005ccc` (для 2.0.12) — подтверждена прямым чтением
тела как классическая реализация `HAL_GPIO_Init(GPIOx, &GPIO_InitTypeDef)`:
цикл по битовой маске `Pin`, запись полей `Mode`/`Pull`/`Speed`/`Alternate`
в MODER/OTYPER/OSPEEDR/PUPDR/AFR соответствующего порта. Первый аргумент —
указатель на регистр MODER конкретного порта (подписан Ghidra как
`&GPIOA_MODER` благодаря ранее применённым SVD-меткам).

Внутри `FUN_08016274` (main) найдены и разобраны все 22 вызова этой
функции — полная таблица (порт/пин/mode/pull) вынесена в `GPIO_MAP.md`,
раздел "Конфигурация GPIO по коду инициализации". Почти все 22 записи
подтверждают сигналы, уже установленные по схеме/netlist (VD_ON, JOY_OK,
JOY_B, JOY_C, Display_CS, BLE_ON, AX_CS, ZP, ACLK, AMOSI, I_CS, U_CS,
RANGE_SELECT, Vbat/VBUS) — то есть код и схема сходятся.

Три открытых момента из этого разбора (подробности и обсуждение — в
`GPIO_MAP.md`):
- **PA9 (сигнал TX) сконфигурирован как обычный Output, не Alternate
  Function** — под вопросом, действительно ли это аппаратный USART1_TX,
  или переключение в AF происходит в другом месте кода;
- **PB1 (DIO протокола PGA113) сконфигурирован здесь только как Output**,
  хотя протокол PGA113 требует двунаправленной передачи — ожидается
  динамическое переключение режима пина в другом месте кода, не найдено;
- **PC13/PC15** сконфигурированы (Output и Input+PullDown), но их
  сигнальное назначение по схеме не установлено.

Также подтверждено MussonOld: **PA3** (Output PP) — это вывод EN
регулятора TPS6300 на текущей плате (будет освобождён в будущей ревизии);
**PC14** (Input + IT_RISING + PullUp, единственная запись в этом блоке с
разрешённым EXTI-прерыванием) — это **INT2 от акселерометра BMI160**.

**PB6/PB7 сконфигурированы кодом** (PB6 — Output PP, PB7 — Output
**Open-Drain**), несмотря на то что физически не разведены на текущей
ревизии платы (netlist подтверждает NC). То, что именно PB7 настроен как
open-drain (в отличие от push-pull почти везде в этом блоке) — сильный
косвенный довод в пользу того, что прошивка писалась с расчётом на
использование I2C на этих пинах (I2C требует open-drain), просто данная
ревизия платы эту возможность не реализует.

### 6.2 `FUN_0800ee00` — вероятная процедура выключения питания (2026-09-05)

Найдена при трассировке записи `PA3 → LOW`. Тело функции (242 инструкции,
трогает DMA1, DMA2, GPIOA, GPIOB):

1. цикл с обратным отсчётом, вызывающий `FUN_0800fb24(uVar2,0)` — похоже
   на гашение дисплея (используется DMA);
2. `FUN_08009404()` — вызов-разделитель, не разобран;
3. `FUN_08005f06(GPIOB, 0x200, 0)` — **`PB9`/`VD_ON` → LOW**, отпускание
   самоблокировки питания, установленной аппаратно по схеме;
4. `FUN_08005f06(&GPIOA_MODER, 8, 0)` — **`PA3`/TPS6300 `EN` → LOW**,
   явное выключение регулятора;
5. цикл опроса `FUN_08005efc(&GPIOA_MODER, 2)` (`PA2`/`JOY_C`) до
   ненулевого значения — ожидание отпускания кнопки;
6. задержка 100мс, возврат.

**Вызывающие функции (6 мест):** `0x08000270`, `0x0800afd4`, **`0x0800ff78`
(обработчик TIM7)**, `0x08016274` (main), `0x08017dc8`, `0x080187f0`.

**PROBABLE:** `FUN_0800ee00` — обработчик команды `POWER_OFF` (см. список
командных строк в разделе 9) и/или срабатывает по таймауту от TIM7.
Остальные 4 вызывающих (`0x08000270`, `0x0800afd4`, `0x08017dc8`,
`0x080187f0`) не разобраны — могут быть дополнительными путями достижения
выключения (например, обработка кнопки, разряд батареи).

### 6.3 Обработчик TIM7 (`FUN_0800ff78`) разобран целиком — CONFIRMED структура, 2026-09-06

Гипотеза из раздела 6.2 ("TIM6 и TIM7 — две стадии автовыключения")
**уточнена**: `FUN_0800ff78` — это не отдельный "таймер второй стадии", а
общий периодический (частота тика не извлечена) обработчик, совмещающий
**таймаут автовыключения** и **периодическое обнаружение удержания
кнопок** (не дебаунс контактов — см. уточнение ниже и в разделе 6.5).
Разбор по блокам:

**Блок A — таймаут автовыключения (CONFIRMED прямым дизассемблированием,
Capstone/Thumb-2, адреса инструкций ниже):**

Это **два независимых счётчика на общем флаге-гейте** `*DAT_0801027c`
(адрес `0x20005617`, семантика флага не установлена — вероятно "не
заряжается"/"питание от батареи"), а не один линейный счётчик:

- `pcVar1[0x2e]` (порог `600`, `cmp.w r0,#0x258` @ `0x0800ffbc`) —
  инкрементируется, пока `< 600` и флаг `== 0`; по достижении порога
  **или** когда флаг `!= 0`, сбрасывается в `0`, вызывается
  `FUN_0800afac()` (`bl 0x800afac` @ `0x0800ffd0`), и если результат
  ненулевой — `FUN_080178dc()`. Эта ветка **не связана** с
  `FUN_0800ee00`.
- Сразу после неё (независимо от того, как завершилась ветка `0x2e`) —
  саб-счётчик `pcVar1[0x28]` (порог `10`, `cmp r0,#0xa` @ `0x0800ffe0`).
  Только когда `0x28` переполняется (раз в 10 входов в этот участок) **и**
  флаг `== 0` (`ldrb r0,[r7]; cbnz` @ `0x0800ffec`), инкрементируется
  **другой** счётчик — `pcVar1[0x2c]` (`adds r0,r0,#1` @ `0x0800fff2`).
  Именно `0x2c` сравнивается с порогом `pcVar1[0x2a]`:
  - `0x2c == 0x2a-30` (`sub.w r2,r0,#0x1e` @ `0x0800fffa`) и
    `0x2c == 0x2a-10` (`sub.w r2,r0,#0xa` @ `0x08010010`) — короткий
    сигнал `bl 0x8002dd6` (100 мс) — предупреждающий писк;
  - `0x2c >= 0x2a` (порог ненулевой, `cbz r0,...` @ `0x0801002a`) —
    сброс `0x2c`, писк и **`bl 0x800ee00` с `movs r0,#2`** @ `0x08010034`
    — автовыключение.

`thunk_EXT_FUN_10000bc0` резолвится в `0x08002dd6` (CONFIRMED, разобран
в разделе 4.3).

**Важное следствие для раздела 6.5:** тот факт, что `0x2c` инкрементируется
только раз на каждые 10 входов в этот блок (через саб-счётчик `0x28`,
порог которого зашит константой `10` прямо в коде), полностью
согласуется с находкой 6.5 "TIM7 application tick = 1 Гц" — **если** сырой
аппаратный IRQ TIM7 срабатывает на самом деле в 10 раз чаще (~10 Гц,
период ~100мс), а `0x28` — программный делитель 10:1, дающий на выходе
именно тот 1-секундный "прикладной тик", с которым напрямую сравниваются
пороги меню (120/300/600 = 2/5/10 мин). Это не доказывает конкретные
`PSC`/`ARR` (раздел 6.4 оставляет их `UNKNOWN`), но даёт конкретную
проверяемую гипотезу для будущего чтения регистров в отладчике: искать
конфигурацию TIM7 на **~100 мс**, а не на 1с напрямую.

**Блок B — периодическое обнаружение удержания кнопок/джойстика
(CONFIRMED по структуре; НЕ дебаунс контактов в привычном смысле — см.
раздел 6.5, при периоде тика 1с порог "10" означает 10 секунд
удержания):**
Тем же тиком опрашиваются три отдельных цифровых входа через
подтверждённый геттер `FUN_08005efc(base, mask)` (читает `IDR`, смещение
`0x10` от базы порта — см. определение функции):
- `GPIOA` маска `0x2` (бит 1, **`PA1` = `JOY_OK`, подтверждено
  `GPIO_MAP.md`** — 2026-09-06: разрешено сверкой со схемой/netlist,
  раздел "Джойстик" в `GPIO_MAP.md`; JOY_OK — средняя кнопка джойстика,
  используется аппаратно для включения питания через MOSFET Q2 —
  логика полностью согласуется: удержание той же кнопки уже во включённом
  состоянии программно выключает устройство) — порог удержания 10
  тиков (= 10 секунд), по достижении вызывает `FUN_0800ee00(0)`
  (автовыключение/выключение по кнопке, причина `0`);
- `GPIOA` маска `0x4` (бит 2, **`PA2` = `JOY_C`**, подтверждено ранее) —
  тот же порог удержания (10 тиков = 10 секунд), по достижении выставляет
  внутренние флаги состояния (`pcVar1[0xd]`, `pcVar1[7]` и т.д.), прямого
  вызова `FUN_0800ee00` в этой ветке не найдено; **смысл действия —
  UNKNOWN**;
- `GPIOB` маска `0x1000` (бит 12, **`PB12` = `JOY_B`**, подтверждено
  `GPIO_MAP.md`) — тот же паттерн (10 тиков удержания), свои флаги
  состояния; **смысл действия — UNKNOWN**.

Смысл выставляемых флагов (`pcVar1[7..0xe]` и т.д.) не установлен — они,
вероятно, читаются в другом месте (меню/UI), не прослежено.

**Уточнение 2026-09-06 (важно для интерпретации периода 1с):** опрос
кнопок в блоке B при периоде тика TIM7 в 1 секунду **не может** быть
источником быстрой реакции UI на нажатия (навигация по меню с задержкой
до секунды была бы непригодна для использования). Проверено — это и не
так: кнопки читаются НАПРЯМУЮ и НАМНОГО быстрее в других местах кода,
независимо от TIM7. Например, в `main()` найден классический блокирующий
опрос:
```c
do {
    iVar15 = FUN_08005efc(&GPIOA_MODER, 2);   // опрос JOY_OK (PA1) на
} while (iVar15 == 0);                          // скорости процессора
```
и множество других прямых вызовов `FUN_08005efc(&GPIOA_MODER, 2)` по
всему `main()` (навигация по меню), вперемешку с вызовами
`FUN_080154c8(ms)` — отдельной функцией программной задержки на
**миллисекундах** (калибруется по частоте шины, разобрана по байтам:
классический busy-wait цикл), например `FUN_080154c8(10)`,
`FUN_080154c8(0x32)` (~50 мс).

**Вывод: блок B в TIM7 — это НЕ основной механизм чтения кнопок для UI.**
Основная навигация по меню читает кнопки напрямую и быстро (мс-масштаб)
в основном цикле `main()`. Блок B в TIM7, с его 1-секундной
гранулярностью, служит отдельной, более медленной задаче — обнаружению
**долгого удержания** (подтверждённый пример — удержание `JOY_OK` 10
секунд для выключения питания). Погрешность в ±1 секунду совершенно
приемлема для обнаружения 10-секундного удержания, но неприемлема для
живой навигации — поэтому для навигации используется отдельный,
независимый путь. Не установлено, что именно делают флаги `PA2`/`PB12`
в блоке B (вероятно, аналогичные "удержание N секунд" действия для этих
кнопок, а не основной ввод) — это отдельная нерешённая задача.

**Блок C — аналоговое сравнение (PROBABLE, не доведено до конца):**
`iVar13 = VectorUnsignedToFloat(FUN_08005148(DAT_08010270), ...)` — ADC-
подобное чтение с преобразованием в float, сравнивается с порогом
(`(iVar13 + DAT_08010294) < DAT_08010298`) тем же паттерном (порог
удержания по тикам — см. раздел 6.5, это не дебаунс контактов).
`DAT_08010294`/`DAT_08010298` в памяти читаются как `0xbd37ffff` /
`0x0247bfff` — похожи на float-битовые паттерны, а не целые константы;
точный физический смысл канала (батарея? термопара? заданная точка?) —
UNKNOWN, не гадать до отдельного разбора `FUN_08005148`.

**Частично разобрано 2026-09-06:** `FUN_08005148(param_1)` — подтверждено
как простой геттер `return *(*param_1 + 0x40)`. Смещение `0x40` от базы
периферии точно совпадает с `ADC_DR` (регистр данных regular-канала) в
карте регистров ADC у STM32F303 — то есть это аналог `HAL_ADC_GetValue()`.
Аргумент — `DAT_08010270`, литерал-указатель на RAM-адрес `0x20006968`
(структура-хэндл ADC), но **какой именно из ADC1..ADC4 и какой физический
канал стоит за этим хэндлом — не установлено**: в отличие от GPIO/RCC,
базовые адреса ADC1..ADC4 в этой прошивке пока не размечены по SVD (кроме
единственной метки `ADC1_CFGR`, найденной в другом месте кода), а сам
хэндл инициализируется во время выполнения (не в статическом `.data`) —
статический поиск `0x20006968` по всему образу даёт ноль совпадений.
Не гадать; нужна отдельная задача — разметить ADC1..ADC4 по SVD (та же
техника, что уже применена к GPIO/RCC), затем повторно проследить
инициализацию хэндла по адресу `0x20006968`.

**Итог по разделу 6.3:** гипотеза "TIM6/TIM7 — две стадии
автовыключения" не опровергнута, но неточна в деталях — правильнее:
**TIM7 совмещает функцию общего сторожевого таймера бездействия
(таймаут -> `FUN_0800ee00(2)`) и периодического обнаружения долгого
удержания кнопок/джойстика (в т.ч. ручное выключение через удержание
`JOY_OK`/`PA1` -> `FUN_0800ee00(0)`)**. Не путать с обычным дребезгом
контактов — при периоде тика 1с это заведомо слишком грубая
гранулярность для debounce, см. раздел 6.5.
`TIM7 ISR application tick` = **CONFIRMED 1 Гц** (раздел 6.5) —
установлено не через PSC/ARR, а косвенно.
`TIM7 PSC/ARR` = **UNKNOWN** (раздел 6.4) — не влияет на функциональное
понимание. Не выяснено: назначение блока C (физический ADC-канал,
раздел 6.3), назначение долгого удержания `PA2`/`PB12` (задача 17).

### 6.4 TIM7 — RCC/NVIC подтверждены; период установлен НЕ через PSC/ARR, а косвенно — CONFIRMED, 2026-09-06

**Метод:** `FUN_0800ff78` (сам ISR, раздел 6.3) дизассемблирован по байтам
(Capstone) целиком, в реальных границах `0x0800FF78..0x080102A0`
(`0x328` байт, 342 инструкции, подтверждено по границе следующей функции
`FUN_080102a0`) — **ни одного обращения к регистрам TIM7 в теле ISR нет**
(ни через литерал-пул, ни через `movw`/`movt`). Значит настройка
периферии TIM7 (RCC/PSC/ARR/DIER/CR1) целиком находится в
функции(-ях) инициализации, не в самом обработчике.

**CONFIRMED — RCC и NVIC:** найдена общая функция-диспетчер
`FUN_08007944(handle)` (вызывается из `FUN_080078e0`/`FUN_080078e8` —
это MSP-Init-подобные обёртки, общие для многих таймеров), которая по
`handle->Instance` определяет конкретный таймер через цепочку
`sub.w/subs.w` с модифицированными непосредственными операндами
(`r1 - 0x40000000 - 0x1400 == 0` для TIM7) и включает клок/IRQ:
```
TIM1  (0x40012C00) -> RCC_APB2ENR |= 0x800  (бит 11, TIM1EN)
TIM2  (0x40000000) -> RCC_APB1ENR |= 0x1    (бит 0, TIM2EN)
TIM3  (0x40000400) -> RCC_APB1ENR |= 0x2    (бит 1, TIM3EN)
TIM4  (0x40000800) -> RCC_APB1ENR |= 0x4    (бит 2, TIM4EN)
TIM7  (0x40001400) -> RCC_APB1ENR |= 0x20   (бит 5, TIM7EN);
                       NVIC: SetPriority(IRQ 0x37=55, 0xF); EnableIRQ(55)
TIM16 (0x40014400) -> RCC_APB2ENR |= 0x20000 (бит 17, TIM16EN)
```
(`RCC_APB1ENR`/`RCC_APB2ENR` — смещения `0x1C`/`0x18` от базы RCC,
стандартные для STM32F3.) **Приоритет NVIC для TIM7 = `0xF` (низший из
16 уровней)** — заметно ниже, чем у TIM6 (приоритет `0`, высший, см.
ниже) — согласуется с ролью TIM7 как фоновой "домашней" задачи
(сторожевой таймаут + опрос кнопок), а не тайминг-критичной операции.

**Для сравнения — TIM6 РАЗОБРАН ЦЕЛИКОМ (`FUN_08003428`,
`0x08003428`), как образец полной, самодостаточной функции инициализации
периодического таймера этого проекта** (TIM6 в диспетчер `FUN_08007944`
не заходит вовсе — делает всё сама):
```c
RCC_APB1ENR |= 0x10;              // TIM6EN (бит 4)
htim6.Instance = TIM6 (0x40001000);
htim6.Init.Prescaler = 99;         // PSC = 99  (т.е. счёт /100)
htim6.Init.CounterMode = 0;         // up
// ARR вычисляется динамически из частоты шины:
//   ARR = ((uint64)targetConst * (busClockHz/100) >> 32 << 10 >> 16) - 1
//   (арифметика на 64-битном промежуточном значении — защита от
//   переполнения при разных вариантах системной частоты)
htim6.Init.ClockDivision = 0;
FUN_080078e0(&htim6, ...);          // MSP init (уже enabled выше вручную)
FUN_0800784c(&htim6, ...);           // TIM_Base_SetConfig-подобный шаг
FUN_08007a00(&htim6);
FUN_08007a70(&htim6);
NVIC SetPriority(IRQ 0x36=54, 0);     // приоритет 0 — ВЫСШИЙ
NVIC EnableIRQ(54);
TIM6->DIER |= 1;                       // UIE, напрямую по смещению 0xC
```
Это даёт точный шаблон формул/полей, но **числовые PSC/ARR у TIM6 и
TIM7 не обязаны совпадать** — это разные функции с разными константами.

**НЕ НАЙДЕНО — PSC/ARR/DIER именно для TIM7:** в отличие от TIM1, TIM2,
TIM3, TIM6, TIM16 (для каждого из которых найдена явная точка
инициализации с `Instance=`, `Prescaler=`, `Period=` в `main()` или в
отдельной функции — см. таблицу адресов литералов в разделе), **для
TIM7 такая точка не обнаружена** несмотря на:
- полный просмотр всех вызовов `FUN_080078e0`/`FUN_080078e8` по всему
  образу (только 5 мест с содержательными аргументами — все TIM1/2/3/6,
  ни одного TIM7);
- посимвольный поиск литерала `0x40001400` (4 байта, любое выравнивание)
  по всему `.hex` — 0 совпадений;
- полный проход Capstone по всему образу (`skipdata=True`, 79867
  инструкций) на предмет пар `movw`/`movt`, строящих `0x40001400`, — 0
  совпадений;
- `FUN_080078e8` вообще не имеет ни одного прямого вызова (`bl`) по
  всему образу — вызывается, видимо, косвенно (через указатель на
  функцию в структуре-хэндле), что делает его недостижимым для
  текстового grep-поиска по декомпилированному листингу.

**Вывод по PSC/ARR:** TIM7, в отличие от TIM6, не имеет отдельной
"самодостаточной" функции инициализации и, похоже, конфигурируется через
тот же generic-механизм (`HAL_TIM_Base_Init`-подобный, с косвенным
вызовом MSP callback через указатель в хэндле), что и TIM1/2/3/16 — но
конкретное место, где создаётся структура-хэндл TIM7 с числовыми
`Prescaler`/`Period`, статическим поиском не найдено (граф косвенных
вызовов не восстановлен). Читать напрямую регистры TIM7 в отладчике
по-прежнему остаётся вариантом для 100%-й полноты, но **для практических
целей это больше не требуется** — см. ниже: реальный период тика найден
другим путём, без PSC/ARR.

### 6.5 Реальный период тика TIM7 = 1 секунда — CONFIRMED, 2026-09-06 (без PSC/ARR, без отладчика)

**Метод:** вместо поиска PSC/ARR — проследить, откуда берутся пороговые
константы блока A (раздел 6.3) и с чем они связаны в остальном коде.

Найдена функция-сеттер порога автовыключения `FUN_0800fcf4(param_1,
param_2)`:
```c
void FUN_0800fcf4(short param_1, int param_2)
{
  iVar1 = DAT_0800fd1c;              // = 0x200056A0 — ТОТ ЖЕ адрес,
                                       //   что и pcVar1 в ISR TIM7 (раздел 6.3)
  *(short*)(iVar1 + 0x2c) = 0;         // сброс счётчика (тот же offset 0x2c из блока A)
  *(short*)(iVar1 + 0x2a) = param_1;    // новый порог (тот же offset 0x2a из блока A)
  if (param_2 != 0) { /* сохранение в постоянную память */ }
}
```
`DAT_0800fd1c` прочитан напрямую из `.hex` = `0x200056A0` — **побитово
совпадает** с адресом `pcVar1`, используемым в `FUN_0800ff78` (см. раздел
6.3). Это независимое подтверждение, что оба места работают с одной и
той же структурой состояния.

Вызовы этого сеттера — из обработчика пунктов меню (`case 0x1c..0x1f` в
`FUN_08016...`-диспетчере экранов):
```
FUN_0800fcf4(0x78, 1);   // 0x78 = 120
FUN_0800fcf4(300, 1);
FUN_0800fcf4(600, 1);
FUN_0800fcf4(0,   1);    // выключить автовыключение
```

Строковые константы прошивки (извлечены из образа флеша, `strings` по
конвертированному в сырой бинарник `.hex`) содержат подписи ИМЕННО этих
пунктов меню:
```
"Timer 2 min"
"Timer 5 min"
"Timer 10 min"
```
Сопоставление однозначное и проверяется трижды независимо:
- 120 тиков ↔ "2 min" → 120 / (2*60) = **1.00 тик/сек**
- 300 тиков ↔ "5 min" → 300 / (5*60) = **1.00 тик/сек**
- 600 тиков ↔ "10 min" → 600 / (10*60) = **1.00 тик/сек**

**ИТОГ — строгая формулировка (2026-09-06, уточнено по замечанию
рецензента):**
- **`TIM7 ISR application tick = CONFIRMED 1 Hz`** — то есть каждый вход
  в `FUN_0800ff78`, инкрементирующий эти прикладные счётчики, соответствует
  ровно одной реальной секунде. Это установлено полностью независимо от
  PSC/ARR — методом сопоставления прикладных констант с текстовыми
  метками меню, подтверждено трижды на разных значениях без единого
  противоречия.
- **`TIM7 PSC/ARR = UNKNOWN`** — регистровая конфигурация таймера
  (собственно `PSC`/`ARR`/`CR1`), которая физически порождает это IRQ с
  такой частотой, статическим поиском не найдена (раздел 6.4) и остаётся
  не восстановленной. Доказана прикладная частота вызовов ISR, а не
  декомпозиция в регистровые значения — это разные утверждения, и
  подменять один результат другим не следует.
- Неявное допущение: каждое срабатывание IRQ55 приводит ровно к одному
  инкременту счётчика (нет пропущенных вызовов ISR, нет обхода
  инкремента веткой кода) — это не проверено отдельно, но следует из
  структуры блока A/B (раздел 6.3), где инкремент — первая операция при
  каждом входе.

**Пересчёт остальных констант блока A/B/C раздела 6.3 в реальное время:**
- Удержание кнопок/джойстика "10 тиков" (блок B) = **10 секунд** —
  многовато для обычного дребезга контактов; значит это не debounce в
  привычном смысле, а **periodic button hold / long-press detection**
  (характерно для "удержите кнопку N секунд, чтобы выключить прибор" —
  стандартный паттерн защиты от случайного выключения в портативных
  приборах). Подтверждено для `PA1`/`JOY_OK` (выключение питания);
  для `PA2`/`JOY_C` и `PB12`/`JOY_B` тот же временной порог применяется,
  но результирующее действие — **UNKNOWN** (см. задачу 17).
- Предупреждающие сигналы блока A на `порог-30` и `порог-10` = за
  **30 секунд** и **10 секунд** до автовыключения соответственно.
- Счётчик `pcVar1[0x26]` с порогом `100` (раздел 6.3) = **100 секунд**
  (~1 мин 40 сек).
- Счётчик `pcVar1[0x2e]` с порогом `600` = **600 секунд = 10 минут** —
  то же самое число, что и один из вариантов таймера автовыключения;
  возможно, тот же физический смысл, возможно совпадение — не
  установлено, что именно измеряет этот отдельный счётчик (см. раздел
  6.3, связь с `FUN_0800afac`/`FUN_080178dc` не разобрана).
- Аналоговое сравнение блока C — свой порог удержания тоже "10 тиков" = 10
  секунд до срабатывания по превышению порога (согласуется с медленным
  процессом вроде разряда батареи, а не быстрым сигналом).

### 6.6 Блок C раздела 6.3 — хэндл ADC4, канал и физический вывод — CONFIRMED, 2026-09-07

Пункт 14 из раздела 13: разметка `ADC1..ADC4` и трассировка хэндла
`0x20006968`.

**Базовые адреса `ADC1..ADC4` по официальному `STM32F303.svd`** (для
справки, использованы для сопоставления ниже):
`ADC1 = 0x50000000`, `ADC2 = 0x50000100`,
`ADC1_2_Common = 0x50000300`, `ADC3 = 0x50000400`,
`ADC4 = 0x50000500`, `ADC3_4_Common = 0x50000700`.

**Instance — CONFIRMED.** Значение `0x20006968` (указатель-хэндл,
используемый в блоке C через `FUN_08005148(DAT_08010270)`, раздел 6.3)
как литерал встречается в образе прошивки ровно в 3 местах: `0x08010270`
(сам блок C в TIM7), `0x08016b14` и `0x08017f10`. По адресу `0x08016b14`
найдена прямая запись `*DAT_08016b14 = DAT_08016b10;` где
`DAT_08016b10 = 0x50000500` — **ADC4**. Поле `Instance` HAL-подобного
хэндла по адресу `0x20006968` указывает на ADC4.

**Канал и физический вывод — CONFIRMED (пересмотрено 2026-09-07).**
Первый проход (см. историю ниже) ошибочно принял `FUN_08004c1c` за
конфигуратор канала — прямая проверка по реальному дизассемблеру этого
не подтвердила. Разобрано заново, с опорой на точные `str`-инструкции
(не на порядок печати decompiled-переменных Ghidra, который вводил в
заблуждение) и на официальные заголовки `STM32CubeF3`
(`stm32f3xx_hal_adc.h`/`_ex.h`, скачаны напрямую с
`github.com/STMicroelectronics/stm32f3xx_hal_driver`):

- `FUN_08004c1c` — **не конфигуратор канала**. По месту записи
  (`iVar1+0x24`/`iVar1+0x28`, где `iVar1=Instance`) и паттерну
  `1 << channel` это соответствует настройке **аналогового watchdog**
  (AWD2CR/AWD3CR в терминах RM0316), не `HAL_ADC_ConfigChannel`. Поля
  `Rank=0xc00000`/`SingleDiff=0xfff` не совпадают ни с одним реальным
  значением HAL для этих имён — подтверждает, что это не
  `ADC_ChannelConfTypeDef`.
- **Реальный конфигуратор канала — `FUN_08004d94`.** Пишет
  `*param_2 << (rank*6)` в регистр по смещению `Instance+0x30` — точная
  аппаратная формула регистра `SQR1` STM32F3 (5-битное поле канала на
  каждые 6 бит по рангу, см. RM0316). Аргументы для того же хэндла ADC4
  (вызов `FUN_08004d94(DAT_08016b14, &uStack_60)` сразу после
  `FUN_08004c1c`, ещё не задокументированный ранее вызов):
  **Channel = 4, Rank = 1** (плюс SamplingTime=7, остальные поля 0) —
  все значения — маленькие целые, естественные для сырого номера канала
  и валидные для реальных HAL-констант (`ADC_REGULAR_RANK_1 = 1`
  подтверждено по заголовку дословно).
- **Физический вывод — CONFIRMED по официальному датащиту** (`DS9118`,
  Table 13, распиновка STM32F303xB/xC): **`ADC4_IN4` = вывод `PB14`.**

**Итоговая цепочка (полностью замкнута, два независимых источника
сходятся на одном выводе):**
```
0x20006968 (RAM, ADC-хэндл)
  -> Instance = 0x50000500 = ADC4                [CONFIRMED, литерал в коде]
  -> FUN_08004d94: Channel=4, Rank=1               [CONFIRMED, реальный SQR1-writer]
  -> ADC4_IN4 = физический вывод PB14              [CONFIRMED, датащит Table 13]
  -> PB14 = JOY_A/JOY_D/BAT                         [CONFIRMED ранее, из схемы — GPIO_MAP.md]
```
Это независимое перекрёстное подтверждение: пин, вычисленный чисто из
прошивки (Instance→SQR1-запись→датащит), совпал с пином, установленным
ранее чисто из схемы (GPIO_MAP.md, до какого-либо анализа этого блока
кода). Two-source confirmation этого уровня — сильное доказательство.

<details>
<summary>История (что было найдено раньше и почему пересмотрено)</summary>

Первый проход принял `FUN_08004c1c` за `HAL_ADC_ConfigChannel` по
структурному сходству (ветки `if (*param_2==1)`, сравнение первого поля
с номером канала) и получил `Channel=1`, что по датащиту соответствует
`ADC4_IN1` = `PE14` — вывод, физически невозможный на подтверждённом
корпусе LQFP48 (порт E отсутствует). Это противоречие и стало сигналом
пересмотреть структуру аргумента с нуля, а не подгонять её под
ожидаемый результат — итог см. выше.
</details>

**Побочная находка:** второе место, где встречается тот же литерал
(`0x08017f10`, чтение `FUN_08005148(DAT_08017f10)`), находится в
`FUN_08017dc8` — это один из **ранее неразобранных вызывающих
`FUN_0800ee00`** (раздел 6.2, "остальные 4 вызывающих... не разобраны").
Функция разобрана целиком (2026-09-06), **CONFIRMED по структуре**:

1. читает то же измерение ADC4 (`FUN_08005148(DAT_08017f10)`,
   `DAT_08017f10 = 0x20006968` — тот же хэндл ADC4), конвертирует в float
   (`VectorUnsignedToFloat`);
2. ведёт скользящий минимум/максимум в структуре по адресу
   `0x200056d0` (отдельная от хэндла ADC, собственная структура этой
   функции);
3. если скользящее значение отрицательное — выполняет калибровочную
   формулу через цепочку вызовов **fixed-point арифметики**
   (`FUN_08002932`/`FUN_08002698`/`FUN_0800277c`/`FUN_080029b8` — судя
   по сигнатурам, это библиотечные int↔fixed-point конвертер/умножение/
   деление/обратный конвертер; сама формула с 7+ константами-литералами
   не восстанавливалась до физических единиц — не гадать);
4. основная ветка: считает **вторую** калибровочную формулу (та же
   библиотека fixed-point), сравнивает результат `iVar5` с порогом
   `DAT_08017f68`;
5. **если `iVar5 < порог` — инкрементирует счётчик `*pbVar1` (байт), и
   при превышении счётчиком `100` — вызывает `FUN_0800ee00(1)`** —
   то есть причина автовыключения `1` = устойчиво низкое (сохраняющееся
   >100 вызовов подряд) значение, вычисленное из ADC4. Если условие не
   выполняется — счётчик сбрасывается в `0`.

**PROBABLE:** по совокупности признаков (калибровочная формула, ADC-канал
= PB14, персистентность 100 подряд идущих "плохих" измерений перед
отключением, типичный для protection-логики паттерн) — это защита по
низкому напряжению батареи, читаемому через тот же аналоговый вывод
PB14, что делит линию с кнопками JOY_A/JOY_D. Не CONFIRMED: физическая
единица измерения и точные пороги в реальных единицах (мВ/°C/и т.п.) —
калибровочные константы не декодированы намеренно, чтобы не выдавать
домысел за факт.

**Итог по причинам `FUN_0800ee00`:** причина `0` = удержание `PA1`/`JOY_OK`
(раздел 6.3), причина `1` = устойчиво низкое значение ADC4/PB14 в
`FUN_08017dc8` (вероятно батарея, см. выше), причина `2` = таймаут
бездействия в TIM7 (раздел 6.3). Из 6 вызывающих `FUN_0800ee00`
(раздел 6.2) остаются неразобранными: `0x08000270`, `0x0800afd4`,
`0x080187f0`.



**Побочная находка:** второе место, где встречается тот же литерал
(`0x08017f10`, чтение `FUN_08005148(DAT_08017f10)`), находится в
`FUN_08017dc8` — это один из **ранее неразобранных вызывающих
`FUN_0800ee00`** (раздел 6.2, "остальные 4 вызывающих... не разобраны").
Функция разобрана целиком (2026-09-06), **CONFIRMED по структуре**:

1. читает то же измерение ADC4 (`FUN_08005148(DAT_08017f10)`,
   `DAT_08017f10 = 0x20006968` — тот же хэндл ADC4), конвертирует в float
   (`VectorUnsignedToFloat`);
2. ведёт скользящий минимум/максимум в структуре по адресу
   `0x200056d0` (отдельная от хэндла ADC, собственная структура этой
   функции);
3. если скользящее значение отрицательное — выполняет калибровочную
   формулу через цепочку вызовов **fixed-point арифметики**
   (`FUN_08002932`/`FUN_08002698`/`FUN_0800277c`/`FUN_080029b8` — судя
   по сигнатурам, это библиотечные int↔fixed-point конвертер/умножение/
   деление/обратный конвертер; сама формула с 7+ константами-литералами
   не восстанавливалась до физических единиц — не гадать);
4. основная ветка: считает **вторую** калибровочную формулу (та же
   библиотека fixed-point), сравнивает результат `iVar5` с порогом
   `DAT_08017f68`;
5. **если `iVar5 < порог` — инкрементирует счётчик `*pbVar1` (байт), и
   при превышении счётчиком `100` — вызывает `FUN_0800ee00(1)`** —
   то есть причина автовыключения `1` = устойчиво низкое (сохраняющееся
   >100 вызовов подряд) значение, вычисленное из ADC4. Если условие не
   выполняется — счётчик сбрасывается в `0`.

**PROBABLE:** по совокупности признаков (калибровочная формула, ADC-канал,
персистентность 100 подряд идущих "плохих" измерений перед отключением,
типичный для protection-логики паттерн) — это защита по низкому
напряжению батареи. Не CONFIRMED: физическая единица измерения и точные
пороги в реальных единицах (мВ/°C/и т.п.) — калибровочные константы не
декодированы намеренно, чтобы не выдавать домысел за факт.

**Итог по причинам `FUN_0800ee00`:** причина `0` = удержание `PA1`/`JOY_OK`
(раздел 6.3), причина `1` = устойчиво низкое значение ADC4 в
`FUN_08017dc8` (вероятно батарея, см. выше), причина `2` = таймаут
бездействия в TIM7 (раздел 6.3). Из 6 вызывающих `FUN_0800ee00`
(раздел 6.2) остаются неразобранными: `0x08000270`, `0x0800afd4`,
`0x080187f0`.



TIM1: `0x40012C00` — подтверждено.

Регистровые структуры `TIM15`/`TIM16`/`TIM17` (`0x40014000` /
`0x40014400` / `0x40014800`) подтверждены по SVD, но **уверенность по
таймерам разная, не единая для группы**:
- **TIM16: CONFIRMED** — прямая ссылка в прошивке (раздел 6.4).
- **TIM15: UNKNOWN / слабый кандидат** — ноль прямых обращений к его
  регистрам найдено; связь только через код `FUN_080101b4`/`FUN_08008384`,
  который "согласуется с группой", но не адресует TIM15 напрямую.
- **TIM17: UNKNOWN / слабый кандидат** — то же самое, ноль прямых
  обращений.

`FUN_080101b4()` и `FUN_08008384()` содержат код, согласующийся с группой
TIM15/16/17, но это не равно прямому xref на TIM15/TIM17 конкретно — см.
также раздел 10.

Точное сопоставление таймер↔IRQ и значения тактирования/PSC/ARR всё ещё
нужно извлечь.

### 6.7 `thunk_EXT_FUN_100001b8` (ядро корреляции в CCM RAM) — граница
функции CONFIRMED (исправлена и расширена в этой же сессии), calling
convention CONFIRMED, семантика PROBABLE — 2026-09-08

Продолжение раздела 13, пункт 21 ("ПРИОРИТЕТ"). Работа велась без Ghidra
(GUI недоступен в этой сессии) — прямым дизассемблированием Capstone
(Thumb-2) по байтам `.hex`, с ручной проверкой стек-фрейма и перекрёстной
сверкой с реальным кодом вызывающей стороны (не с пересказом из раздела
4.4 — там нашлась неточность, см. ниже).

**Метод извлечения** — тот же, что в разделе 4.3: тело функции физически
лежит во флеше по адресу `flash = 0x0802D7DC + (ram - 0x10000000)`,
копируется в CCM RAM простым словным `ldm/stm`-циклом при старте
(`FUN_08013690`, раздел 4.3) — то есть чтение флеш-образа байт-в-байт
эквивалентно чтению рантайм-содержимого CCM RAM, выполнять код не нужно.

**ГРАНИЦА ПЕРЕСМОТРЕНА 2026-09-08 (та же сессия, продолжение) —
предыдущая оценка `0x100001b8..0x100008d9` была НЕПОЛНОЙ, а не ошибочной
в частностях: то, что ниже описывалось как "следующая функция
`0x100008da..`", на самом деле — прямое продолжение ЭТОЙ ЖЕ функции,
достижимое только через внутренний условный переход, а не через
нормальный вход по `bl`/`b.w` снаружи.**

Обнаружение (получено из независимого чата с тем же контекстом, где
закончился лимит; проверено в этой сессии заново прямым
дизассемблированием, а не принято на веру): непосредственно перед первым
"выходом B" (`0x100008d4..0x100008d6`) есть условный переход
`bhs #0x100008da` по адресу `0x100008a0` — то есть код внутри тела
функции 1, ДО её `pop`, может перейти прямо на `0x100008da`, минуя
`pop`. Раз переход происходит изнутри тела, с теми же живыми регистрами
(`r0`,`r1`,`r2`,`r3`,`r4`,`r5`,`r6` не переинициализируются заново),
`0x100008da` не может быть независимой функцией — независимая функция
не может быть целью условного перехода из середины чужого тела в обход
пролога.

**Полная повторная трассировка (Capstone, весь блок CCM RAM,
`0x100001b8..0x10000b27`) даёт следующую картину — CONFIRMED:**

```
0x10000884  ldrb r2,[r6]; cmp r2,#0x64; itt hs; addhs sp,#0x6c
0x1000088c  pophs.w {r4,r5,r6,r7,r8,sb,sl,fp,pc}     ; ВЫХОД 1 (ранний):
                                                       ; побайтовый счётчик [r6] >= 100 — нечего делать
0x10000890  r0 = *0x200049b8 ; r0(2я загрузка) = *0x20005a78 ; r1 = r0+0x190
0x10000892  r3 = *(r0_1)              ; r3 = персистентный счётчик по 0x200049b8
0x1000089c  it lo; movlo r2,#0        ; r2=0 только если r3<100
0x100008a0  bhs #0x100008da           ; ЕСЛИ r3>=100 — сразу на продолжение, минуя цикл ниже
0x100008a4..0x100008d2  цикл: копирует пары 16-бит отсчётов (r5,r4 -> r0,r1
            по индексу r3&0xff), r2 += 2 каждую итерацию, пока r2<100
0x100008d4  add sp,#0x6c
0x100008d6  pop.w {r4,r5,r6,r7,r8,sb,sl,fp,pc}        ; ВЫХОД 2: обычный после первых 100 отсчётов

0x100008da  cmp.w r3,#0x1f4 (500); bhs #0x100009da     ; ПРОДОЛЖЕНИЕ той же функции,
                                                        ; не отдельная функция
0x100008e0..0x100009d6  развёрнутый (unrolled) цикл, часть 1 (14 итераций,
            офсеты чтения 0x00..0x68 с шагом 8 байт): копирует пары 16-бит
            отсчётов с шагом чтения 8 байт по источнику (децимация,
            вероятно "разреженная" фаза набора буфера после первых 100
            плотных отсчётов), пишет по индексу r2&0xff
0x100009d8  b #0x100009dc                              ; безусловный переход через
                                                        ; следующую инструкцию — это
                                                        ; ОБХОД для нормального потока
0x100009da  b #0x10000ae4                              ; ЦЕЛЬ перехода bhs выше (r3>=500):
                                                        ; сразу в блок диспетчера стадий
0x100009dc..0x10000ab4  развёрнутый цикл, часть 2 (11 итераций, офсеты
            0x70..0xc0) — прямое продолжение той же последовательности,
            обновляет побайтовый счётчик [r6]. **Итого по обеим частям:
            ровно 25 итераций (офсеты 0x00..0xc0 с шагом 8, пересчитано
            скриптом по числу вхождений `ldrh r3,[r5,...]` в диапазоне —
            не "на глаз"), не 16, как было названо в независимой
            перепроверке этой же находки в параллельном чате — уточнено
            здесь на основе точного подсчёта.**
0x10000ab8  add sp,#0x6c; b #0x10000ae0
0x10000ae0  pop.w {r4,r5,r6,r7,r8,sb,sl,fp,pc}          ; ВЫХОД 3 (главный, общий)

0x10000ae4  r3 = *0x200049e8                             ; тот же адрес, что и
                                                          ; "переключатель" из раздела ниже!
            cmp r3,#8; bhs #0x10000afe
0x10000aec  копирует ОДНУ пару отсчётов (r5,r4 -> r0,r1 по индексу r2),
            обновляет [r6], b #0x10000ab8 (-> ВЫХОД 3)
0x10000afe..0x10000b22  каскад cmp r3,#0x20/#0x40/#0x30/#0x18/#6 над тем
            же r3 и вторым счётчиком [r6+4] — ступенчатый диспетчер стадий,
            в конце сбрасывает [r6+4]=0 и переходит на 0x10000aec
0x10000b24..0x10000b27  4 байта литерал-пула (значение 0x200049e8 как
            данные — то есть литерал ЛЕЖИТ сразу после кода, это не
            начало новой функции)
0x10000b28  начинается EXTI15_10 (уже CONFIRMED, раздел 4.3) — граница
            подтверждена независимо: сразу после литерала начинается уже
            ранее опознанный, самодостаточный обработчик.
```

**Итог — ГРАНИЦА ФУНКЦИИ CONFIRMED (исправлено):** `thunk_EXT_FUN_100001b8`
занимает `0x100001b8 .. 0x10000b23` (код) + 4 байта литерала
(`0x10000b24..0x10000b27`), то есть **0x996C = 2412 байт** (флеш-эквивалент
`0x0802D994 .. 0x0802E32F`) — больше чем в 1.3 раза длиннее, чем считалось
изначально (было `0x722` = 1826 байт). У функции **три точки выхода**
(`0x1000088c`, `0x100008d6`, `0x10000ae0`), все с одинаковой суммарной
поправкой SP (`0x6c` из `sub sp,#0x60` + доп. слово, сходится с прологом
`push.w {12 регистров}; sub sp,#0x60`), а не два, как считалось раньше.
Немедленно после функции (`0x10000b28`) без зазора начинается уже
подтверждённый ранее `EXTI15_10` — то есть открытый пункт 23 ("разобрать
функцию `0x100008da..`") закрывается отрицательно: **такой отдельной
функции не существует**, это была ошибка определения границы, а не
недоразобранный сосед.

**Побочная, важная находка:** литерал по адресу `0x10000b24` резолвится
ровно в `0x200049e8` — **это тот же самый адрес**, который в первой
половине функции (см. ниже) уже был отмечен как "переключатель варианта
цикла корреляции, сравниваемый с `1`". В блоке диспетчера стадий
(`0x10000ae4`) то же самое слово `*0x200049e8` читается снова и
сравнивается уже не с `1`, а каскадно с `8`/`0x20`/`0x40` — то есть это
**не бинарный флаг 0/1**, как предполагалось в PROBABLE-выводе ниже, а
скорее **многозначный счётчик стадии** (0..64+), проверяемый в разных
точках функции с разными порогами в зависимости от того, какая стадия
уже пройдена. Прежняя формулировка "переключатель 0/1" — пересмотрена,
см. обновлённый PROBABLE-вывод ниже. Куда и когда именно `0x200049e8`
инкрементируется — по-прежнему UNKNOWN, не найдено в этой сессии (нужен
отдельный поиск `str`/`strb` по этому адресу за пределами уже
просмотренного диапазона CCM RAM).

**ИСПРАВЛЕНИЕ раздела 4.4 (важно):** реальная calling convention —
**три** входных регистра (`r0`, `r1`, `r2`), а не два, как было записано
в разделе 4.4. Прямое повторное дизассемблирование `DMA1_CH1`
(`0x08000644`, без опоры на прежний пересказ) даёт:

- ветка **TC** (`DMA1_ISR&2`): `r2 = *(halfword*)(r4+2)`, затем
  `r1 = r2` (буквально `mov r1,r2`) — то есть **`r1 == r2`** на входе в
  `thunk_EXT_FUN_100001b8`.
- ветка **HT** (`DMA1_ISR&4`): `r2 = *(halfword*)(r4+2)`, затем
  **`r1` явно обнуляется** (`movs r1,#0`) — то есть здесь **`r1 == 0`**,
  а `r2` — тот же halfword, что и в ветке TC.
- `r0 = 0x20007840` — общий указатель, подтверждено (совпадает в обеих
  ветках, литерал перепроверен напрямую).

**Что делает найденный код с этими тремя аргументами (CONFIRMED по
инструкциям):**
- `r3 = 0x200048a8` (литерал в теле функции) — это **`0x200048a0 + 8`**,
  то есть смещение `+8` от того же указателя-структуры `r4`, который
  `DMA1_CH1` использует необработанным (раздел 4.4). Функция читает
  `[r3+8]` (= `[структура+0x10]`) и `[r3+0xc]` (= `[структура+0x14]`),
  прибавляет к каждому из них входной `r2` и пишет обратно — то есть
  **`r2` — это аддитивный инкремент, применяемый к двум 32-битным полям
  структуры на каждый вызов**, а не индекс и не флаг.
- `r1` дальше по функции используется как **база смещения при записи
  выходных отсчётов** (виден код вида `[базовый_указатель + r1*2]`,
  раздел с копированием отсчётов через `strh.w ... , [rX, r3, lsl #1]` —
  собственный внутренний индекс функции `r3`, не путать с входным `r2`
  вызывающей стороны, регистры Capstone переиспользует).
- Внутри функции есть развилка по значению `*(word*)[0x200049e8]`
  (сравнение с `1`): при значении `1` пропускается блок, где `r8`/`sb`
  один раз заранее разыменовываются как **фиксированная пара опорных
  значений**, и вместо этого используется вариант цикла, где **оба**
  операнда `smlad` берутся из **двух независимо инкрементируемых
  указателей** (`r2`/`sb` в этой ветке) — то есть один и тот же цикл
  корреляции существует в двух вариантах: "буфер против одной
  фиксированной опорной пары" и "буфер против двух независимо
  проходимых массивов". Какой из вариантов — "калибровочный"/"первый
  проход", а какой "рабочий" — не установлено (UNKNOWN, не гадать).
- В конце функции — отдельный блок с однобайтовым счётчиком по адресу,
  на который указывает `r6` (внутренний, инициализируется в начале
  функции), сравниваемым с `0x64` (100): пока `< 100` — копирует пары
  16-битных отсчётов из двух источников в два выходных буфера и
  увеличивает счётчик; при достижении `100` — ранний выход (см. выше).
  **Важно не путать этот счётчик-100 с counter-100 в `FUN_08017dc8`
  (раздел 6.6, защита по низкому напряжению) — это два разных счётчика в
  двух разных функциях, совпадение порогового значения не означает
  связь между ними.**

**PROBABLE (интерпретация, не факт, обновлено 2026-09-08 с учётом новой
границы):** правдоподобная связная картина — `DMA1_CH1` обслуживает
половины кольцевого DMA-буфера АЦП; `r1` (0 для HT, halfword-значение для
TC) выбирает, с какой половины буфера брать отсчёты для корреляции
(типичная схема double-buffering: "первая половина готова" / "вторая
половина готова"), а `r2` — общий для обеих половин счётчик-приращение,
накапливаемый в структуре `0x200048a0` как "всего обработано отсчётов" по
двум независимым 32-битным полям (возможно, раздельно для двух
корреляционных каналов — синус/косинус, согласуется с парой аккумуляторов
`smlad r6.../r7...`, раздел 4.4). Вся функция целиком (с учётом
исправленной границы) выглядит как **накопительный конвейер заполнения
буфера отсчётами в несколько стадий** — плотная фаза (первые 100
отсчётов подряд), затем разреженная/децимированная фаза (до 500), затем
пошаговый режим по одному отсчёту за вызов с каскадным диспетчером стадий
(пороги `6/8/0x18/0x20/0x30/0x40` от `0x200049e8`) — согласуется с ролью
"наполнение окна корреляции по мере поступления новых DMA-блоков", но
это интерпретация, не факт. Сам механизм записи `0x200049e8` (кто и
когда его инкрементирует/сбрасывает) и физический смысл опорных таблиц
(`sb`, `r8`) из более ранней части функции — **не установлены, UNKNOWN**,
декодирование требует отдельной задачи (см. раздел 13).

**Явно НЕ делаем (сохраняя правило раздела "Явно НЕ делаем пока"):** не
приписываем найденным аккумуляторам единицы измерения (В/Ом/градусы), не
восстанавливаем формулу R/L/C по этому фрагменту — недостаточно данных
без установления, где пишется `0x200049e8`, и без разбора оставшихся
вызывающих.

### 6.8 Поиск точки записи `0x200049e8` — не найдена; найдена точка
чтения в новой, ранее неразобранной функции `~0x08017f70` — CONFIRMED
(2026-09-08)

**Метод:** сырой байтовый поиск литерала `0x200049e8` (little-endian
слово `E8 49 00 20`) по всему образу флеша (`0x08000000..0x0802e3f3`),
не только в CCM RAM. Найдено **3 вхождения**, все выровнены по слову:

```
0x0802dd9c  — внутри флеш-зеркала CCM RAM (ram-эквивалент 0x100005c0),
              уже известное место "сравнение с 1" из ранней части
              thunk_EXT_FUN_100001b8 (раздел 6.7)
0x0802e300  — внутри флеш-зеркала CCM RAM (ram-эквивалент 0x10000b24),
              уже известный литерал из блока диспетчера стадий
              (раздел 6.7)
0x08018138  — НОВОЕ: в обычном флеше, вне CCM RAM, литерал-пул после
              ранее неразобранной функции
```

Третье вхождение проверено дизассемблированием (Capstone) вручную
подобранного диапазона `0x08017f70..0x08018108` (граница функции по
`push.w`/`pop.w` не проверена так же строго, как в разделе 6.7 — это
PROBABLE, не CONFIRMED). Инструкция-источник — `0x08017ff6: ldr r0,
[pc, #0x140]` → резолвится ровно в `0x08018138` = `0x200049e8`
(проверено вычислением `(addr+4)&~3 + imm`, не на глаз). Дальше:

```
0x08017ff6  r0 = <литерал>            ; r0 = адрес 0x200049e8
0x08017ff8  r1 = *r5                  ; r5 = адрес 0x200049f4 (др. литерал,
                                        ; +0xc от 0x200049e8 — соседнее поле)
0x08017ffa  r0 = *r0                  ; r0 = *0x200049e8   (ЧТЕНИЕ)
0x08017ffc  r0 = r1 * r0
0x08017ffe  r1 = r0 >> 0x10
0x08018000  strh r1, [r4, #0x10]
0x08018002  strh r0, [r4, #0x12]
```

Это **чтение**, не запись. `r4` в этой функции — не постоянный указатель,
а `r4 = *(0x20004650 + 0x44)` (загружен в прологе через `r6`-литерал
`0x20004650`), то есть указатель на некую **структуру-отчёт**, которая
заполняется полями по смещениям `0x06, 0x08, 0x0a, 0x0c, 0x0e, 0x10,
0x12, 0x14...0x28` (≈21 поле по 2 байта, около 42 байт) — похоже на
формирование пакета результата измерения (см. открытый пункт "полный
протокол последовательного порта", раздел 11).

**Важно и неожиданно:** та же функция интенсивно вызывает
`FUN_08002932`, `FUN_08002698`, `FUN_08002900`, `FUN_080029b8`,
`FUN_080133d0` — это ровно те **нерасшифрованные fixed-point/float
библиотечные функции**, которые пункт 20 раздела 13 отмечал как
блокер для декодирования калибровочных констант `FUN_08017dc8`. Плюс
здесь же прямые `vdiv.f32`/`vcvt.f32.u32`/`vcvt.u32.f32` (аппаратный
VFP). То есть это, по всей видимости, **первый найденный код,
связывающий сырые накопители корреляции с итоговым числовым
результатом измерения** — вероятный кандидат на (часть) искомого
"расчёта R/L/C" из раздела 11. Это PROBABLE, не CONFIRMED: числовая
семантика полей структуры и смысл `FUN_08002932`/`FUN_08002698`/итд.
не декодированы, только зафиксирован факт вызова и его расположение.

**Итог по исходной задаче (запись в `0x200049e8`):** НЕ найдена. Прямой
поиск по литералу нашёл только 3 места чтения абсолютного адреса, ни
одного `str`/`strb`. Вероятная причина — запись идёт не через
абсолютный литерал, а через смещение от соседнего базового указателя
(например, `0x200049e4`, `r7`-литерал из этой же функции, или другой
уже известный базовый адрес структуры), что сырой поиск байтовой
константы `0x200049e8` не находит. **Не гадаю дальше** — это отдельная
задача (см. раздел 13, пункт 24, уточнено).

### 6.9 Разбор "нерасшифрованных fixed-point функций" (пункт 20) — CONFIRMED,
это стандартная библиотека soft-float, не кастомная логика (2026-09-08)

Дизассемблированы все пять функций, отмеченных в пункте 20 раздела 13 как
блокер, плюс их внутренние вызовы. Результат однозначный — это компиляторные
библиотечные процедуры двойной точности (EABI soft-float), а не
проприетарный fixed-point код прошивки:

| Адрес | Идентификация | Признак |
|---|---|---|
| `0x08002698` | `__aeabi_dmul` (умножение `double`) | извлечение знака через `eor`+маска `0x80000000`, экспоненты через `ubfx #0x14,#0xb` (11-бит поле IEEE-754 double), `umull`/`mla` на мантиссах |
| `0x08002900` | `__aeabi_d2iz`/`d2uiz`-подобная (double → int, round-to-zero) | сравнение экспоненты с `0x3ff`/`0x433` (границы представимости double как целого) |
| `0x08002932` | `__aeabi_i2d` (int → double) | классическая нормализация: извлечение знака, `lsrs`+`add #0x380`, сборка экспоненты/мантиссы |
| `0x080029b8` | `__aeabi_d2f` (double → float) | вычитание разницы смещения экспонент double/float: `sub r2,r2,#0x380` — `0x380`=896=1023-127, ровно разница биасов |
| `0x08002be8` | `trunc()`/`floor()`-подобная для `double` | сравнение экспоненты с `0x433` (1075) и `0x3fe` (1022) — стандартные пороги "число уже целое"/"число < 1" для double |
| `0x0800268c`/`0x8002692` | инвертирование знакового бита + переход на `0x800254a` (общий `__aeabi_dadd`) | это тело `__aeabi_dsub` (вычитание = сложение с инвертированным знаком) |
| `0x080133d0` | вызывает `trunc(x)` (`0x8002be8`), затем `x - trunc(x)` (`0x800268c`→`dadd`) | **вычисление дробной части `double`** (fmod-подобная обёртка) — вероятно используется для нормализации фазы при генерации опорной синус/косинус-таблицы (раздел 4.4/6.7), но это PROBABLE, не проверено по вызывающим |

**Вывод:** задача пункта 20 переформулирована — искать нужно не "как
устроены кастомные fixed-point конвертеры" (их нет), а **какие
double-константы** (калибровочные коэффициенты) лежат в литерал-пуле
рядом с местами вызова этих функций, и что именно за формула
складывается из вызовов (`i2d` → `dmul` → `d2f`/`d2iz` — типичная цепочка
"привести целое к double, умножить на коэффициент, привести обратно").
Для `FUN_08017dc8` (раздел 6.6) и для новой функции `~0x08017f70..
0x08018108` (раздел 6.8) это означает: найти double-литералы в их
литерал-пулах и попытаться распознать физический смысл коэффициентов
(например, опорное напряжение АЦП, масштаб R/L/C) — не разбор
инструкций библиотеки, а разбор конкретных констант вызывающего кода.
Библиотечные функции сами по себе декодированы полностью и дальнейшего
разбора не требуют.

### 6.10 Потребители переменных `0x20005668`/`0x20005670` (раздел 6.8) —
найдена вторая точка чтения: функция автоматического выбора разрядности
отображения — CONFIRMED структура вызова, PROBABLE назначение (2026-09-08)

Обе переменные `0x20005668` (var A) и `0x20005670` (var B), которые
раздел 6.8 нашёл читаемыми в функции `~0x08017f70` (там их умножают на
константу `100.0` перед упаковкой в отчёт), также читаются ещё в одном
месте — в функции с диапазоном примерно `0x0800a9f8..0x800ad28`
(точные границы не проверялись так строго, как в разделе 6.7; это
рабочая оценка). Проверено дизассемблированием и вычислением адресов
литералов (не на глаз):

```
d8 = 99.0     (double-константа @0x800ad48, vldr [pc])
d9 = 1000.0   (double-константа @0x800ad58, vldr [pc])
...
r4 = &var_B (0x20005670)          ; @0x800ac24
d0 = *r4
bl __aeabi_lcmp-подобная (0x8002958)  сравнение битового представления
                                        d0 против d8 (=99.0) как 64-бит
bhs -> (var >= 99.0): форматирование БЕЗ масштаба (bl 0x801798c, 1 арг)
иначе (var < 99.0): d0 = var * 1000.0 (d9, через __aeabi_dmul/0x8002698),
                     форматирование С масштабом и десятичными разрядами
                     (bl 0x80179c8, аргументы decimals=3/width=4/1)
```

Плюс отдельная, более ранняя в этой же функции (`0x800abc0..0x800abe2`)
многоступенчатая проверка через сравнение float-битов с порогами
(`0x3f800000`=1.0f и ещё два порога из `r8`/`sb`, не декодированы) —
похоже на классический выбор разрядности/масштаба для ЖК-индикации
(типично для LCR-метров: например "показывать как `XX.X`, если < 99,
иначе как целое `XXX`/`XXXX`"), но конкретные пороги `r8`/`sb` не
идентифицированы — PROBABLE, не UNKNOWN исключительно из лени, просто
не является предметом этой сессии.

**Значение находки:** `0x20005668`/`0x20005670` теперь имеют ДВУХ
независимых потребителей — форматирование для дисплея (этот раздел) и
упаковка в отчёт ×100 (раздел 6.8) — оба варианта чтения, ни одного
найденного места записи. Это убедительно говорит, что эти две
переменные — **уже готовые, финальные измеренные величины** (вероятно,
два компонента результата — например R и X, или величина и фаза,
возможно сами R/L/C), а не промежуточные накопители. Само место, где
они ВЫЧИСЛЯЮТСЯ и записываются — по-прежнему не найдено (сырой поиск
литерала находит только эти 2 уже известных места чтения, как и было с
`0x200049e8` в разделе 6.8 — вероятно, запись тоже идёт через смещение
от соседнего базового указателя, а не прямым литералом этого адреса).
**Это главный кандидат на прямой путь к разделу 11 ("расчёт R/L/C")** —
следующий логичный шаг: найти запись в `0x20005668`/`0x20005670` тем же
методом (поиск смещений от соседних адресов, а не только точного
литерала), либо трассировкой вызывающих функций сверху вниз от `main()`.

`FUN_080045c4()` — **адрес не подтверждён** (см. раздел 10) —
использовался как механизм доступа к параметрам конфигурации; роль
правдоподобна (числовые ID ниже присутствуют в прошивке), но конкретный
адрес функции требует переустановки для текущей цели анализа.

Известные ID, обнаруженные в заметках реверс-инжиниринга:
`0x556`, `0x557`, `0x558`, `0x559`, `0x560`, `0x561`, `0x562`, `0x563`, `0x567`, `0x568`, `0x569`, `0x571`, `0x572`, `0x575`, `0x576`, `0x577`, `0x578`, `0x579`, `0x580`, `0x581`, `0x582`.

Их точные смысловые имена пока не установлены. Сохранять числовые ID до
появления доказательств для именования.

### 6.11 Реальная граница функции результата (`FUN_08009618`), структура
результата `0x20004a48` (Rs/Rp/L/C — CONFIRMED по строкам прошивки) и
идентификация `Re`/`Im` — CONFIRMED, 2026-09-08 (уточнено 2026-09-09,
раздел 6.22: смещения `+0x50/+0x60/+0x80/+0x88` — display shadow, не
цель записи, см. пометку в таблице ниже)

Продолжение раздела 6.10 (пункт 25 раздела 13). Работа велась без Ghidra
GUI, но с доступом к уже готовому файлу декомпиляции
`nRLC_2_0_12_FREEWARE_ghidra_decompiled.txt` — искал по символам `DAT_*`
и строковым константам, которые Ghidra уже вытащила из прошивки, а не
переразбирал байты заново.

**Граница функции — CONFIRMED, пересмотрена.** Функция, ранее оценённая
в разделе 6.10 как `~0x0800a9f8..0x800ad28` (рабочая оценка, не
проверенная строго), на самом деле — `FUN_08009618 @ 0x08009618`,
заметно длиннее указанного диапазона. Это главная функция отрисовки
экрана результата измерения.

**Структура результата — CONFIRMED, база `0x20004a48`.** Указатель на
неё резолвится из литерала `DAT_0800a860` (`iVar18 = DAT_0800a860`
внутри `FUN_08009618`). Тот же литерал `0x20004a48` встречается ещё в
7 местах по всему образу (`0x08001178`, `0x08002004`, `0x08009a18`,
`0x08009f34`, `0x0800ade4`, `0x08017410`, `0x08017cc4`) — то есть это
общая, часто используемая структура состояния измерения, не
единичная ссылка.

Поля структуры подписаны не по догадке, а напрямую строками, которые
Ghidra извлекла из прошивки как ASCII-константы и присвоила им имена
символов вида `s_ИМЯ__адрес`:

| Смещение | Величина | Строка-подпись в прошивке |
|---|---|---|
| `+0x50` | **Rs** (последовательное сопротивление) | `s_Rs__0800a868` |
| `+0x60` | **Rp** (параллельное сопротивление) | `s_Rp__0800a87c` |
| `+0x80` | **L** (индуктивность) | `s_L__0800a8a0` |
| `+0x88` | **C** (ёмкость) | `s_C__0800a890` |
| `+0` (байт) | флаг состояния/готовности | пишется напрямую из `main()` (`FUN_08016274`, район `0x08017410`, управляет вызовом `FUN_08009618()`) |

> ⚠ **УТОЧНЕНИЕ 2026-09-09 (раздел 6.22), важно для дальнейшего поиска
> записи.** Строки-подписи выше верны для UI (это то, что реально
> печатается на экране под именами Rs/Rp/L/C), но раздел 6.22 показал,
> что `+0x50/+0x60/+0x80/+0x88` — это **display shadow**: сглаженная
> копия для анимации цифр (см. блок lerp в `FUN_08009618`, строки
> ~11150-11330), не то место, куда пишет вычислитель результата.
> Физическая цель записи, по структуре lerp-блока — `+0x30` (Rs),
> `+0x40` (Rp), `+0x90` (L), `+0x98` (C). **Статус переноса ярлыков —
> PROBABLE, не CONFIRMED переименование структуры**: ни в одно из этих
> четырёх смещений (+0x30/+0x40/+0x90/+0x98) пока не найдено ни одной
> прямой записи с явным контекстом "это Rs" и т.п. — только то, что
> lerp-блок использует их как источник цели анимации для полей,
> подписанных Rs/Rp/L/C. Сама функция-писатель ещё не найдена (раздел
> 6.22, "следующий шаг"). **Если в новой сессии снова возникнет соблазн
> искать запись именно в `+0x50/+0x60/+0x80/+0x88` — это тупик,
> проверено и закрыто в 6.22; искать нужно `+0x30/+0x40/+0x90/+0x98`.**

Также в этой же функции встречены строки-подписи `Z` (`s_Z__0800a910`),
`X` (`s_X__0800ad34`), `Q` (`s_Q__0800adb0`), `D` (`s_D__0800ad60`) —
классический полный набор параметров LCR-метра, но их смещения в
структуре `0x20004a48` в этой сессии не проверялись так же строго, как
Rs/Rp/L/C выше (не путать со значением `+0x18/+0x1c`, которое
используется для переключения между режимами отображения `Rs/Rp` и
`L/C`, и не подтверждено как отдельное поле `Z`/`X`).

**`Re` (`0x20005668`) и `Im` (`0x20005670`) — CONFIRMED, отдельная от
структуры `0x20004a48` пара глобальных переменных.** В той же функции
`FUN_08009618` найдены строковые подписи `s_Re__0800ad9c` и
`s_Im__0800adc0`, напрямую предшествующие чтению `*DAT_0800ad50`
(`=0x20005668`) и `*DAT_0800add4` (`=0x20005670`) соответственно. Это
закрывает открытый в разделе 6.10 вопрос "что такое var A/var B" —
это реальная и мнимая часть комплексного импеданса, а не абстрактный
парный результат.

**Место вычисления/записи — по-прежнему НЕ найдено, поиск подтверждён
как исчерпывающий (для полей `+0x50` и далее — см. уточнение выше).**
Прямой байтовый поиск литералов `0x20005668` и
`0x20005670` по всему образу флеша (не только вокруг известных функций)
даёт ровно по 2 совпадения на каждый — те же самые 2 места чтения,
что уже задокументированы в разделах 6.8/6.10 (дисплей и упаковка
отчёта). Ни одной записи. Аналогичная попытка для базы структуры
`0x20004a48` тоже не нашла прямой записи double-полей (`+0x50` и
дальше) — найдена только запись байта состояния по смещению `0` (в
`main()`, управляет флагом готовности экрана, не самим значением).
**Раздел 6.22 объясняет почему: `+0x50` и далее — это display shadow
(см. уточнение выше), туда пишет не вычислитель, а lerp-блок самой
`FUN_08009618`; искать нужно запись в `+0x30/+0x40/+0x90/+0x98`, для
которых аналогичный исчерпывающий литерал-поиск ЕЩЁ НЕ проводился.**
Вывод не изменился по сравнению с `0x200049e8` (раздел 6.8): запись,
судя по всему, идёт через смещение от соседнего/локального базового
указателя внутри функции-вычислителя, а не через прямой литерал этого
конкретного адреса — такой паттерн сырой поиск константы не находит.

**Практический итог:** раздел 11 ("расчёт R/L/C") сужен предметно —
локализована и размечена сама структура итогового результата
(`Rs`/`Rp`/`L`/`C`/`Re`/`Im`), но не функция, которая её заполняет.
Следующий шаг остаётся тем же, что предлагался в разделе 6.10 —
трассировка вызовов сверху вниз от `main()`/цикла измерения к моменту,
когда флаг готовности (`0x20004a48+0`) выставляется в `1`, — эта точка
кода, скорее всего, находится непосредственно рядом с записью
Rs/Rp/L/C/Re/Im.

### 6.12 `FUN_0800dbf8` — вероятный конвертер Re/Im → физическая величина, CONFIRMED структура, 2026-09-08

Найдена продолжением раздела 6.7: `DAT_08001620` (общий указатель на
аккумулятор корреляции, значение `0x20007840`, уже подтверждён в 6.7 как
`r0 = 0x20007840`, общий для обеих веток `thunk_EXT_FUN_100001b8`)
передаётся в `FUN_0800dbf8(DAT_08001620)` сразу после завершения
корреляционного цикла в вызывающей функции.

**Тело `FUN_0800dbf8(int param_1)` — CONFIRMED прямым чтением, семантика полей PROBABLE:**

```
fVar9  = FUN_08000d5a()                 // диапазонный коэффициент, канал 1 (см. ниже)
fVar10 = FUN_08000d7c()                 // диапазонный коэффициент, канал 2
uVar11 = FUN_08000d9e()                 // калиброванная/дефолтная константа
fVar13 = (*DAT_0800dd44) * DAT_0800dd48 // DAT_0800dd44 -> указатель на 0x20004998 (runtime-значение);
                                         // DAT_0800dd48 = 0x39800000 = 0.000244141 = 2^-12 — шкала 12-бит АЦП/ЦАП
fVar12 = FUN_08015de0(param_1)          // нормировка — вероятно, число накопленных отсчётов N

// канал 1: пара аккумуляторов param_1+0x08/+0x0c (Re) и +0x10/+0x14 (Im), делятся на fVar12,
//          сворачиваются в магнитуду через FUN_080029b8
// канал 2: аналогично на param_1+0x20/+0x24 (Re) и +0x28/+0x2c (Im)

uVar14 = FUN_08012a04(магнитуда_канал1, fVar13/fVar9,  DAT_0800dd4c)  // канал 1 в физические единицы
uVar15 = FUN_08012a04(магнитуда_канал2, fVar13/fVar10, DAT_0800dd4c)  // канал 2 в физические единицы
uVar14 = FUN_080129e2(uVar14, uVar15)                                 // канал1 ⊘ канал2
uVar14 = FUN_08012a04(uVar14, uVar11, DAT_0800dd4c)                   // применена калибровка uVar11

*(0x20004a4d) = 1     // DAT_0800dd54 -> 0x20004a4d = известная структура результата 0x20004a48 + смещение 0x5 —
                       // НОВЫЙ найденный флаг (отдельный от уже известного флага по смещению 0, раздел 6.11)
*DAT_0800dd50 = 1      // DAT_0800dd50 -> 0x20004914 — отдельная область, не входящая в диапазон 0x20004a48+
*(0x20004914+0xc) = uVar14   // итоговое double-значение (адрес записи 0x20004920)
```

Все адреса-константы (`DAT_0800dd44`→`0x20004998`, `DAT_0800dd48`=`2^-12`,
`DAT_0800dd4c`=`0`, `DAT_0800dd50`→`0x20004914`, `DAT_0800dd54`→`0x20004a4d`)
разрешены напрямую по `.hex` (не по декомпиляции — Ghidra не подписывает
эти литералы символами, найдены сырым 4-байтовым поиском).

**PROBABLE (сильная гипотеза, не доказано до конца):** два канала
(`param_1+0x08.. / +0x20..`) соответствуют **U_Measure**/**I_Measure**
(`PA6`/`PA0`, подтверждены в `GPIO_MAP.md`), и `FUN_080129e2` вычисляет
**Z = U/I** — это canonical способ измерения импеданса синхронным
детектированием. Смещение `+5` в структуре `0x20004a48` — новый,
ранее не найденный флаг готовности (вероятно, один из нескольких
раздельных флагов по значениям Rs/Rp/L/C/Re/Im, отдельно от общего флага
"показать результат" по смещению `0`, раздел 6.11).

**Открыто:** какая именно из шести величин (Rs/Rp/L/C/Re/Im) записывается
в `0x20004920` при данном вызове — видимо, `FUN_0800dbf8` вызывается
многократно с разным контекстом/режимом, каждый раз считая одно значение;
не установлено, что определяет текущий режим и куда именно (`0x20004920`
vs поля структуры `0x20004a48`) фактически попадает результат для
отображения. Семантика `FUN_08012a04`/`FUN_080129e2`/`FUN_080029b8`
(вероятно fixed-point/float арифметика общего назначения) не разобрана
по инструкциям, взята по паттерну использования. Следующий шаг —
трассировка вызывающих `FUN_0800dbf8(...)`.

### 6.13 Вызывающие `FUN_0800dbf8` — оба внутри `FUN_08000f52`, вызываемой из диспетчера стадий `FUN_08000618` — CONFIRMED цепочка, 2026-09-08

`FUN_0800dbf8` имеет ровно два места вызова в прошивке, **оба внутри одной
функции** `FUN_08000f52(float param_1)` (`0x08000f52`, ~1100 строк
декомпиляции — не разобрана целиком):

- вызов 1 (строка декомпиляции ~1392): `FUN_0800dbf8(DAT_08001620)` — буфер `0x20007840`
- вызов 2 (строка ~1616): `FUN_0800dbf8(DAT_08001b38)` — буфер `0x20007c70`

То есть `FUN_08000f52` — оркестратор одного цикла измерения: обрабатывает
через одну и ту же функцию конвертации (раздел 6.12) два вызова —
**но это не два независимых канала** (см. уточнение раздела 6.18):
`0x20007c70` — не отдельный источник данных, а снятая КОПИЯ
`0x20007840` в момент, когда показания признаны стабильными. Формулировка
"канал 1 / канал 2" ниже в этом разделе — устаревшая рабочая гипотеза
на момент написания, оставлена для истории, актуальна версия —
"живой аккумулятор `0x20007840` / его периодический снимок `0x20007c70`".

**`FUN_08000f52` вызывается только из одного места** — `FUN_08000618`
(диспетчер стадий измерения над структурой `0x20005610`, ранее не
задокументирован, найден в этой же сессии при поиске использования
`0x20005610`), конкретно в ветке `case '\x06':` (стадия 6) её внутреннего
`switch(cVar4)` по байту состояния структуры. Соседние ветки того же
`switch`:

- `case '\x04':` → вызывает `FUN_08001f46()` — не разобрана;
- `case '\a' (7):` → устанавливает состояние обратно в `'\x06'` и
  проваливается в ту же метку — похоже на повторный проход стадии 6.

**Вызов `FUN_08000f52()` в декомпиляции без видимых аргументов** — её
единственный параметр (`float param_1`) передаётся через регистр `s0`,
установленный раньше по коду; типичный артефакт декомпиляции float-ABI,
сам параметр этим не опровергнут и не подтверждён, просто не виден
текстом здесь.

**Итоговая подтверждённая цепочка на сегодня:**
```
FUN_08000618 (диспетчер стадий, стр. 0x20005610)
  └─ case 6 → FUN_08000f52(float)               [не разобрана целиком]
        ├─ буфер 0x20007840 (живой аккумулятор) → FUN_0800dbf8(...) (раздел 6.12)
        └─ буфер 0x20007c70 (снимок 0x20007840, раздел 6.18) → FUN_0800dbf8(...) (раздел 6.12)
```

**Открыто:** тело `FUN_08000f52` целиком (что именно происходит между
двумя вызовами `FUN_0800dbf8`); точная роль второго буфера `0x20007c70`
(коррелятор пишет напрямую или это копия/пост-обработка); `FUN_08001f46`
(стадия 4); что взводит `*DAT_08000684` (флаг, разрешающий выполнение
`FUN_08000618`) и с какой частотой/откуда вызывается сам диспетчер.

**Источник буфера `0x20007840`:** DMA1_CH1 ← ADC1_DR ← ADC1_IN1/PA0
(I_Measure) — channel mapping **CONFIRMED**, см. раздел 6.14.
Семантика «в буфере именно токовые отсчёты» — PROBABLE до trace
использования `0x200058E8` внутри коррелятора.

### 6.14 ADC1/ADC2 channel mapping и DMA1/DMA2 source — CONFIRMED (с оговорками) 2026-09-08

Прямым чтением кода инициализации (`FUN_08003140` + MSP `FUN_08005404` +
запись регистров DMA) установлено раздельно по уровням:

#### A. DMA1_CH1 ← ADC1_DR — CONFIRMED
- `DMA1_CPAR1` = `0x50000040` = **ADC1_DR**
- `DMA1_CMAR1` = `0x200058E8` (memory buffer)
- `DMA1_CCR1` |= HTIE | TCIE | EN
- `ADC1_CFGR` |= DMAEN | DMACFG (continuous DMA)
- NVIC IRQ 11 (`DMA1_CH1`) включён

#### B. ADC1 rank1 = Channel 1 = PA0 — CONFIRMED
- `FUN_08004d94(ADC1, {Channel=1, Rank=1, ...})`
- MSP GPIO: pin mask `0x1` = **PA0**, Mode = Analog
- STM32F303: ADC1_IN1 = PA0
- GPIO_MAP / netlist: PA0 = **I_Measure**

#### C. ADC2 rank1 = Channel 3 = PA6 — CONFIRMED
- `FUN_08004d94(ADC2, {Channel=3, Rank=1, ...})`
- MSP GPIO: pin mask `0x40` = **PA6**, Mode = Analog
- STM32F303: ADC2_IN3 = PA6
- GPIO_MAP / netlist: PA6 = **U_Measure**
- `DMA2_CH1`: CPAR = **ADC2_DR** (`0x50000140`), CMAR = `0x20005758` — **CONFIRMED**
  (прямая запись `*(DMA2_CCR1+8)` / `*(DMA2_CCR1+12)`)

#### D. Семантика буферов и «основной path» — PROBABLE
| Утверждение | Оценка |
|-------------|--------|
| DMA1_CH1 получает из ADC1_DR | **CONFIRMED** |
| DMA1_CH1 CPAR = ADC1_DR | **CONFIRMED** |
| DMA1_CH1 CMAR = `0x200058E8` | **CONFIRMED** |
| ADC1 channel/rank → PA0 | **CONFIRMED** |
| PA0 = I_Measure | **CONFIRMED** (GPIO_MAP + netlist) |
| DMA1_CH1 пишет в `0x200058E8` | **CONFIRMED** |
| DMA2_CH1 CPAR = ADC2_DR | **CONFIRMED** |
| DMA2_CH1 CMAR = `0x20005758` | **CONFIRMED** |
| ADC2 channel/rank → PA6 | **CONFIRMED** |
| PA6 = U_Measure | **CONFIRMED** (GPIO_MAP + netlist) |
| `0x200058E8` содержит именно токовые отсчёты | **PROBABLE** |
| `0x20005758` содержит именно напряженческие отсчёты | **PROBABLE** |
| Это основной RLC acquisition path | **PROBABLE** |

**Схема (уровни разделены):**
```
CONFIRMED:
  PA0 → ADC1_IN1 → ADC1_DR → DMA1_CH1 → 0x200058E8
  PA6 → ADC2_IN3 → ADC2_DR → DMA2_CH1 → 0x20005758

PROBABLE (downstream):
  0x200058E8 / 0x20005758 → thunk_EXT_FUN_100001b8 → 0x20007840
                         → FUN_08000f52 → FUN_0800dbf8 → физ. величина
```

**Открыто:** trace чтения `0x200058E8`/`0x20005758` внутри CCM-коррелятора;
роль `0x20007c70`; раскладка полей `0x20007840`.

**Верификация (2026-09-08, проверка отдельным чатом):** все ключевые
числовые утверждения раздела перепроверены независимо — напрямую по
`.hex` (разыменованы литералы `DAT_08003410`/`08003414`/`08003418`/
`08003420`/`08003424` из `FUN_08003140`, минуя decompiled-текст, где эти
значения не показаны как числа), и по официальному датащиту
STM32F303xB/xC (pin definitions: PA0→ADC1_IN1, PA6→ADC2_IN3). Все
адреса регистров, битовые маски CCR1 (HTIE|TCIE|EN), ADC1_CFGR/ADC2_CFGR
(DMAEN|DMACFG), NVIC IRQ11 и GPIO-маски (`0x1`/`0x40`) подтвердились
без расхождений.

### 6.15 Связь коррелятора (раздел 6.7) с сырыми ADC-буферами (раздел 6.14) —
найден источник данных канала A — CONFIRMED арифметика, PROBABLE
интерпретация параметра, 2026-09-08

Продолжение пункта 27 раздела 13 (разбор `FUN_08000f52`) — по пути
дизассемблировал самое НАЧАЛО `thunk_EXT_FUN_100001b8` (`0x100001b8`),
которое раньше не смотрели детально (раздел 6.7 начинал разбор дальше,
с `0x10000870`).

**Настройка указателей на входные данные — CONFIRMED прямым чтением:**
```
0x100001bc  r3 = &0x200048a8            ; литерал, вычислен через .hex
0x100001c0  ip = *(r3+8)                ; = *0x200048b0
0x100001c4  r2 = [sp+0x68]              ; параметр функции (со стека)
0x100001c6  r2 = r2 + ip
0x100001c8  *(r3+8) = r2                ; накопление — тот же паттерн,
                                        ; что и для 0x200049e8 (раздел 6.8)
...
0x100001e4  r2 = *(r3+0x10)             ; = *0x200048b8
0x100001e6  r3 = *(r3+0x14)             ; = *0x200048bc — r3 ПЕРЕОПРЕДЕЛЁН,
                                        ; теперь это ДИНАМИЧЕСКИЙ указатель
                                        ; из RAM, не литерал функции
...
0x100001d8  r2(др.) = 0x20000000        ; литерал — БАЗА SRAM, вычислено
                                        ; через .hex, не на глаз
...
0x100001f6  r5 = r2 + r1*2              ; = 0x20000000 + (параметр r1)*2
                                        ; — ИСТОЧНИК ДАННЫХ КАНАЛА A
0x100001fe  r1(др.) = r3(dynamic) + r1*2 ; — источник данных канала B,
                                        ; база берётся из RAM (0x200048bc)
```

**Ключевая находка:** источник канала A — это `0x20000000` (адрес самого
начала SRAM, не случайное совпадение — резолвится через `.hex`, не
предположение) плюс параметр функции `r1`, умноженный на 2 (индекс
16-битных отсчётов).

> ⚠ Гипотеза `r1=0x58e8` ниже — ОТКЛОНЕНА в разделе 6.17 (реальный
> `r1`/`r2` оказался runtime-значением из `0x200048a2`, не литералом).
> Оставлено как есть для истории рассуждения.

Если этот параметр `r1` при вызове из ISR
`DMA1_CH1` равен `0x58e8`, итоговый адрес получается **`0x20000000 +
0x58e8 = 0x200058e8`** — это ТОЧНО совпадает с уже подтверждённым в
разделе 6.14 адресом `DMA1_CH1 CMAR1`. Проверено арифметикой
(`0x20000000+0x58e8=0x200058e8`), не на глаз.

**Что это доказывает и что нет:**
- CONFIRMED: механизм вычисления адреса источника (`SRAM_база + параметр
  ×2`) и то, что `0x20000000+0x58e8` действительно даёт адрес буфера
  DMA1.
- PROBABLE, не CONFIRMED: что параметр `r1` при РЕАЛЬНОМ вызове из ISR
  `DMA1_CH1` действительно равен `0x58e8` — это не прочитано в этой
  сессии (не разобран сам ISR `DMA1_CH1`, только косвенно выведено из
  совпадения арифметики). Раздел 6.7 ранее предполагал, что `r1` — это
  "0 для HT, halfword-значение для TC"; если так, `0x58e8` был бы
  значением именно для случая TC (transfer complete, вторая половина
  кольцевого буфera).
- Источник канала B (`0x200048bc`, динамический указатель) — где именно
  он ЗАПИСЫВАЕТСЯ, НЕ найдено. Исчерпывающий поиск по литералу
  `0x200048bc` во всём образе — 0 совпадений. Единственная НЕ-CCM-RAM
  ссылка на базовый адрес структуры `0x200048a8` (`0x08002f04`) пишет
  float-поле по смещению `+0x20`, не указатель по `+0x14`. Вероятно,
  `0x200048bc` — часть инициализированной `.data` (статическое начальное
  значение, скопированное при старте, а не записанное инструкцией `str`)
  — это НЕ проверено, требует установления границ `.data`-копирования в
  `Reset_Handler` (раздел 2) и прямого чтения соответствующего
  флеш-источника.

**Открыто:** разобрать сам ISR `DMA1_CH1` (не тайл-функцию, а
непосредственно обработчик прерывания) на предмет фактического значения
`r1` при вызове; установить `.data`-источник `0x200048bc` и проверить,
равен ли он `0x20005758` (DMA2-буфер, канал B).

### 6.16 Место записи `Re`/`Im` (`0x20005668`/`0x20005670`) найдено внутри
`FUN_08000f52` — CONFIRMED адреса и механизм, PROBABLE точная формула,
2026-09-08 (сведено из независимой сессии + перекрёстно подтверждено
разделом 6.12)

Продолжение пункта 25 раздела 13 и раздела 6.11. Работа велась
параллельно с независимой сессией, которая в это же время нашла раздел
6.12 (`FUN_0800dbf8`) — координаты обеих находок сошлись на **одном и
том же буфере `0x20007840`** с точным совпадением смещений, что
взаимно подтверждает оба независимых захода.

**Структура `0x20005610` (`puVar21`, диспетчер стадий, раздел 6.13) —
CONFIRMED, прямая запись найдена.** Внутри `FUN_08000f52`:

```c
pfVar4 = DAT_08001180;   // = 0x20007840 — ТОТ ЖЕ буфер, что в разделе 6.12/6.13
                          // (DAT_08001620 там же резолвится в 0x20007840)
```

Далее, пока счётчик отсчётов `*(puVar21+0x22) < 0xf9` (249), каждый
вызов функции читает из этого буфера **те же самые смещения**, что
раздел 6.12 определил как "канал 1"/"канал 2":

| Прочитано (`pfVar4[N]`, float) | Смещение в `0x20007840` | Соответствует (раздел 6.12) |
|---|---|---|
| `pfVar4[2]`,`[3]` | `+0x08`,`+0x0c` | канал 1, Re |
| `pfVar4[4]`,`[5]` | `+0x10`,`+0x14` | канал 1, Im |
| `pfVar4[8]`,`[9]` | `+0x20`,`+0x24` | канал 2, Re |
| `pfVar4[10]`,`[11]` | `+0x28`,`+0x2c` | канал 2, Im |

Совпадение смещений с разделом 6.12 — CONFIRMED прямым сравнением
адресов (`0x20007840+0x08` и т.д.), не совпадение по смыслу "на глаз".

Из каждой пары вычисляется отношение (`FUN_0800285a` — вероятно, то же
семейство операций, что `FUN_080029b8` в разделе 6.12, PROBABLE:
hypot/magnitude или ratio), делится на `param_1` (параметр функции,
PROBABLE — нормировка по времени/частоте цикла), и результат **пишется
в два параллельных кольцевых буфера-истории**:

```c
DAT_08001610[idx] = <результат для канала 1>;   // = 0x200060f8
DAT_08001614[idx] = <результат для канала 2>;   // = 0x200064e0
idx = *(puVar21 + 0x22)++;                       // счётчик отсчётов, растёт с каждым вызовом
```

**Контigуйность буферов `0x200060f8`/`0x200064e0` — CONFIRMED
арифметикой:** `0x200060f8 + 0xfa·4 = 0x200064e0` (проверено вычислением,
не на глаз) — то есть `DAT_08001610 + 0xfa` (при типе `undefined4*`,
арифметика в 4-байтных элементах) указывает ровно на начало
`DAT_08001614`. Оба — 250-элементные (`0xfa`) массивы `float`/`int32`,
физически смежные.

**Когда счётчик достигает `0xf9` (249) — CONFIRMED, найдена сама запись
Re/Im:**

```c
*(undefined8 *)(puVar21 + 0x58) = uVar53;   // = 0x20005668 — Re, запись НАЙДЕНА
*(undefined8 *)(puVar21 + 0x60) = uVar53;   // = 0x20005670 — Im, запись НАЙДЕНА
// затем цикл на 249 итераций, попарно перемножающий/накапливающий
// значения из DAT_08001610[i] и DAT_08001614[i] (через FUN_0800268c/
// FUN_08002698/FUN_0800254a — double-арифметика, раздел 6.9), с
// финальным накоплением обратно в (puVar21+0x58) и (puVar21+0x60).
```

Это **CONFIRMED снимает вопрос раздела 6.11/пункта 25** "где пишется
Re/Im" — адреса и сама запись найдены и прочитаны напрямую в
декомпиляции. PROBABLE (не до конца разобрано на уровне формулы): точная
математическая операция цикла — похоже на перекрёстную корреляцию двух
временных рядов (канал 1 × канал 2 по отсчётам), а не на умножение на
опорную синус/косинус-таблицу, как предполагалось при первом заходе на
эту находку в этой же сессии — это уточнение первоначальной гипотезы,
исправлено после того, как нашлось, что `DAT_08001610` сам является
буфером записи (`DAT_08001610[idx] = ...`), а не константной таблицей.

**Полная подтверждённая цепочка на сегодня для одного цикла измерения
(стадия 6 диспетчера, раздел 6.13):**
```
DMA1_CH1 ISR → thunk_EXT_FUN_100001b8 (CCM RAM, разд. 6.7)
    накапливает сырые Re/Im для 2 каналов в 0x20007840 (+0x08/0x0c/0x10/0x14/0x20/0x24/0x28/0x2c)
        │
        ├─ FUN_08000f52, вызывается 249 раз подряд (стадия 6):
        │     читает 0x20007840, считает отношение канал1/канал2,
        │     копит 249-точечную историю в 0x200060f8 / 0x200064e0
        │     → на 249-м вызове считает Re/Im и пишет в 0x20005610+0x58/0x60
        │       (= 0x20005668/0x20005670, ОТОБРАЖАЕМЫЕ Re/Im, разд. 6.11)
        │
        └─ FUN_08002ec0(0x20007840) → FUN_0800dbf8(0x20007840) (разд. 6.12)
              считает магнитуду/отношение каналов НАПРЯМУЮ из сырого
              буфера (без 249-точечной истории) → 0x20004920
              → копируется в 0x20005610+0x50/0x54 (разд. 6.11's puVar21+0x50)
```

Иными словами, у прошивки, похоже, ДВА параллельных пути обработки
одного и того же сырого буфера `0x20007840`: быстрый (`FUN_0800dbf8`,
скаляр за один вызов, PROBABLE = похоже на грубую/промежуточную оценку
величины) и накопительный (`FUN_08000f52`, 249-точечная история,
итоговые Re/Im). Это PROBABLE интерпретация архитектуры, не
подтверждена документально — оба пути видны в коде, их конкретное
назначение (промежуточный/финальный, per-range/per-channel) не
установлено.

**Открыто:** точная формула цикла на 249 итераций (что именно
накапливается — произведение, разность, или что-то ещё); связь
`puVar21+0x50` (Z-подобное значение из `0x20004920`) со структурой
`0x20004a48` (Rs/Rp/L/C, раздел 6.11) — **это последнее недостающее
звено** между уже найденными Re/Im/Z-фрагментами и итоговой отображаемой
структурой результата.

### 6.17 ISR `DMA1_CH1` (`0x08000644`) разобран целиком — veneer-таблица
`0x08002dcc` CONFIRMED, трёхрегистровая конвенция раздела 6.7 CONFIRMED
со стороны caller'а, гипотеза `r1=0x58e8` из раздела 6.15 ОТКЛОНЕНА,
2026-09-08

Найдено параллельной сессией (прямая дизассемблирование `0x08000644`),
проверено здесь заново независимо, а не принято на веру.

**`0x08002dcc` — CONFIRMED, это флеш-адрес veneer-таблицы (линкерных
заглушек для длинных переходов flash→CCM RAM), а не отдельная функция:**

```
0x08002dcc: movw ip,#0x1b9 ; movt ip,#0x1000 ; bx ip  → 0x100001b8
0x08002dd6: movw ip,#0xbc1 ; movt ip,#0x1000 ; bx ip  → 0x10000bc0
0x08002de0: movw ip,#0xb5d ; movt ip,#0x1000 ; bx ip  → 0x10000b5c
```

Первая запись — это конкретный флеш-адрес того, что Ghidra именует
`thunk_EXT_FUN_100001b8` (раздел 6.7); вторая и третья — уже разобранные
ранее CCM-RAM функции `0x10000bc0`/`0x10000b5c` (пункт 15; статус
`0x10000b5c` на момент написания ЭТОГО раздела считался "мёртвый код",
но позже переоткрыт и уточнён в разделах 6.19/6.20 — именно эта
veneer-таблица `0x08002de0` и оказалась причиной, почему прежний поиск
вызывающего кода в разделе 4.3 ничего не находил). Противоречия с
разделами 4.4/6.7 НЕТ — это уточнение конкретного адреса, а не новый
факт о поведении.

**Вызов из ISR — CONFIRMED, обе ветки:**

```
TC-ветка (tst r0,#2):
  r0 = 0x20007840              ; CONFIRMED — тот же буфер, что раздел 6.16
  r4 = 0x200048a0 (литерал)    ; CONFIRMED
  r1 = *(u16*)(r4+2)           ; = *(u16*)0x200048a2 — RUNTIME-значение, НЕ литерал
  bl 0x08002dcc → 0x100001b8

HT-ветка (tst r0,#4), хвостовой переход:
  r2 = *(u16*)(0x200048a2)     ; то же значение, но в r2
  r1 = 0                       ; явно обнулён
  r0 = 0x20007840
  pop {r4,r5,r6,lr}; b.w 0x08002dcc → 0x100001b8
```

**CONFIRMED:** трёхрегистровая конвенция раздела 6.7 ("r1/r2 по-разному
ведут себя в ветках TC/HT") подтверждена теперь и со стороны caller'а
(раздел 6.7 видел это только изнутри thunk-функции) — в TC-ветке
значение кладётся в `r1` (r2 не трогается), в HT-ветке то же значение
кладётся в `r2`, а `r1` явно обнуляется. Независимое схождение двух
разных заходов на один и тот же факт.

**ОТКЛОНЕНО:** гипотеза раздела 6.15 "параметр `r1` при вызове из ISR
равен литералу `0x58e8`" — не подтвердилась. Реальный `r1`/`r2` — это
halfword, прочитанный в рантайме из ячейки `0x200048a2` (структура с
базой `0x200048a0`, сама база — CONFIRMED литерал, на 8 байт раньше уже
известного аккумулятора `0x200048a8` из разделов 6.7/6.15). Совпадение
арифметики `0x20000000+0x58e8=0x200058e8`, отмеченное в 6.15, по
всей видимости, случайное (или относилось к другой точке кода внутри
самого thunk, не к месту вызова) — понижено с PROBABLE до
неподтверждённого, дальше как гипотеза не используется.

**Открыто:** назначение структуры `0x200048a0` (что за поле по
смещению `+2` — вероятно, HAL DMA-хендл или счётчик пакетов, не сырой
адрес буфера); откуда конкретно в рантайме берётся значение `r1`/`r2`
и как оно связано (если связано) с адресами буферов DMA1/DMA2 из
раздела 6.14.

### 6.18 `FUN_08000f52` между двумя вызовами `FUN_0800dbf8` — снапшот,
усреднение, счётчик стабильности; сама формула `Rs`/`Rp`/`L`/`C` ЕЩЁ НЕ
НАЙДЕНА, 2026-09-08

Продолжение раздела 6.16/пункта 25. Честный промежуточный статус, а не
находка — три слоя кода прочитаны и идентифицированы, ни один не
оказался искомой записью в `0x20004a48`.

**Слой 1 — снапшот сырого буфера при стабильном отсчёте (CONFIRMED
адреса, PROBABLE назначение):**
```c
if ((uVar20 < 2 || puVar21[0xe]==0) && puVar21[7]==0) {
    // копирует все 14 float из 0x20007840 (pfVar4) в 0x20007c70 (DAT_08001634)
}
```
`DAT_08001634` → `0x20007c70` — CONFIRMED тот же адрес, что раздел 6.13
уже определил как второй буфер, передаваемый в `FUN_0800dbf8`. То есть
`0x20007c70` — не независимый "канал 2" источник, как предполагалось в
6.13, а **снятая копия `0x20007840`** в момент, когда показания
признаны стабильными (`puVar21[0xe]`/`puVar21[7]` — флаги условия).
Это уточняет (не отменяет) раздел 6.13.

**Слой 2 — 64-битное усреднение сумм (CONFIRMED, но это библиотечный
код, не прикладная логика):** `DAT_08001b48` (`0x20007c70`) и
`DAT_08001640` (`0x20007c00`, структура из самого начала функции,
раздел 6.11-я эпоха) складываются попарно (classic add-with-carry на
двух 32-битных словах = 64-битное сложение) по четырём парам смещений
(`+8/0xc`, `+0x10/0x14`, `+0x20/0x24`, `+0x28/0x2c` — те же четыре поля
Re/Im канал1/канал2, что раздел 6.12/6.16), затем делятся на счётчик
отсчётов через `FUN_0800243e`. **`FUN_0800243e` разобрана и
идентифицирована как стандартная знаковая 64-битная division
(`__aeabi_ldivmod`-подобная, вызывает `FUN_080023dc` — беззнаковое
деление через нормализацию, CONFIRMED по телу функции)** — то есть это
обычное усреднение целочисленных сумм, не формула физической величины.

**Слой 3 — счётчик стабильности (CONFIRMED адреса, PROBABLE роль):**
`puVar21[10]` инкрементируется при прохождении каскада сравнений
(`FUN_08002958` против порогов `DAT_08001b80/88/90`) с эскалацией
2→4→6→8; при достижении порога вызывается `FUN_08009574(0x13,8)` и
`FUN_0800ade8(1,&DAT_08001b98)`. **`FUN_0800ade8` разобрана и оказалась
НЕ расчётом** — это посимвольный вывод строки на экран (спецобработка
символа `.` как десятичной точки), то есть просто печатает какое-то
сообщение/строку по достижении стабильности. Тупиковая ветка для
поиска формулы.

**Итог: формула `Rs`/`Rp`/`L`/`C` НЕ НАЙДЕНА.** Прочитанный кусок
(строки декомпиляции ~1408-1690) — это подготовительный конвейер
(снапшот → усреднение → debounce), а не сама физика. Функция
`FUN_08000f52` продолжается минимум до строки ~2140 (вызов
`FUN_08009618()` в конце) — искомый код либо в оставшейся
непрочитанной части (после debounce-блока, до конца функции), либо
вызывается из одной из ещё не разобранных функций этого куска
(`FUN_08015eb0`, `FUN_08009574`, `DAT_0800f550`-ветка).

**Открыто:** дочитать `FUN_08000f52` до конца; либо, если формула не
найдётся текстовым чтением, признать, что для этого шага нужен другой
метод (реальная Ghidra с типами, а не построчное чтение decompiled
текста).

### 6.19 Продолжение чтения `FUN_08000f52` после debounce-блока (строки декомпиляции ~1671–1852) — честный тупик по формуле, но найдена контрадикция по `thunk_EXT_FUN_10000b5c`, 2026-09-08

Продолжение раздела 6.18/пункта 25. После блока debounce/счётчика
стабильности (раздел 6.18, слой 3) код идёт в разветвление:
`if (*(char *)(DAT_0800f550 + 5) == '\x01') { ... } else { ... }` —
два почти идентичных блока, каждый вызывает `FUN_0800fa48(N)` /
`FUN_0800fa82(M)` (PROBABLE: установка строки/столбца курсора экрана),
затем `FUN_08015eb0(0x3c, ...)` (проверка условия) и, если истинно,
`FUN_08015ff0(буфер, длина)` (запись строки в текстовый буфер экрана)
плюс `thunk_EXT_FUN_10000b5c()`. Повторяется 8 раз (по числу строк
экрана), суммарно **`thunk_EXT_FUN_10000b5c()` вызывается 20+ раз** в
этом единственном блоке.

**Проверено и ОТКЛОНЕНО (гипотеза, не найдено):** параметр
`FUN_0800ade8(1,&DAT_08001b98)` (вызывается непосредственно перед этим
блоком) резолвится напрямую по `.hex` в **ASCII-строку `"bad"`**
(`0x08001b98` → `0x646162` = байты `62 61 64` = `b`,`a`,`d`) — то есть
это статусное сообщение (вероятно "bad range"/ошибка диапазона), не
печать числового результата. Гипотеза "здесь форматируется и печатается
Rs/Rp/L/C" — не подтвердилась.

Буфер `DAT_0800ae74` резолвится в `0x20007d24` — обычный RAM-буфер
текста экрана (8 строк × `0x80` байт), не структура результата
(`0x20004a48`/`0x20005610`). **Итог: весь прочитанный участок (строки
~1671–1852) — это вывод статусного сообщения на экран, тупик для поиска
формулы Rs/Rp/L/C**, как и честно предполагал раздел 6.18. Оба варианта
if/else заканчиваются `return` — то есть это последний код в данной
ветке `FUN_08000f52` (ветка "диапазон не подошёл", по которой шла
навигация в начале этой сессии).

**НАЙДЕНА КОНТРАДИКЦИЯ (требует исправления, не исправлено в этом
коммите):** пункт 15 раздела 13 утверждает, что `thunk_EXT_FUN_10000b5c`
(`0x10000B5C`, CCM RAM) — "мёртвый код, нигде не вызывается (проверено
по всему образу + по всей CCM RAM), функция битбэнг-сброса I2C на
PB6/PB7, физически не задействованные пины". Это **прямо противоречит**
находке этой сессии: функция вызывается **минимум 20 раз** именно в этом
блоке `FUN_08000f52`. Возможные объяснения (не проверено): (а) прежний
"поиск по всему образу" искал литеральный адрес `0x10000B5C` только как
данные (указатель), а не как цель `bl`/`b.w` перехода — тогда вызовы
через `thunk_EXT_FUN_...` (Ghidra-имя для трамплина) могли быть
пропущены таким поиском; (б) сама атрибуция функции ("I2C bit-bang
PB6/PB7") может быть неверной — судя по контексту вызова (сразу после
записи каждой строки текста экрана), более вероятная гипотеза —
это **строб/защёлка (latch) вывода строки на дисплей**, возможно через
бит-банг `CS`/`DC` (`PB15`/`PC13`), а не через I2C. **Требуется
переразбор `0x10000B5C` заново** — атрибуция "I2C, мёртвый код" из
пункта 15/раздела 4.3 должна считаться под вопросом до перепроверки.

**Открыто:** переразобрать `0x10000B5C`; формула Rs/Rp/L/C всё ещё не
найдена — не прочитана ветка "показания стабильны" (без reset
диспетчера, начинается сразу после этого блока, за пределами прочитанного
диапазона строк).

### 6.20 Переразбор `thunk_EXT_FUN_10000b5c` (`0x10000B5C`, CCM RAM) — контрадикция пункта 15 РАЗРЕШЕНА, 2026-09-09

Прямое дизассемблирование (Capstone, Thumb) флеш-зеркала CCM RAM
(`src=0x0802D7DC, dst=0x10000000`, раздел 4.3) диапазона
`0x10000B5C..0x10000BB9`:

```
push   {r4, lr}
ldr    r4, [pc, #0x5c]        ; r4 = 0x48000400 (GPIOB, CONFIRMED прямым чтением .hex)
movs   r2, #0  ; movs r1, #0x40 ; mov r0, r4 ; bl 0x10000c02   ; PB6 = 0
movs   r1, #0  ; [busy-wait ~256 итераций]                     ; delay
movs   r2, #0  ; movs r1, #0x80 ; mov r0, r4 ; bl 0x10000c02   ; PB7 = 0
                 [busy-wait]
movs   r2, #1  ; movs r1, #0x40 ; mov r0, r4 ; bl 0x10000c02   ; PB6 = 1
                 [busy-wait]
movs   r2, #1  ; movs r1, #0x80 ; mov r0, r4 ; bl 0x10000c02   ; PB7 = 1
                 [busy-wait]
pop    {r4, pc}                ; конец функции, 0x10000BB8
```
`bl 0x10000c02` — тонкий вениир (`movw ip,#0x5f07; movt ip,#0x800; bx ip`)
на `0x08005f06` — уже известная `FUN_08005f06` (GPIO bit set/reset).
Функция кончается на `0x10000BB8`; сразу за ней с `0x10000BC0` начинается
уже известный `thunk_EXT_FUN_10000bc0` (драйвер пищалки, раздел 4.3;
подтверждён видом `ldrheq r2,[r1,#2]; cmpeq r2,#0; bxne lr` на новом
дизасме). **Итог: байткод и атрибуция портов (GPIOB, PB6 push-pull /
PB7 open-drain) — полностью подтверждают прежнее описание раздела 4.3,
инструкция в инструкцию.** Изменилась не эта часть.

**Разрешена контрадикция пункта 15:** утверждение "функция нигде не
вызывается" — ОПРОВЕРГНУТО. Раздел 6.19 нашёл прямым grep'ом по
декомпиляции `FUN_08000f52` **20+ вызовов** `thunk_EXT_FUN_10000b5c()`,
каждый сразу после `FUN_08015ff0` (запись строки в буфер экрана) в
статусной ветке "диапазон не подошёл" (строки ~1791-1852). Причина
пропуска в прежнем поиске (раздел 4.3): способ 1 искал 4-байтный
литерал-указатель `0x10000B5C` как ДАННЫЕ, а реальные вызовы идут через
двухинструкционный вениир `0x08002de0` (`movw ip,#0xb5d; movt
ip,#0x1000; bx ip`), не содержащий адрес как единое 32-битное слово в
памяти — такой поиск его не находит. Способ 2 (проход по `bl`/`b.w`
внутри самой CCM RAM) тоже не находил, потому что реальные caller'ы — во
ФЛЕШЕ (`FUN_08000f52`), а поиск был ограничен CCM RAM.

**Электрическая часть вывода MussonOld остаётся в силе и не
противоречит новому:** `PB6`/`PB7` физически не разведены на этой
ревизии платы (`GPIO_MAP.md`, подтверждено MussonOld). Код
**вызывается** регулярно (20+ раз при каждой отрисовке статусной
строки), но переключение этих GPIO физически ни на что не влияет — нет
ни I2C-периферии, ни другой нагрузки на линиях. **Правильная
формулировка: не "мёртвый код" (dead code, никогда не выполняется), а
"электрически инертный код" (выполняется регулярно, но без физического
эффекта)** — вероятно унаследованный boilerplate от варианта платы, где
на PB6/PB7 сидела реальная I2C-периферия (например, тачскрин-контроллер
или EEPROM), не популированная на данной ревизии.

**Атрибуция "I2C" vs "строб/защёлка дисплея" (альтернатива раздела
6.19):** open-drain конфигурация `PB7` нетипична для CS/DC-строба (те
обычно push-pull) и типична именно для I2C-линии — гипотеза "строб
дисплея" ОТКЛОНЕНА, атрибуция "I2C-подобный битбэнг" остаётся PROBABLE
(по конфигурации пинов и do-nothing-паттерну, не по прямому
доказательству протокола на реальной шине, которой физически нет).

**Статус: CONFIRMED** (байткод, GPIO-порт, 20+ вызовов из
`FUN_08000f52`, физическое отсутствие эффекта на этой ревизии платы).

### 6.21 Ветка "показания стабильны" (`FUN_08000f52`, строки ~1856-2224) прочитана целиком — запись Rs/Rp/L/C здесь НЕ найдена, гипотеза раздела 14 ОТКЛОНЕНА, 2026-09-09

Прочитана целиком ветка, следующая сразу за `return` на строке 1853 —
та самая, на которую раздел 14 (Handoff) указывал как на вероятное
место записи в `0x20004a48`. Все `DAT_`-литералы диапазона (81 штука по
всей функции, из них ~30 в этом диапазоне) резолвлены напрямую по
`.hex`, не по имени символа.

**Содержимое ветки (CONFIRMED по прочтению):**
- накопление кольцевого лог-буфера истории измерений (`0x20007b1c`,
  пары double через `FUN_080029b8`, строки 1878-1884);
- конечный автомат автовыбора диапазона: пороги `999/9999/49999/99999`
  (`DAT_08001fc8/fcc/...`) сравниваются с константами масштаба
  `1e2..1e8` (резолвятся в `0x08001fd0..0x08001fe0` как raw double-биты);
- управление реле диапазона через уже известную `FUN_0800ae7c(0/1,
  range)` (SPI-команда `0x2a01`, строки 2153-2164);
- финальная проверка флага готовности `*DAT_08002004` (=`*0x20004a48`,
  offset 0, **ТОЛЬКО ЧТЕНИЕ**, строка 2218) и условный вызов
  `FUN_08009618()` (отрисовка экрана).

**Контрадикция с разделом 14:** прямая запись double-полей в
`0x20004a48` в диапазоне строк 1856-2224 ОТСУТСТВУЕТ. Единственные два
обращения к базовому адресу структуры (`DAT_08001178`/`DAT_08002004`,
оба резолвятся в `0x20004a48`) — оба ЧТЕНИЕ байта offset 0 (флаг
готовности, строки 1256 и 2218), не запись. Формулировка раздела 14
("ветка должна вести к формуле Rs/Rp/L/C") **не подтвердилась** —
ветка полностью объясняется автовыбором диапазона и триггером
перерисовки, без вычисления результата.

**Попутно найдена карта дополнительных полей структуры `0x20004a48`
(новые смещения, резолв по `.hex`, не из раздела 6.11):**
| offset | адрес | доступ | что вероятно |
|---|---|---|---|
| +0x00 | `0x20004a48` | R (строки 1256, 2218) | флаг готовности (известно, 6.11) |
| +0x03 | `0x20004a4b` | W 0/1 (строки 1589,1592,1868 — 3 разных `DAT_` на один адрес) | статус-байт |
| +0x04 | `0x20004a4c` | R (строка 1599) | статус-байт |
| +0x09 | `0x20004a51` | W→0 (строки 1979, 1997) | статус-байт |
| +0x18 | `0x20004a60` | R, double, сравнение с ±10.0 (строка 1595) | PROBABLE: отклонение/дрейф, % |
| +0x20 | `0x20004a68` | R, double (строки 1879, 2091) | уже готовое значение |
| +0x28 | `0x20004a70` | R, double (строка 1878) | уже готовое значение |

Ни одно из этих полей не выглядит как ЗАПИСЬ итогового Rs/Rp/L/C — все
либо флаги состояния, либо уже готовые значения, читаемые для
отображения/сравнения с порогами.

**Новый кандидат на место вычисления (PROBABLE, не досмотрено до
конца):** ДО обеих прочитанных веток (строки ~1636-1653) в структуру
`puVar21+0x68` (т.е. `0x20005610+0x68 = 0x20005678` — рядом с уже
известными Re/Im `0x20005668`/`0x20005670`, раздел 6.16) пишется
8-байтное значение через `FUN_08012f18` (PROBABLE: hypot/√(a²+b²), два
double-аргумента) → `FUN_0800277c` (не идентифицирована, PROBABLE:
деление). Похоже на модуль `|Z|` или производную величину, но куда это
значение уходит ДАЛЬШЕ — не прослежено, UNKNOWN.

**Вывод:** формула/место записи Rs/Rp/L/C всё ещё не найдено. Раздел
14/приоритет 1 остаётся ОТКРЫТЫМ, но с исправленной областью поиска —
ветка 1856-2224 закрыта ОТРИЦАТЕЛЬНО. Следующий шаг — один из:
(а) непрочитанный участок строк ~1200-1580 (от начала функции до
первого `FUN_0800dbf8`); (б) проследить `0x20005678` дальше по коду —
куда копируется после строки 1653; (в) функция, вызываемая из
диспетчера стадий `FUN_08000618` после stage 6 (гипотеза 6.16, не
проверена).

### 6.22 `FUN_08000f52` дочитана ПОЛНОСТЬЮ (строки 1123-2223, весь остаток) — записи Rs/Rp/L/C НЕТ нигде в функции; переосмыслены целевые смещения структуры `0x20004a48` по находке в `FUN_08009618`, 2026-09-09

Продолжение разделов 6.18-6.21. Дочитаны все ранее непрочитанные
промежутки `FUN_08000f52`: начало функции (1123-1256, инициализация
при первом вызове + гейт `*DAT_08001178`), участок между первым и
вторым `FUN_0800dbf8` (1420-1616 — это и есть "слои 1-3" раздела 6.18,
подтверждено построчно ещё раз), и участок 1616-1671 (второй вызов
`|Z|`-формулы через `FUN_08012f18`, ведущий прямо в уже прочитанную
ветку 6.19). **Итог: вся функция `FUN_08000f52` (0x08000f52,
~1100 строк декомпиляции) прочитана от первой до последней строки.
Прямой записи double/float-полей в `0x20004a48` НЕТ НИГДЕ в этой
функции.** Единственное касание структуры — уже известное чтение/сброс
байта готовности по смещению 0 (`DAT_08001178`/`DAT_08002004`).
Гипотеза "запись где-то в непрочитанном хвосте `FUN_08000f52`"
(раздел 14, пункт (а)) — **ЗАКРЫТА ОТРИЦАТЕЛЬНО, окончательно.**

**Ключевая новая находка — переосмысление структуры `0x20004a48`
(раздел 6.11 уточняется, не отменяется):** при попутной проверке
`FUN_08009618` (функция отрисовки, раздел 6.11) обнаружен ранее
непрочитанный блок кода (строки декомпиляции ~11150-11330,
`pbVar2 = DAT_08009a18`, тот же базовый адрес `0x20004a48` — один из
8 литералов раздела 6.11) — это **фильтр сглаживания/анимации
отображаемых цифр**, а не константный блок памяти:

```c
// пример для пары Rs (аналогично для Rp/L/C):
*(undefined8 *)(pbVar2 + 0x50) =  // "текущее отображаемое" значение Rs
    weighted_blend(*(undefined8*)(pbVar2+0x50),   // старое отображаемое
                    *(undefined8*)(pbVar2+0x30),   // ЦЕЛЕВОЕ (реальное) значение Rs
                    weight(pbVar2[0xc]));          // счётчик шага анимации, 0..0x13 (20 шагов)
```

Идентичная структура повторена для четырёх пар:
- `+0x50` (отображается) ⇐ цель `+0x30`
- `+0x60` (отображается) ⇐ цель `+0x40`
- `+0x80` (отображается) ⇐ цель `+0x90`
- `+0x88` (отображается) ⇐ цель `+0x98`

Арифметика — классический lerp через `FUN_08002698`(умножение)/
`FUN_0800254a`(сложение) с весами `FUN_08002886(0x14-N)`/
`FUN_080028a8(N)`, где `N = pbVar2[0xc]` — счётчик шага (инкрементируется
до `0x13`=19 в другом месте той же функции). Есть и "мгновенный" путь
(без анимации, копирование за один шаг) в начале того же блока (строки
~11150-11165), выполняемый когда режим/страница только что переключены.

**ВЫВОД (меняет постановку задачи):** смещения `+0x50/+0x60/+0x80/+0x88`
— это **не** место записи итогового измерения, а только "витрина"
для дисплея (то, что реально печатается функциями `s_Rs__.../s_Rp__...
/s_L__.../s_C__...` раздела 6.11 — отсюда и путаница, строки-подписи
стоят рядом с ЧТЕНИЕМ этих полей для печати, а не рядом с их записью).
**Настоящая цель поиска — где пишутся `+0x30` (Rs), `+0x40` (Rp),
`+0x90` (L), `+0x98` (C).** Раздел 6.11 подлежит уточнению (не
отменяется — Rs/Rp/L/C как физические величины и офсеты области
подтверждены строками, но конкретные "живые" офсеты для записи —
`+0x30/+0x40/+0x90/+0x98`, а не `+0x50/+0x60/+0x80/+0x88`).

**Проверено и НЕ подтвердилось искать запись через `FUN_0800dbf8`
напрямую в `0x20004a48`:** повторно прочитана целиком (раздел 6.12
корректен) — пишет только в отдельную промежуточную ячейку
`0x20004914`(флаг)/`0x20004920`(double-значение) и флаг
`0x20004a4d`(=`0x20004a48+5`), НЕ в `+0x30/+0x40/+0x90/+0x98` напрямую.
Значит между `FUN_0800dbf8` и структурой отображения есть ещё одна
непрочитанная функция-посредник, которая: (1) читает флаг `0x20004a4d`
и значение `0x20004920`, (2) определяет, к какой из четырёх величин
(Rs/Rp/L/C) оно относится (текущий режим измерения), (3) пишет в
соответствующий `+0x30/+0x40/+0x90/+0x98`.

**Кандидат на местонахождение (не проверено, UNKNOWN):** сырой
4-байтовый поиск по всему образу подтвердил ровно 8 мест со значением-
литералом `0x20004a48` (список раздела 6.11 полный, дополнительных не
найдено — `0x08017cc4` из старого списка сомнителен: строка "17cc4" не
встречается в декомпиляции ни разу, возможна случайная битовая
последовательность не в коде, а в данных — статус этого одного адреса
понижен до НЕПОДТВЕРЖДЁН). Ни в одном из 8 адресов не найдено прямой
записи в `+0x30/+0x40/+0x90/+0x98` — значит функция-посредник либо (а)
использует один из уже известных 8 базовых адресов, но в ещё не
прочитанном месте той же вызывающей функции (кандидаты: неполностью
прочитанные части `FUN_08009618`, которая занимает большой диапазон
адресов и явно больше того, что уже разобрано в разделе 6.11/6.22),
либо (б) держит указатель на структуру в регистре/переменной без
собственного литерала (маловероятно для функции, вызываемой не из
самого начала — обычно требует загрузки литерала).

**Следующий шаг:** дочитать `FUN_08009618` (0x08009618) целиком — она
явно больше, чем показано в разделе 6.11 (там разобран только блок
показа Rs/Rp/L/C на первом "экране"; в разделе 6.22 найден ещё блок
сглаживания и упомянут второй "детальный" экран D/Q/L/C/Ri в районе
`DAT_08009f34`, тоже read-only в прочитанной части) — искать запись в
`+0x30/+0x40/+0x90/+0x98` относительно любого из известных базовых
литералов (`DAT_08009a18`, `DAT_08009f34`, `DAT_0800ade4` и др.).
Также проверить функции-кластер в районе `0x0800d900-0x0800dc00`
(обнаружены по литерал-пуловым ссылкам на смещения структуры `+4`,
`+0x18`, `+0x20`, `+0x28` — адреса `0x0800d994`, `0x0800d998`,
`0x0800d9a0`, `0x0800d9a4`, `0x0800d9b8`, `0x0800d9c4`, `0x0800d9fc` —
не идентифицировано, к какой функции относятся, не прочитаны).

## 9. Командный интерфейс

Известные командные строки:
- `get_buff`
- `get_dpf`
- `get_sin`
- `get_dac`
- `get_window`
- `setCount`
- `setRUI`
- `setDACperiod`
- `setDACfreq`
- `setDACTest`
- `setLCD`
- `POWER_OFF`
- `isSerial`
- `DAC_ZERO`
- `autofreq`

`setRUI` передаёт три параметра в `FUN_08000ec0()` — адрес **проверен и
не разрешается** (такой функции не существует в текущем дампе) —
открытая задача, найти актуальный адрес.

Команды, связанные с DAC, манипулируют параметрами DAC.

Структура протокола/парсера: неполная.

## 10. Важные функции (актуализировано 2026-09-06)

Таблица собрана из подтверждённых находок, разбросанных по разделам
2/4/6/7/9 этого документа.

| Адрес | Функция | Роль | Уверенность |
|---|---|---|---|
| `0x080023B8` | `Reset_Handler` | точка входа при сбросе (раздел 2) | CONFIRMED |
| `0x0800FF68`, `0x08002D48`, `0x08016274` | цепочка старта (раздел 2) | init тактирования/FPU/watchdog → init рантайма C → `main()` | CONFIRMED |
| `0x0800019C` | `FUN_0800019c` | тело обработчика SysTick (хвостовой `b.w` из `SysTick_Handler`, раздел 4.2) | CONFIRMED |
| `0x08005F06` | `FUN_08005f06(port, mask, val)` | запись бита GPIO: `val!=0` → `BSRR` (0x18), `val==0` → `BRR` (0x28) | CONFIRMED |
| `0x08005EFC` | `FUN_08005efc(base, mask)` | чтение флага по смещению `0x10` от базы (GPIO `IDR` либо TIM `SR`, контекстно) | CONFIRMED |
| `0x08005148` | `FUN_08005148(handle)` | `HAL_ADC_GetValue`-подобный геттер (`ADC_DR`, смещение `0x40`) | CONFIRMED |
| `0x08007944` | `FUN_08007944(handle)` | общий диспетчер RCC-enable + NVIC (TIM1/2/3/4/7/16 — раздел 6.4) | CONFIRMED |
| `0x0800606C` | `FUN_0800606c(irq, prio)` | `NVIC_SetPriority` | CONFIRMED |
| `0x0800604E` | `FUN_0800604e(irq)` | `NVIC_EnableIRQ` | CONFIRMED |
| `0x08003428` | `FUN_08003428()` | полная самодостаточная инициализация TIM6 (RCC/PSC=99/ARR/DIER/NVIC IRQ54 prio0) — раздел 6.4 | CONFIRMED |
| `0x0800FF78` | `FUN_0800ff78()` | ISR TIM7, IRQ55 — таймаут автовыключения + долгое удержание кнопок + блок C (раздел 6.3) | CONFIRMED |
| `0x0800FCF4` | `FUN_0800fcf4(ms, save)` | сеттер порога автовыключения (тот же `pcVar1`, что и в ISR TIM7 — раздел 6.5) | CONFIRMED |
| `0x080154C8` | `FUN_080154c8(ms)` | программная задержка на миллисекундах (busy-wait, калибровка по частоте шины) | CONFIRMED |
| `0x10000B28` | `EXTI15_10` (тело в CCM RAM) | обрабатывает только EXTI14 (`PC14`=INT2 BMI160), инкрементирует счётчик (раздел 4.3) | CONFIRMED |
| `0x10000BC0` | `thunk_EXT_FUN_10000bc0(ms)` (CCM RAM) | неблокирующий драйвер пищалки `PA8`/`ZP` (раздел 4.3) | CONFIRMED |
| `0x10000B5C` | безымянная функция (CCM RAM) | GPIOB `PB6`/`PB7` push-pull/open-drain toggle — НЕ мёртвый код (20+ вызовов из `FUN_08000f52` через вениир `0x08002de0`, раздел 6.19), но физически инертен (PB6/PB7 NC на плате); атрибуция "I2C-битбэнг" — PROBABLE (раздел 4.3/6.20) | байткод CONFIRMED, атрибуция PROBABLE |
| `0x08005CAC` | `FUN_08005cac()` | низкоуровневая настройка GPIO/EXTI (раздел 6) | **не подтверждено для 2.0.12** — открытая задача |
| `0x080045C4` | `FUN_080045c4()` | доступ к параметру конфигурации (раздел 8) | **не подтверждено для 2.0.12** — открытая задача |
| `0x08008384`, `0x080101B4` | `FUN_08008384()`, `FUN_080101b4()` | код, согласующийся с группой TIM15/16/17 (раздел 7) | PROBABLE — атрибуция понижена с "high" 2026-09-06, прямых доказательств для TIM15/17 не найдено |
| `0x08016F44` и аналогичные `DAT_0x8016fXX` | точки `handle->Instance = TIMx` в `main()` | инициализация хэндлов TIM1/2/3/16 (раздел 6.4) | CONFIRMED |

## 11. Пока не установлено

- ~~полная таблица векторов~~ ЗАКРЫТО — см. раздел 4.1;
- ~~точное сопоставление IRQ -> обработчик~~ ЗАКРЫТО для реализованных
  векторов — см. раздел 4.1 (некоторые записи таблицы остаются "не
  реализован"/заглушка по построению, это не открытый вопрос);
- точное сопоставление таймер -> IRQ для TIM15/TIM17 (TIM16 и TIM7
  закрыты — см. раздел 7 и 6.4);
- ~~обработчик SysTick~~ ЗАКРЫТО — см. раздел 4.2;
- идентификация и конфигурация ADC/DAC как подсистемы в целом остаётся
  открытой (в частности: ADC1..ADC3, генерация DAC, полная временная
  диаграмма измерения). ~~Канал блока C (хэндл `0x20006968`)~~ ЗАКРЫТО
  2026-09-07 — CONFIRMED `ADC4`, `Channel=4` -> `ADC4_IN4`/`PB14`, см.
  раздел 6.6 (не путать с общей задачей ADC/DAC выше — закрыт только
  этот конкретный канал);
- DMA;
- сопоставление USART/SPI/I2C;
- полные функции выводов GPIO (закрыто файлом `GPIO_MAP.md`, кроме
  отдельных открытых вопросов там же);
- структуры глобальных данных;
- временная диаграмма измерения;
- генерация формы сигнала DAC;
- алгоритм частоты/autofreq;
- расчёт R/L/C;
- полный протокол последовательного порта;
- реализация постоянного хранения;
- итоговый `.ioc` для CubeIDE.

## 12. Дорожная карта

1. Карта железа — частично готова (см. также `GPIO_MAP.md`).
2. ~~Карта векторов / IRQ / ISR~~ — **завершено** (согласуется с шапкой
   документа); сейчас в работе детальный разбор сверх этого этапа (см.
   "Текущий этап" вверху документа).
3. Глобальные структуры.
4. Измерение и алгоритм RLC — начато (найден вероятный DSP-код
   измерения в CCM RAM, раздел 4.4, пункт 21 раздела 13 — не разобран
   до конца).
5. Карта GPIO/выводов — в основном готова, см. `GPIO_MAP.md`.
6. Реконструкция `.ioc` для CubeIDE.
7. Реализация старта/main/ISR.
8. Периферийные/прикладные модули.
9. Сборка и функциональная проверка.

## 13. Ближайшие конкретные действия

1. Восстановить реальное представление таблицы векторов/старта. — СДЕЛАНО 2026-09-04
2. Сопоставить каждую запись вектора с адресом. — СДЕЛАНО для IRQ 11/13/14/15/20/37/40/54-61 (см. 4.1)
3. Сопоставить адреса обработчиков с функциями Ghidra. — продолжено 2026-09-06: DMA1_CH3/4/5, DMA2_CH1/3 сведены к общему `HAL_DMA_IRQHandler`-подобному диспетчеру `FUN_080059ba`; `DMA1_CH1` (IRQ11) разобран отдельно как уникальный обработчик, тайл-коллящий крупную DSP-функцию в CCM RAM (см. раздел 4.4). Остаётся: callback'и пяти DMA-хэндлов не прослежены, `USART1` (`0x08010B5C`) не разобран.
4. ~~Разрешить IRQ 11 и доказать/опровергнуть сопоставление `FUN_0800019c`.~~ ЗАКРЫТО (стале запись, не обновлённая ранее): вопрос про `FUN_0800019c` разрешён ещё в разделе 4.2 (это тело SysTick, не связано с IRQ11). Сам IRQ11 (`DMA1_CH1`) отдельно разобран 2026-09-06 — см. раздел 4.4.
5. ~~Разрешить IRQ 20 / ISR USB.~~ РАЗРЕШЕНО 2026-09-06: трамплин `0x080126E4` -> `FUN_08006328`, битовые маски совпадают с `USB_ISTR` (см. раздел 5)
6. ~~Разрешить IRQ 54 и его периферию.~~ РАЗРЕШЕНО 2026-09-04: IRQ54 = TIM6_DACUNDER (см. 4.1)
7. ~~Разрешить IRQ 37, 40, 55.~~ РАЗРЕШЕНО 2026-09-04 (IRQ37=USART1, IRQ40=EXTI15_10, IRQ55=TIM7 — см. 4.1)
8. ~~Разрешить обработчик SysTick.~~ РАЗРЕШЕНО 2026-09-04 (см. раздел 4.2) — ранее не отмечено в этом списке, исправлено 2026-09-06
9. Извлечь значения тактирования/PSC/ARR таймеров и вывести периоды.
10. Обновлять этот документ доказательствами, а не предположениями.
11. ~~Разобрать обработчик TIM7 (`0x0800FF78`) целиком~~ СДЕЛАНО 2026-09-06 — см. раздел 6.3. Гипотеза "две стадии автовыключения" уточнена: TIM7 = сторожевой таймаут бездействия + скан кнопок/джойстика, а не отдельная "вторая стадия".
12. По желанию — разобрать дочерние обработчики USB-событий `FUN_0800e754` (CTR/endpoint), `FUN_0800659c`/`FUN_08012728` (RESET/enumeration) для второго уровня детализации (не обязательно для завершения этапа 2).
13. Новые открытые вопросы из раздела 6.3: ~~идентифицировать сигнал `PA1`~~ РАЗРЕШЕНО 2026-09-06 — `PA1` = `JOY_OK` (см. `GPIO_MAP.md`); блок C — `FUN_08005148` подтверждён как чтение `ADC_DR`, хэндл идентифицирован как ADC4, канал = 4 (`PB14`) — РАЗРЕШЕНО 2026-09-07, см. пункт 14/22, раздел 6.6; ~~разобрать `thunk_EXT_FUN_10000bc0`~~ РАЗРЕШЕНО 2026-09-06 — подтверждён как неблокирующий драйвер пищалки `PA8`/`ZP` (см. раздел 4.3).
14. ~~Новое (2026-09-06): разметить базовые адреса `ADC1..ADC4`... затем повторно проследить инициализацию ADC-хэндла `0x20006968`~~ РАЗРЕШЕНО 2026-09-07: хэндл `0x20006968` указывает на **ADC4** (`Instance = 0x50000500`), инициализация найдена в `FUN_08016274` (`main`). Канал = 4 (`PB14`) — см. пункт 22 (теперь тоже закрыт).
18. Новое (2026-09-06): проверить физическое соответствие `ADC4_IN1` конкретному выводу корпуса `LQFP48` по даташиту `STM32F303CC` — раздел 6.6. ВЫПОЛНЕНО 2026-09-06: даташит (`DS9118`, таблица 13) сверен напрямую — `ADC4_IN1` выведен на `PE14`, которого физически нет на `LQFP48`. Результат отрицательный — см. пункт 22.
22. ~~Новое (2026-09-06): разрешить противоречие раздела 6.6~~ РАЗРЕШЕНО 2026-09-07 — `FUN_08004c1c` пересмотрен и оказался НЕ конфигуратором канала (вероятно, настройка аналогового watchdog); реальный конфигуратор — ранее не разобранный `FUN_08004d94`, пишущий напрямую в `SQR1` (`Instance+0x30`) по формуле `channel << (rank*6)`, что дало `Channel=4, Rank=1`. По датащиту (`DS9118`, Table 13) `ADC4_IN4 = PB14` — совпадает с уже установленным по схеме назначением `PB14` (`JOY_A/JOY_D/BAT`, `GPIO_MAP.md`). См. раздел 6.6.
19. ~~Новое (2026-09-06): разобрать `FUN_08017dc8` целиком~~ СДЕЛАНО 2026-09-06 — CONFIRMED по структуре: читает ADC4, ведёт скользящий мин/макс, калибровочная fixed-point формула, порог `DAT_08017f68`, персистентность 100 вызовов подряд → `FUN_0800ee00(1)`. PROBABLE: защита по низкому напряжению батареи. См. раздел 6.6.
20. Новое (2026-09-06, уточнено 2026-09-08 дважды): декодировать калибровочные константы `FUN_08017dc8` (раздел 6.6) до физических единиц. **ЗАКРЫТО как "разбор библиотечных функций" (раздел 6.9)** — `FUN_08002932`/`FUN_08002698`/`FUN_08002900`/`FUN_080029b8`/`FUN_08002be8`/`0x080133d0` полностью идентифицированы как стандартная EABI soft-float библиотека double-точности (`__aeabi_i2d`/`dmul`/`d2iz`/`d2f`/`trunc`/"дробная часть"), не кастомная логика. **Остаётся открытым** (новая формулировка): найти double-литералы (константы) в литерал-пулах `FUN_08017dc8` и новой функции `~0x08017f70` (раздел 6.8), сопоставить с физическими величинами (опорное напряжение АЦП, масштаб R/L/C). Разобрать оставшиеся 3 неразобранных вызывающих `FUN_0800ee00`: `0x08000270`, `0x0800afd4`, `0x080187f0`.
21. **ПРИОРИТЕТ (продолжено 2026-09-08, граница исправлена и закрыта, см. раздел 6.7):** разобрать крупную DSP-функцию в CCM RAM `0x100001b8` (тайл-вызов из `DMA1_CH1`/IRQ11, раздел 4.4) — 42+ инструкций `SMLAD`, похоже на синхронное детектирование/корреляцию с опорной синус/косинус-таблицей. Граница функции ИСПРАВЛЕНА на `0x100001b8..0x10000b27` (была неполной — `0x100008da..` не отдельная функция, см. пункт 23) и calling convention (`r0`,`r1`,`r2`) — CONFIRMED, раздел 6.7. Остаётся UNKNOWN: где пишется/сбрасывается `0x200049e8` (теперь установлено, что это стадийный счётчик, а не бинарный флаг — см. пункт 24) и опорные таблицы `sb`/`r8`; единицы измерения аккумуляторов `0x200048a8`/`0x200048ac`.
23. ~~Разобрать функцию, начинающуюся сразу после предыдущей, по адресу `0x100008da` в CCM RAM~~ ЗАКРЫТО ОТРИЦАТЕЛЬНО 2026-09-08: полная повторная трассировка (см. раздел 6.7) показала, что `0x100008da` достигается условным переходом (`bhs`) ИЗНУТРИ тела `thunk_EXT_FUN_100001b8`, до его `pop` — то есть это не отдельная функция, а продолжение той же функции. Прежняя граница `0x100001b8..0x100008d9` была неполной; исправленная граница — `0x100001b8..0x10000b27` (0x996C байт, три точки выхода). Находка подсказана параллельным чатом (наблюдение про `bhs`-переход изнутри тела), перепроверена здесь заново прямым дизассемблированием, а не принята на веру.
24. Новое (2026-09-08, уточнено дважды в этой же сессии): установить, где и когда пишется/инкрементируется значение по адресу `0x200049e8`. Прежняя формулировка ("переключатель варианта цикла, сравнивается с 1") сохраняется как относящаяся к более ранней части функции (раздел 6.7), но то же слово используется ПОВТОРНО в блоке диспетчера стадий (`0x10000ae4`, сравнение каскадом с `6/8/0x18/0x20/0x30/0x40`) — вероятно многозначный счётчик стадии. **Второе уточнение (раздел 6.8):** прямой байтовый поиск литерала `0x200049e8` по всему образу флеша нашёл только 3 места — все ЧТЕНИЕ (2 уже известных в CCM RAM + 1 новое в `~0x08017f70`, см. раздел 6.8), ни одной записи. Вывод: запись, вероятно, идёт через смещение от соседнего базового указателя (кандидат — `0x200049e4`, база из той же новой функции), а не через прямой литерал этого конкретного адреса — сырой поиск константы такое не находит. Следующий шаг — не гадать дальше, а искать `str`/`strb` относительно базовых указателей `0x200049e0..0x200049f8` (диапазон соседних полей) по всему образу, и/или разобрать ISR `DMA1_CH1` (сам обработчик прерывания, а не тайл-функцию раздела 6.7/6.8) на предмет инкремента счётчика при каждом новом DMA-блоке.
15. ~~Переразобрать `0x10000B5C`~~ РАЗРЕШЕНО 2026-09-09 (см. раздел 6.20): байткод подтверждён инструкция в инструкцию (GPIOB PB6/PB7 push-pull/open-drain toggle); "нигде не вызывается" — ОПРОВЕРГНУТО, 20+ реальных вызовов из `FUN_08000f52` найдены. Гипотеза "строб/защёлка дисплея" — ОТКЛОНЕНА (open-drain нетипичен для CS/DC), атрибуция "I2C-подобный битбэнг" остаётся PROBABLE. Правильная формулировка: код электрически инертен (PB6/PB7 физически NC на этой плате, подтверждено MussonOld), но НЕ dead code в смысле "никогда не выполняется" — выполняется регулярно, просто без физического эффекта.
16. ~~Найти PSC/ARR/DIER именно для TIM7~~ ОБОЙДЕНО 2026-09-06 — точные регистровые значения статическим поиском не найдены (см. раздел 6.4), но реальный период (1 секунда) установлен независимо, через сопоставление порогов автовыключения со строками меню "Timer 2/5/10 min" (см. раздел 6.5). Числовые PSC/ARR остаются опциональной задачей для полноты (чтение в отладчике), но не блокируют дальнейшую работу.
17. Новое (2026-09-06): выяснить точный смысл флагов, выставляемых блоком B TIM7 для `PA2`/`PB12` (см. раздел 6.3, "Уточнение") — вероятно, аналоги долгого удержания для этих кнопок, не основной ввод (основной ввод для навигации по меню подтверждён отдельно, напрямую в `main()`, независимо от TIM7).
25. **Обновлено 2026-09-08 (раздел 6.16) — Re/Im ЗАКРЫТО, Rs/Rp/L/C остаётся приоритетом.** Место записи `Re`/`Im` (`0x20005668`/`0x20005670`) найдено: внутри `FUN_08000f52` (стадия 6 диспетчера `FUN_08000618`, раздел 6.13), после накопления 249-точечной истории отношений канал1/канал2 (буферы `0x200060f8`/`0x200064e0`, CONFIRMED смежные), считанных из уже известного сырого буфера коррелятора `0x20007840` (раздел 6.7/6.12) на тех же смещениях `+0x08/0x0c/0x10/0x14/0x20/0x24/0x28/0x2c`, что независимо нашла и параллельная сессия в разделе 6.12 — перекрёстное подтверждение. Точная формула 249-итерационного цикла — PROBABLE (похоже на кросс-корреляцию, не разобрана до вычисления). **Остаётся ПРИОРИТЕТОМ:** связь `puVar21+0x50` (структура `0x20005610`, значение из `0x20004920`, вероятно Z, см. раздел 6.12) со структурой `0x20004a48` (`Rs`/`Rp`/`L`/`C`, раздел 6.11) — прямая запись в неё по-прежнему не найдена (только флаг готовности по смещению `0`, в `main()`/`0x08017410`). Следующий шаг — либо разобрать `FUN_08000f52` целиком (в этой сессии прочитана примерно половина, до вызова `FUN_0800dbf8`), либо искать, что вызывается ПОСЛЕ стадии 6 диспетчера (стадия 7 просто возвращает в стадию 6 — раздел 6.13 — так что копирование в `0x20004a48`, вероятно, происходит либо в непрочитанном хвосте `FUN_08000f52`, либо в `FUN_08009618` самой на лету при отрисовке, что противоречит разделу 6.11 — нужно перепроверить).
26. **Обновлено 2026-09-08 (раздел 6.13):** вызывающие `FUN_0800dbf8` найдены — оба внутри `FUN_08000f52(float)`, вызываемой единственный раз из диспетчера стадий `FUN_08000618` (case `6`). `FUN_08000f52` обрабатывает два буфера (`0x20007840` и `0x20007c70`) по одному разу каждый через `FUN_0800dbf8`. Не разрешено: (а) как выбирается режим/какая из шести величин Rs/Rp/L/C/Re/Im в итоге получается, (б) действительно ли `0x20004920` относится к структуре результата `0x20004a48`/паре `0x20005668`/`0x20005670`.
27. ~~разобрать `FUN_08000f52` целиком~~ **ЗАКРЫТО 2026-09-09 (раздел 6.22): функция прочитана от первой до последней строки, прямой записи Rs/Rp/L/C в `0x20004a48` в ней НЕТ.** Также не разобрана `FUN_08001f46` (стадия 4 того же диспетчера) и не найдено, что взводит `*DAT_08000684` (флаг, разрешающий выполнение `FUN_08000618`).
29. **Обновлено 2026-09-08 (раздел 6.17) — ISR `DMA1_CH1` разобран, гипотеза 6.15 отклонена.** ISR (`0x08000644`) вызывает veneer-таблицу `0x08002dcc` (CONFIRMED: первая запись → `0x100001b8`, thunk раздела 6.7). Трёхрегистровая конвенция (r1/r2 меняются местами между TC/HT) CONFIRMED со стороны caller'а. Гипотеза "`r1=0x58e8`" — ОТКЛОНЕНА: реальный r1/r2 — halfword из рантайм-структуры `0x200048a0+2`, не литерал; связь с `0x200058e8` не подтвердилась (вероятно, случайное совпадение). **Остаётся открытым:** назначение структуры `0x200048a0` (поле `+2`). Установить `.data`-источник указателя `0x200048bc` (канал B) — проверить прямым чтением флеш-источника инициализации RAM (после установления границ копирования в `Reset_Handler`, раздел 2), равен ли он `0x20005758`.
28. **ADC channel mapping / DMA1/DMA2 source — частично ЗАКРЫТО 2026-09-08 (раздел 6.14):**
    - CONFIRMED: DMA1_CH1 ← ADC1_DR; DMA1_CH1 CMAR = `0x200058E8`;
      ADC1_IN1/PA0 = I_Measure; DMA2_CH1 ← ADC2_DR;
      DMA2_CH1 CMAR = `0x20005758`; ADC2_IN3/PA6 = U_Measure.
    - PROBABLE: семантика содержимого `0x200058E8`/`0x20005758` и единственность acquisition path.
    - Открыто: trace буферов сэмплов внутри коррелятора; роль `0x20007c70`.

## 14. Передача (Handoff)

В новом чате вставить:

> Продолжи реверс-инжиниринг RLC из `REVERSE_STATUS.md` (репозиторий
> `MussonOld/RLC` на GitHub). Склонируй репозиторий, прочитай раздел
> "14. Передача" и раздел "13. Ближайшие конкретные действия" целиком,
> проверь `git log` на более новые коммиты (могли появиться от
> параллельных сессий), и продолжай с ТЕКУЩЕЙ ЗАДАЧИ ниже. Не начинай
> анализ заново и не выдумывай неразрешённые аппаратные сопоставления —
> только CONFIRMED через прямое чтение кода/`.hex`, либо явно
> помеченное PROBABLE/UNKNOWN.

**Текущая задача (обновлено 2026-09-09, после раздела 6.22):**

1. ~~Переразобрать `0x10000B5C`~~ ЗАКРЫТО 2026-09-09, см. раздел 6.20/пункт 15.
2. ~~Доразобрать `FUN_08000f52` целиком~~ **ЗАКРЫТО ОТРИЦАТЕЛЬНО
   2026-09-09 (раздел 6.22): функция прочитана целиком (все ~1100
   строк, от первой до последней), прямой записи Rs/Rp/L/C в
   `0x20004a48` в ней НЕТ.** Единственное касание структуры — уже
   известное чтение/сброс флага готовности (offset 0).
3. **ПРИОРИТЕТ (новое, раздел 6.22): переосмыслена сама структура
   `0x20004a48`.** Найден блок сглаживания/анимации цифр внутри
   `FUN_08009618` (строки ~11150-11330) — смещения `+0x50/+0x60/+0x80
   /+0x88` (к которым привязаны строки-подписи Rs/Rp/L/C из раздела
   6.11) оказались **отображаемыми (сглаженными) копиями**, а не
   местом записи результата. Реальная цель записи — смещения
   `+0x30` (Rs), `+0x40` (Rp), `+0x90` (L), `+0x98` (C), в которые
   пока не найдено ни одной записи нигде в образе. Также повторно
   проверена `FUN_0800dbf8` (раздел 6.12) — она пишет только в
   отдельную ячейку `0x20004914`/`0x20004920` + флаг `0x20004a4d`, НЕ
   напрямую в `+0x30/+0x40/+0x90/+0x98`. Значит есть непрочитанная
   функция-посредник между `FUN_0800dbf8` и структурой отображения.
   Следующий шаг — один из двух (раздел 6.22): (а) дочитать
   `FUN_08009618` целиком (сейчас разобраны только блок показа
   Rs/Rp/L/C и блок сглаживания; функция явно больше — есть минимум
   ещё один "детальный" экран D/Q/L/C/Ri в районе `DAT_08009f34`,
   пока read-only в прочитанной части) на предмет записи в
   `+0x30/+0x40/+0x90/+0x98`; (б) разобрать кластер неидентифицированных
   функций в районе `0x0800d900-0x0800dc00` (литерал-пуловые ссылки
   на смещения структуры `+4/+0x18/+0x20/+0x28`, адреса
   `0x0800d994/d998/d9a0/d9a4/d9b8/d9c4/d9fc` — не прочитаны).

**Как воспроизвести окружение (проверено, рабочий способ):**
```bash
cd /home/claude && git clone https://github.com/MussonOld/RLC.git
cd RLC
```
Файлы: `GPIO_MAP.md`, `REVERSE_STATUS.md`, `nRLC_2_0_12_FREEWARE.hex`,
`nRLC_2_0_12_FREEWARE_ghidra_decompiled.txt` (~700К, читать через `sed`/
`grep` по диапазонам строк, не целиком), `nRLC_2_0_12_FREEWARE_peripheral_xref.txt`.

**Методологическая заметка (важно, экономит время):** Ghidra в этом
дампе НЕ подписывает многие литералы-указатели символами — `DAT_XXXXXXXX`
в декомпиляции часто оказывается адресом ЛИТЕРАЛ-ПУЛА (флеш), а не
итоговым значением. Чтобы узнать, что реально лежит по такому адресу,
всегда резолвить напрямую по `.hex` (4-байтное LE-чтение), не гадать по
имени переменной. Рабочий питон-сниппет для этого использовался во всей
сессии — распарсить `.hex` в `dict{addr:byte}` через `:` intel-hex формат
(тип записи `04` = extended linear address, `00` = data), затем
`read_u32(a) = mem[a] | mem[a+1]<<8 | ...`. Так же полезен сырой
4-байтный побайтовый поиск константы-указателя по всему образу, когда
текстовый `grep` по декомпиляции ничего не находит (пример — раздел
6.13, поиск `0x20005610`).

**Git push:** пользователь каждую сессию даёт свежий GitHub PAT прямо в
чате для `git push https://MussonOld:<PAT>@github.com/MussonOld/RLC.git main`
— это установленный, согласованный процесс, не разовое исключение.

## 15. Список изменений

### 2026-09-09 (уточнение §6.11 по замечанию параллельного ревью)
- **§6.11:** добавлена явная пометка (таблица полей + заголовок раздела
  + абзац "Место вычисления/записи"): смещения `+0x50/+0x60/+0x80/+0x88`
  — display shadow для UI-анимации (раздел 6.22), а не цель записи
  вычислителя; физическая цель — `+0x30/+0x40/+0x90/+0x98`, статус
  переноса ярлыков явно помечен PROBABLE (не CONFIRMED переименование
  структуры — прямой записи с контекстом Rs/Rp/L/C в новые смещения
  пока не найдено). Цель — чтобы следующая сессия не искала запись
  повторно в старых (уже проверенных и закрытых) смещениях.

### 2026-09-09 (гигиена статуса, по замечаниям параллельного ревью)
- **§4.3:** снят устаревший вывод "мёртвый код / битбэнг I2C — CONFIRMED"
  у `0x10000B5C` — заменён на актуальный (не мёртвый код, 20+ вызовов
  через вениир `0x08002de0`, электрически инертен на данной плате,
  атрибуция I2C-bitbang понижена до PROBABLE). Объяснено, почему прежний
  поиск (побайтовый литерал + скан CCM RAM) не находил вызывающий код —
  вызовы шли не через прямой литерал, а через вениир-таблицу (§6.17/6.19).
- **§10 (таблица):** синхронизирована запись `0x10000B5C` с §4.3.
- **§6.13:** снята устаревшая формулировка "два независимых
  буфера-аккумулятора / канал 1 / канал 2" — заменена на "живой
  аккумулятор / его периодический снимок" по факту, установленному в
  §6.18 (тот же текст исправлен и в диаграмме цепочки вызовов).
- **§6.15:** добавлена форвард-пометка о том, что гипотеза `r1=0x58e8`
  отклонена в §6.17 (сам параграф уже содержал корректный PROBABLE-статус,
  пометка облегчает чтение без забегания вперёд по документу).
- **§6.17:** ссылка на пункт 15 дополнена пояснением, что статус
  `0x10000b5c` на момент написания параграфа считался иным и был позже
  переоткрыт в §6.19/6.20.

### 2026-09-09 (продолжение)
- **Раздел 6.22 (новый):** `FUN_08000f52` дочитана ПОЛНОСТЬЮ (все
  оставшиеся промежутки строк 1123-2223) — запись Rs/Rp/L/C
  ОТСУТСТВУЕТ во всей функции, приоритет закрыт отрицательно
  окончательно. Ключевая находка: в `FUN_08009618` обнаружен блок
  сглаживания/анимации цифр, из-за которого смещения `+0x50/+0x60
  /+0x80/+0x88` структуры `0x20004a48` — это отображаемые
  (сглаженные) копии, а не место записи; реальная цель поиска
  переставлена на `+0x30/+0x40/+0x90/+0x98`. `FUN_0800dbf8`
  перепроверена — пишет только в промежуточную ячейку `0x20004914
  /0x20004920` + флаг `0x20004a4d`, не напрямую в структуру
  отображения. Найдена непрочитанная функция-посредник между ними.
  Раздел 13/14 обновлены — новый приоритет: дочитать `FUN_08009618`
  целиком и/или разобрать кластер функций `0x0800d900-0x0800dc00`.

### 2026-09-09
- **Раздел 6.20 (новый):** `thunk_EXT_FUN_10000b5c` (`0x10000B5C`, CCM
  RAM) переразобран прямым дизассемблированием (Capstone/Thumb) флеш-
  зеркала. Байткод подтверждает прежнее описание раздела 4.3 инструкция
  в инструкцию (GPIOB `0x48000400`, PB6/PB7 push-pull/open-drain toggle
  через `FUN_08005f06`). Контрадикция пункта 15 РАЗРЕШЕНА: "нигде не
  вызывается" — опровергнуто (20+ вызовов из `FUN_08000f52`, раздел
  6.19); причина пропуска в прежнем поиске — вызовы идут через
  двухинструкционный вениир `0x08002de0`, не через литерал-указатель.
  Гипотеза "строб/защёлка дисплея" (альтернатива 6.19) — отклонена
  (open-drain нетипичен для CS/DC). Новая формулировка: код электрически
  инертен (PB6/PB7 физически NC), но регулярно исполняется — не "мёртвый
  код" в смысле "недостижим".
- **Раздел 6.21 (новый):** стабильная ветка `FUN_08000f52` (строки
  ~1856-2224) прочитана целиком. Запись Rs/Rp/L/C в `0x20004a48` ЗДЕСЬ
  НЕ найдена — ветка полностью объясняется автовыбором диапазона,
  управлением реле и триггером перерисовки; единственное обращение к
  базовому адресу структуры — чтение флага готовности. Гипотеза раздела
  14 (предыдущая версия) — отклонена. Найдена карта новых полей
  структуры `0x20004a48` (offsets +3/+4/+9/+0x18/+0x20/+0x28) и новый
  кандидат на промежуточное вычисление — `puVar21+0x68` (`0x20005678`,
  PROBABLE `|Z|`) в строках ~1636-1653, ДО обеих прочитанных веток.
  Раздел 13/14 обновлены: приоритет 2 закрыт, единственный открытый
  приоритет — доразбор `FUN_08000f52` с исправленной областью поиска.

### 2026-09-08 (продолжение 10, в тот же день)
- **Раздел 6.17 (новый):** ISR `DMA1_CH1` (`0x08000644`) разобран
  целиком. `0x08002dcc` (найден параллельной сессией) — CONFIRMED,
  это linker veneer-таблица (3 записи: `0x100001b8` = thunk раздела
  6.7, `0x10000bc0`/`0x10000b5c` = уже известные функции пункта 15),
  не отдельная функция — противоречия с 4.4/6.7 нет, это уточнение
  адреса. Трёхрегистровая конвенция раздела 6.7 (r1/r2 меняются между
  TC/HT) — CONFIRMED теперь и со стороны caller'а. Гипотеза раздела
  6.15 (`r1=0x58e8`) — ОТКЛОНЕНА: реальный параметр — halfword из
  рантайм-структуры `0x200048a0+2`, не литерал; связь с `0x200058e8`
  не подтвердилась, снята с рассмотрения. Пункт 29 переформулирован.

### 2026-09-08 (продолжение 9, в тот же день — сведение с параллельной сессией)
- **Раздел 6.16 (новый):** независимо (до синхронизации с параллельной
  сессией, добавившей разделы 6.12-6.15) найдено место записи `Re`
  (`0x20005668`) и `Im` (`0x20005670`) — внутри `FUN_08000f52`, после
  накопления 249-точечной истории отношений в буферах `0x200060f8`/
  `0x200064e0` (CONFIRMED смежные: `0x200060f8+0xfa·4=0x200064e0`).
  Источник данных для этой истории — `pfVar4 = 0x20007840`,
  **тот же буфер и те же смещения** (`+0x08/0x0c/0x10/0x14/0x20/0x24/
  0x28/0x2c`), что параллельная сессия независимо определила в разделе
  6.12 для `FUN_0800dbf8` — двойное независимое подтверждение одного
  и того же адреса разными путями анализа. Пункт 25 раздела 13
  переформулирован: Re/Im закрыто, Rs/Rp/L/C остаётся приоритетом.
  Точная формула 249-итерационного цикла — PROBABLE, не до конца
  разобрана.

### 2026-09-08 (продолжение 8, в тот же день)
- **Раздел 6.15 (новый):** разбирая приоритетный пункт 27
  (`FUN_08000f52`), сначала дизассемблировал незатронутое ранее начало
  `thunk_EXT_FUN_100001b8` (`0x100001b8..0x10000220`) — нашёл настройку
  источников данных для корреляции. Канал A: адрес = `0x20000000`
  (литерал, SRAM-база) + параметр функции `r1`×2. Арифметика
  `0x20000000+0x58e8=0x200058e8` точно совпадает с адресом `DMA1_CH1`
  буфера из раздела 6.14 — CONFIRMED сама арифметика, PROBABLE что
  реальный параметр вызова равен `0x58e8` (ISR `DMA1_CH1` не разобран).
  Канал B: указатель динамический, из `0x200048a8+0x14`=`0x200048bc`,
  место записи не найдено (вероятно `.data`, не `str`-инструкция).
  Добавлен пункт 29 раздела 13.

### 2026-09-08 (продолжение 7, в тот же день)
- **Раздел 6.14 (новый):** раздельная фиксация уровней доказательства:
  - CONFIRMED: `DMA1_CH1` = ADC1_DR; CMAR1 = `0x200058E8`;
    ADC1 rank1 = Channel 1 = PA0 (I_Measure);
    `DMA2_CH1` = ADC2_DR; CMAR1 = `0x20005758`;
    ADC2 rank1 = Channel 3 = PA6 (U_Measure).
  - PROBABLE: семантика «буфер = ток/напряжение» и единственность
    acquisition path (нужен trace `0x200058E8`/`0x20005758` в корреляторе).
- Раздел 4.4 дополнен обоими DMA-каналами:
  - `DMA1_CH1: CPAR = ADC1_DR, CMAR = 0x200058E8`
  - `DMA2_CH1: CPAR = ADC2_DR, CMAR = 0x20005758`
- Пункт 28 в разделе 13: channel mapping и оба DMA source закрыты,
  downstream — открыт.
- **Независимая верификация (отдельный чат):** все числовые утверждения
  раздела перепроверены напрямую по `.hex` (не по decompiled-тексту, где
  значения литералов не раскрыты) и по датащиту STM32F303xB/xC —
  расхождений не найдено.

### 2026-09-08 (продолжение 6, в тот же день)
- **Раздел 6.11 (новый):** уточнена граница функции результата — это
  `FUN_08009618`, а не рабочая оценка `~0x0800a9f8..0x800ad28` из
  раздела 6.10. Найдена и размечена структура результата измерения
  `0x20004a48` (8 независимых ссылок по всему образу) — поля `Rs`
  (`+0x50`), `Rp` (`+0x60`), `L` (`+0x80`), `C` (`+0x88`) подтверждены
  напрямую строками-подписями, извлечёнными Ghidra из прошивки. Пара
  `0x20005668`/`0x20005670` из раздела 6.10 идентифицирована как **Re**
  и **Im** (реальная/мнимая часть импеданса) — тем же методом (строки
  `s_Re__0800ad9c`/`s_Im__0800adc0` в той же функции). Место записи
  всех этих величин по-прежнему не найдено — только запись байта
  готовности (смещение `0` структуры) в `main()`. Пункт 25 раздела 13
  переформулирован под текущее состояние.

### 2026-09-08 (продолжение 5, в тот же день)
- **Раздел 6.10 (новый):** нашёл вторую точку чтения для переменных
  `0x20005668`/`0x20005670` (раздел 6.8) — функция автоматического
  выбора разрядности/масштаба для отображения (`~0x0800a9f8..0x800ad28`,
  границы не проверены так строго, как в 6.7). Обе переменные читаются
  в двух независимых местах (дисплей + упаковка отчёта ×100), нигде не
  записываются — сильный аргумент, что это уже готовые финальные
  результаты измерения, а не промежуточные накопители. Место вычисления
  и записи по-прежнему не найдено — главный кандидат на прямой путь к
  разделу 11 ("расчёт R/L/C"), новый пункт 25 раздела 13.

### 2026-09-08 (продолжение 4, в тот же день)
- **Раздел 6.9 (новый):** разобраны все 5 "нерасшифрованных fixed-point
  функций" из пункта 20 (плюс 2 их внутренних вызова). Результат:
  это стандартная EABI soft-float библиотека для `double` —
  `__aeabi_i2d`, `__aeabi_dmul`, `__aeabi_d2iz`/`d2uiz`, `__aeabi_d2f`,
  `trunc()`-подобная и `__aeabi_dsub`/`dadd` — не кастомная логика
  прошивки. Пункт 20 переформулирован: искать не "как устроены
  конвертеры" (уже известно), а double-константы в литерал-пулах
  `FUN_08017dc8` и новой функции раздела 6.8.

### 2026-09-08 (продолжение 3, в тот же день)
- **Раздел 6.8 (новый):** искал точку записи `0x200049e8` сырым байтовым
  поиском литерала по всему образу флеша (не только CCM RAM) — найдены
  только 3 вхождения, все чтения, ни одной записи. Два уже известны
  (раздел 6.7), третье — новое: `0x08017ff6` в ранее неразобранной
  функции `~0x08017f70..0x08018108` (граница PROBABLE, не проверена так
  строго, как в 6.7). Значение используется в умножении для заполнения
  структуры-отчёта (`r4 = *(0x20004650+0x44)`, поля `0x06..0x28`).
  Побочно: эта функция интенсивно вызывает те же нерасшифрованные
  fixed-point-функции (`FUN_08002932` и др.), что и пункт 20 раздела 13
  (калибровка `FUN_08017dc8`) — теперь это общий блокер для двух задач.
  Сама точка записи `0x200049e8` не найдена — уточнено, что искать
  дальше нужно смещения от соседних базовых указателей, не сам литерал.

### 2026-09-08 (продолжение 2, в тот же день)
- **ИСПРАВЛЕНА граница `thunk_EXT_FUN_100001b8`** (раздел 6.7): предыдущая
  оценка `0x100001b8..0x100008d9`, зафиксированная ранее в этот же день,
  оказалась неполной — `0x100008da..`, ранее считавшийся "следующей
  функцией" (пункт 23), на самом деле достигается условным переходом
  `bhs #0x100008da` изнутри тела функции 1 (адрес перехода `0x100008a0`),
  до её `pop`. Находка получена из параллельного чата (наблюдение про
  внутренний переход), перепроверена в этой сессии заново полной
  дизассемблировкой всего блока CCM RAM (Capstone), а не принята на веру.
  Исправленная граница: `0x100001b8..0x10000b27` (0x996C = 2412 байт, было
  0x722 = 1826), три точки выхода вместо двух, все со сходящейся
  арифметикой стек-фрейма. Сразу после функции (без зазора после 4 байт
  литерала) начинается уже ранее CONFIRMED `EXTI15_10` (`0x10000b28`,
  раздел 4.3) — граница снизу подтверждена независимо. Побочно: литерал
  `0x10000b24` резолвится ровно в `0x200049e8` — тот же адрес, который
  раньше отмечался как "переключатель 0/1"; в новой (расширенной) части
  функции то же слово используется повторно и сравнивается каскадно с
  несколькими порогами (`6/8/0x18/0x20/0x30/0x40`), что скорее говорит о
  многозначном счётчике стадии, чем о бинарном флаге — формулировка
  пункта 24 уточнена, не удалена целиком (прежнее наблюдение "сравнение с
  1" в ранней части функции остаётся в силе как отдельный факт). Пункт 23
  закрыт отрицательно: отдельной функции по этому адресу не существует.

### 2026-09-04
- CONFIRMED: семейство МК — STM32F303 (STM32F3), независимо сверено с
  официальным `STM32F303.svd`: раскладка таблицы векторов
  (ADC1_2/USB_HP_CAN_TX/TIM8/ADC3/ADC4/векторы DMA1 по каналам), прямая
  ссылка на адрес общих регистров ADC `0x50000000`, и начальный SP
  (`0x20009968`), согласующийся с 40КБ SRAM у F303. Полное обоснование —
  в разделе 1.
- Понижена атрибуция TIM15/TIM17 для `FUN_08008384`/`FUN_080101b4` с
  "high" до PROBABLE: прямых ссылок на регистры ни одного из таймеров не
  найдено при исчерпывающей сверке адресов по SVD; использование TIM16
  остаётся подтверждённым.
- Отмечено, что реальный размер Flash превышает 128КБ (CB) — на тот
  момент это давало только диапазон CC/RC (256КБ+); **позже, 2026-09-06,
  окончательно подтверждено MussonOld по физической маркировке на
  корпусе снятого чипа: точная модель — STM32F303CCT6** (см. раздел 1).

### 2026-09-04
- Сведено известное состояние реверс-инжиниринга в единый документ передачи.
- Зафиксированы доказательства по МК/адресам периферии.
- Зафиксированы находки по старту/main и кандидату в ISR.
- Зафиксированы известные IRQ NVIC.
- Зафиксированы доказательства по USB/GPIO/таймерам.
- Зафиксированы ID конфигурации и командные строки.
- Определены правила уверенности и текущие ближайшие действия.

### 2026-09-05 (перевод)
- Файл полностью переписан на русском языке по просьбе MussonOld; вся
  техническая информация (адреса, имена функций, таблицы, обоснования)
  сохранена без изменений по содержанию, переведена только описательная
  часть.

### 2026-09-06 (продолжение 10)
- Пункт 18 ВЫПОЛНЕН (даташит STM32F303xB/xC `DS9118` открыт напрямую с
  сайта ST, таблица 13 пинов сверена): вывод продолжения 7 про канал
  ("PROBABLE `ADC4_IN1`") **ОТКЛОНЁН** — `ADC4_IN1` физически выведен на
  `PE14`, а порт E отсутствует на корпусе `LQFP48` подтверждённого
  `STM32F303CCT6`. Заодно проверен реальный `stm32f3xx_hal_adc_ex.h` из
  `STM32CubeF3` — методика "Channel=первое поле, ADC_CHANNEL_1=1"
  подтверждена корректной для этого семейства МК, но поле `Rank=0xc00000`
  в том же вызове не совпадает с ожидаемым `ADC_REGULAR_RANK_1=0x1`,
  ослабляя доверие к структуре аргумента `FUN_08004c1c` в целом. Канал
  блока C — снова UNKNOWN (пункт 22). Раздел 6.6 переписан начисто под
  текущий (актуальный) вывод — прежняя формулировка CONFIRMED/PROBABLE
  для канала удалена, а не оставлена рядом с опровержением.

### 2026-09-06 (продолжение 9)
- Раздел 13, пункт 3: разобраны все оставшиеся DMA IRQ (11/13/14/15/56/58).
  Пять из шести (`DMA1_CH3/4/5`, `DMA2_CH1/3`) — общий диспетчер
  `FUN_080059ba`, CONFIRMED как `HAL_DMA_IRQHandler`-подобная функция.
  `DMA1_CH1` (IRQ11) — уникальный, разобран целиком: работает напрямую с
  `DMA1->ISR/IFCR`, тайл-вызывает `thunk_EXT_FUN_100001b8`.
- **Крупная находка:** `thunk_EXT_FUN_100001b8` ведёт в большую функцию в
  CCM RAM (`0x100001b8`) с 42+ инструкциями `SMLAD` — PROBABLE ядро
  алгоритма измерения (синхронное детектирование/корреляция с опорной
  синус/косинус-таблицей). Функция не разобрана до конца (больше 1350
  байт без найденного возврата) — вынесена в раздел 13 как приоритетный
  пункт 21 для отдельной сессии. Подробности — раздел 4.4.
- Исправлена рассинхронизация чеклиста: пункт 4 раздела 13 годами (с
  2026-09-04) содержал устаревшую формулировку про `FUN_0800019c`,
  фактически разрешённую ещё в разделе 4.2 в тот же день — исправлено.

### 2026-09-06 (продолжение 8)
- РАЗРЕШЕНО (пункт 19): `FUN_08017dc8` (один из ранее неразобранных
  вызывающих `FUN_0800ee00`) разобрана целиком — читает ADC4 (тот же
  хэндл `0x20006968`), считает калибровочную формулу через fixed-point
  библиотечные функции, и при устойчиво низком результате (100 подряд
  идущих "плохих" измерений) вызывает `FUN_0800ee00(1)`. PROBABLE:
  защита по низкому напряжению батареи. Составлена сводка всех трёх
  известных причин `FUN_0800ee00` (0/1/2). Подробности — раздел 6.6.
- Пункт 18 (сверка `ADC4_IN1` с выводом корпуса `LQFP48` по даташиту)
  не выполнен — в текущей среде реверса нет доступа к сайту ST/PDF
  даташита; оставлено как открытая задача для MussonOld или сессии с
  доступом к даташиту.

### 2026-09-06 (продолжение 7)
- РАЗРЕШЕНО (пункт 14): хэндл `0x20006968`, используемый в блоке C
  обработчика TIM7 (раздел 6.3), CONFIRMED указывает на **ADC4**
  (`Instance = 0x50000500`, прямая запись литерала найдена в `main`,
  `FUN_08016274`). Канал — на тот момент PROBABLE `ADC4_IN1` (этот вывод
впоследствии отклонён, см. запись "продолжение 10" ниже, и окончательно
пересмотрен на CONFIRMED `ADC4_IN4`/`PB14` — раздел 6.6, запись
2026-09-07). Побочно: второй
  потребитель того же хэндла — `FUN_08017dc8`, один из ранее
  неразобранных вызывающих `FUN_0800ee00` (раздел 6.2) — вероятный
  кандидат на защитное отключение по напряжению батареи. Подробности —
  раздел 6.6. Также довнесена в раздел 6.3 (Блок A) точная
  двух-счётчиковая структура таймаута (`0x2E` независим от `0x28→0x2C`),
  утерянная при параллельном редактировании файла в этой же сессии —
  восстановлена и связана с находкой про 1Гц из раздела 6.5 (гипотеза
  про программный делитель 10:1 от сырого ~10Гц IRQ).

- CONFIRMED: точный номер модели МК — **STM32F303CCT6**. Подтверждено
  MussonOld напрямую по физической маркировке на корпусе снятого с
  прибора (неисправного) процессора — самое надёжное из возможных
  оснований, сильнее любого косвенного вывода по размеру образа. Убраны
  все формулировки "CC против RC против другого варианта" из
  `REVERSE_STATUS.md`, `REVERSE_EVIDENCE.md` и `GPIO_MAP.md`.

### 2026-09-06 (продолжение 5)
- УТОЧНЕНИЕ по блоку B TIM7 (раздел 6.3): на резонное сомнение (1-секундный
  опрос кнопок непригоден для навигации по меню) найдено подтверждение —
  кнопки читаются напрямую и быстро (мс-масштаб) в `main()` независимо от
  TIM7, включая блокирующий busy-wait опрос JOY_OK и отдельную функцию
  задержки на миллисекундах `FUN_080154c8` (разобрана по байтам). Блок B в
  TIM7 переквалифицирован: это не основной ввод, а отдельный, более
  медленный механизм обнаружения долгого удержания (10 секунд для
  выключения питания подтверждено; смысл для `PA2`/`PB12` пока не
  установлен — новый пункт 17).

### 2026-09-06 (продолжение 4)
- РАЗОБРАНА и ЗАКРЫТА функция `0x10000B5C` в CCM RAM (см. раздел 4.3):
  битбэнг-сброс `PB6`/`PB7` (GPIOB) через `FUN_08005f06`. Гипотеза о
  связи с протоколом PGA113 НЕ подтвердилась — те линии на PB0/PB1/PB2/
  PB10 (`GPIO_MAP.md`). Функция нигде не вызывается (0 совпадений и по
  литерал-поиску во всём образе, и по дизассемблированному проходу всей
  CCM RAM на `bl`/`b.w`), а её пины физически NC на этой плате — признана
  мёртвым кодом, вероятно от неиспользуемой I2C-периферии.

### 2026-09-06 (продолжение 3)
- CONFIRMED: реальный период тика TIM7 = 1 секунда (см. новый раздел
  6.5). Найден не через PSC/ARR, а через сеттер порога автовыключения
  `FUN_0800fcf4` (работает с той же структурой состояния `0x200056A0`,
  что и ISR TIM7 — подтверждено побитовым совпадением адреса) и
  сопоставление его аргументов (120/300/600 тиков) со строковыми метками
  меню "Timer 2/5/10 min", извлечёнными из образа флеша. Совпадение
  подтверждено трижды независимо, без противоречий. Заодно пересчитаны в
  реальное время все константы блока A/B/C из раздела 6.3 (10с удержания
  кнопки, предупреждения за 30с/10с до автовыключения, счётчики 100с и
  600с). Задача из пункта 16 списка действий закрыта иначе, чем
  планировалось — отладчик для этого не понадобился.

### 2026-09-06 (продолжение)
- TIM7 RCC/NVIC/PSC/ARR (раздел 6.4): CONFIRMED — RCC_APB1ENR бит5
  (TIM7EN), NVIC IRQ55 приоритет `0xF` (низший), через общий диспетчер
  `FUN_08007944`. Заодно этим же методом расписаны RCC-биты и IRQ для
  TIM1/2/3/4/16. Для сравнения полностью разобрана самодостаточная
  функция инициализации TIM6 (`FUN_08003428`) — даёт точный шаблон
  полей/формул (PSC, ARR по формуле от частоты шины, DIER напрямую).
  Числовые PSC/ARR/DIER именно для TIM7 НЕ найдены статически (хэндл,
  видимо, создаётся через косвенный вызов MSP-callback, не через прямой
  `bl`) — после исчерпания прямых методов (литерал-скан всего образа,
  полный проход Capstone на movw/movt, обход всех вызовов
  MSP-init-обёрток) реальный период найден другим путём (см. запись
  выше, раздел 6.5), без обращения к отладчику.
- РАЗРЕШЕНО: сигнал `PA1` (раздел 6.3) = `JOY_OK`, сверкой с `GPIO_MAP.md`.
- НАЙДЕН механизм инициализации CCM RAM (`0x1000xxxx`): таблица копирования
  из Reset-цепочки (3 записи src/dst/len/func), запись для CCM —
  простое побайтовое копирование без сжатия из `0x0802D7DC` в
  `0x10000000`, длина `0xC18`. Это позволяет читать содержимое CCM RAM
  напрямую из `.hex`, не выполняя код. См. новый раздел 4.3.
- РАЗОБРАН ПО БАЙТАМ (Capstone) обработчик `EXTI15_10` @ `0x10000B28`:
  обрабатывает только линию EXTI14 (`PC14`/INT2 BMI160), инкрементирует
  счётчик по смещению `0x6` от структуры состояния; линия EXTI15
  сбрасывается без обработки. См. раздел 4.3.
- РАЗРЕШЕНО (повышено с PROBABLE до CONFIRMED): `thunk_EXT_FUN_10000bc0`
  @ `0x10000BC0` — неблокирующий драйвер пищалки/зуммера (`PA8`/`ZP`):
  принимает длительность в мс, конвертирует в тики по 10мс, игнорирует
  повторный запуск, пока предыдущий сигнал активен. См. раздел 4.3.
- ЧАСТИЧНО РАЗОБРАН `FUN_08005148` (блок C, раздел 6.3): подтверждено
  чтение `ADC_DR` (смещение `0x40` от базы периферии совпадает с ADC у
  STM32F303), но конкретный ADC1..ADC4/канал не установлен — хэндл
  инициализируется во время выполнения, не найден статическим поиском.
  Новая задача — разметить ADC1..ADC4 по SVD.
- Побочная находка: в CCM RAM по адресу `0x10000B5C` — необследованная
  функция, битбэнгающая GPIO через `FUN_08005f06` (см. раздел 4.3).

### 2026-09-06
- РАЗРЕШЕНО: ISR USB (IRQ20, вектор `0x080126E4`) — прямым
  дизассемблированием (Capstone, Thumb-2) найден 2-инструкционный
  трамплин (`ldr r0,[pc,#4]; b.w 0x08006328`), передающий управление в
  `FUN_08006328`, чья битовая логика опроса точно совпадает с раскладкой
  регистра `USB_ISTR` USB FS Device controller STM32F3. Подробности,
  таблица бит -> действие и дочерние обработчики — раздел 5.
- Исправлена рассинхронизация чеклиста: пункт "разрешить обработчик
  SysTick" был фактически закрыт ещё 2026-09-04 (раздел 4.2), но не был
  отмечен как выполненный в разделе 13 — исправлено.
- Добавлены новые ближайшие действия: разобрать целиком обработчик TIM7
  (для проверки гипотезы про автовыключение) и, опционально, дочерние
  обработчики USB-событий.
- РАЗОБРАН ЦЕЛИКОМ обработчик TIM7 (`FUN_0800ff78`, раздел 6.3).
  Уточнена гипотеза раздела 6.2: это не отдельная "вторая стадия
  автовыключения", а совмещённый сторожевой таймаут бездействия
  (счётчик до порога -> предупреждающие сигналы -> `FUN_0800ee00(2)`)
  и периодический дебаунс трёх кнопок/джойстика (`PA1` — на момент
  разбора не идентифицирован, `PA2`=`JOY_C`, `PB12`=`JOY_B`), плюс
  отдельный аналоговый канал сравнения с порогом (физический смысл не
  установлен). Новые открытые вопросы зафиксированы в разделе 13,
  пункт 13.
- РАЗРЕШЕНО: сигнал `PA1` из раздела 6.3 — сверкой с `GPIO_MAP.md`
  (раздел "Джойстик") установлено, что `PA1` = `JOY_OK` (средняя кнопка
  джойстика). Согласуется с ролью в `FUN_0800ff78`: JOY_OK аппаратно
  используется для включения питания (замыкает Q2), а удержание той же
  кнопки уже во включённом состоянии программно вызывает
  `FUN_0800ee00(0)` — то есть кнопка одна и та же на вкл/выкл, как это
  обычно и бывает у портативных приборов. Раздел 6.3 и пункт 13
  обновлены.

### 2026-09-07
- РАЗРЕШЕНО: противоречие раздела 6.6 по каналу ADC4 (пункт 22).
  `FUN_08004c1c` пересмотрен по точным `str`-инструкциям дизассемблера
  (не по порядку печати decompiled-переменных Ghidra, который вводил в
  заблуждение) — оказался НЕ конфигуратором канала, а, по месту записи
  (`Instance+0x24`/`+0x28`) и паттерну `1<<channel`, вероятно, настройкой
  аналогового watchdog. Найден и разобран реальный конфигуратор канала —
  ранее не задокументированный вызов `FUN_08004d94`, пишущий
  `channel << (rank*6)` напрямую в `SQR1` (`Instance+0x30`, точная
  аппаратная формула по RM0316). Для хэндла ADC4 это дало `Channel=4,
  Rank=1`. По официальному даташиту (`DS9118`, Table 13, скачан
  напрямую с `st.com`) `ADC4_IN4 = PB14`. Это совпало с уже независимо
  установленным по схеме назначением `PB14` (`JOY_A/JOY_D/BAT`,
  `GPIO_MAP.md`) — два независимых метода (реверс прошивки и чтение
  схемы) сошлись на одном выводе. Заодно скачаны и использованы реальные
  заголовки `stm32f3xx_hal_adc.h`/`_ex.h` из
  `github.com/STMicroelectronics/stm32f3xx_hal_driver` для точной
  проверки семантики полей `ADC_ChannelConfTypeDef`/`ADC_InitTypeDef`.
  Пункты 13/14/22 раздела 13 и раздел 6.6 обновлены.

### 2026-09-08
- Исправлена устаревшая формулировка в записи "2026-09-06 (продолжение 7)":
  строка "Канал — PROBABLE `ADC4_IN1`" оставалась без пометки о том, что
  этот вывод впоследствии отклонён ("продолжение 10") и затем окончательно
  пересмотрен на CONFIRMED `ADC4_IN4`/`PB14` (раздел 6.6, 2026-09-07) —
  добавлена аннотация со ссылкой на итоговый результат, сама историческая
  запись не удалена (отражает, что было известно на тот момент).
- Продолжен пункт 21 раздела 13 (ПРИОРИТЕТ): разобран `thunk_EXT_FUN_100001b8`
  (ядро корреляции в CCM RAM, тайл-вызов из `DMA1_CH1`). Без Ghidra GUI
  (недоступен в этой сессии) — прямым дизассемблированием Capstone
  (Thumb-2) по байтам `.hex`. Найдена и подтверждена побайтовой сходимостью
  стек-фрейма полная граница функции: `0x100001b8..0x100008d9` (0x722
  байт), ранее считавшаяся неустановленной (раздел 4.4). Сразу за ней —
  ещё одна функция (`0x100008da..`), не разбиралась в этой сессии.
  Попутно найдена и исправлена неточность раздела 4.4: реальная calling
  convention `thunk_EXT_FUN_100001b8` — три регистра (`r0`,`r1`,`r2`), а
  не два; `r1`/`r2` различаются между ветками TC/HT обработчика
  `DMA1_CH1` (проверено прямым повторным дизассемблированием самого
  обработчика, а не по прежнему пересказу). Подробности, включая
  CONFIRMED-находки про структуру `0x200048a0` и UNKNOWN-вопросы про
  переключатель `0x200049e8` — раздел 6.7. Добавлены новые открытые
  пункты 23/24 в раздел 13.
