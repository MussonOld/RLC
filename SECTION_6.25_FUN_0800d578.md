### 6.25 `FUN_0800d578` — формула Rs/Rp/L/C (закрытие раздела «расчёт R/L/C»), 2026-09-09

Продолжение раздела 6.23 (адреса записи CONFIRMED). Разобраны
комплексная арифметика, источник частоты и алгебраические тождества
L/C/Rp. Вызов: один раз из `FUN_08000f52` (строка декомпиляции 1629),
`FUN_0800d578(param_1, param_2)`, где `param_1` — буфер с полями
коррелятора (`+8/+0xc`, `+0x10/+0x14` — ток; `+0x20/+0x24`,
`+0x28/+0x2c` — напряжение; тот же макет, что у `FUN_0800dbf8`).

#### 6.25.1 Примитивы (CONFIRMED по телу функций + `.hex`)

| Функция | Операция |
|---|---|
| `FUN_08012a04(a,b,c,d)` | \((a+jb)(c+jd)\): \(\mathrm{Re}=ac-bd\), \(\mathrm{Im}=ad+bc\) |
| `FUN_080129e2(a,b,c,d)` | \((a+jb)/(c+jd)\): деление на \(c^2+d^2\) |
| `FUN_08002698` / `FUN_0800277c` | soft-float double × / ÷ |
| `FUN_08002932` / `FUN_080029b8` | float ↔ double |
| `FUN_08015de0(buf)` | \(*(buf+0x30) \cdot 32767.0 \cdot 0.5\) — масштаб коррелятора |
| `FUN_08000d5a` / `FUN_08000d7c` | gain I / U (PGA × 1.5, см. ранее) |
| `FUN_080006a8` | \(f = (*(0x200049A8+0x3c)) / (*(0x200049A8+0x14))\) (Гц); при нулевом делителе возвращает `1.0f`; пишет uint в `+0x48` |

Константы литерал-пула `FUN_0800d578` (LE double/float, чтение `.hex`):

| Адрес | Значение |
|---|---|
| `0x0800d9e0` | \(2\pi\) |
| `0x0800d9b0` | \(\pi\) |
| `0x0800d9a8` | `180.0` |
| `0x0800d9d8` | `1.0` |
| `0x0800d9c8` | `-1.0` |
| `0x0800d98c` | float `1/4096` (`0x39800000`) |
| пороги | `±1e-10`, `2e-8`, `1e5`, `-5e-12` (отсев «плохих» L/C) |

#### 6.25.2 Префикс: Z и Rs (CONFIRMED)

```text
scale = FUN_08015de0(param_1)
I = (Re_I + j·Im_I) / (scale · gain_I)
U = (Re_U + j·Im_U) / (scale · gain_U)
Z = U / I                              // FUN_080129e2
Rs = Re(Z)  →  *0x20004a78 (+0x30)     // DAT_0800d9bc, до ветки +3
```

Промежуточные поля (запись в префиксе, роли — рабочие буферы):

| Адрес | Смещение от `0x20004a48` | Роль |
|---|---|---|
| `0x20004a60` | `+0x18` | промежуточная величина (фаза/\|Z\|-связанное) |
| `0x20004a68` | `+0x20` | источник Rs (копируется в `+0x30`) |
| `0x20004a70` | `+0x28` | промежуточная (Im/связанное с X) |

Дополнительно используется рабочая структура `0x20004914` (`DAT_0800d994`) — поля `+0x14/+0x18` (double) после усреднения/нормировки Z.

`FUN_080006a8()` вызывается после префикса — дальше L/C используют \(f\) и \(\omega = 2\pi f\).

#### 6.25.3 Частота и ω (CONFIRMED)

```text
f = FUN_080006a8()    // из генератора 0x200049A8: (+0x3c)/(+0x14)
ω = 2π · f            // литерал DAT_0800d9e0
```

Связь с трактом возбуждения (разделы генератора / TIM2): `+0x3c` — база от system clock; `+0x14` — произведение длины таблицы и числа периодов. См. также `FUN_080009f0` / `FUN_08018158`.

#### 6.25.4 Ветки по режимному байту `+3` (`0x20004a4b`)

Селектор: `*DAT_0800d9c4` (`0x20004a4b`). Тот же байт в `FUN_08009618` переключает подпись «Rs»/«Rp» на экране (разделы 6.11 / 6.22 / 6.23).

Обозначения: \(R = \mathrm{Re}(Z)\), \(X = \mathrm{Im}(Z)\), \(|Z|^2 = R^2 + X^2\).

**Физические тождества (CONFIRMED по операциям ×/÷ с константами \(2\pi\), \(1\), \(-1\)):**

| Величина | Выражение | Адрес записи |
|---|---|---|
| **Rs** | \(R\) | `0x20004a78` (`+0x30`) |
| **L** | \(X / \omega\) | `0x20004ad8` (`+0x90`) |
| **C** | \(-1 / (\omega \cdot X)\) | `0x20004ae0` (`+0x98`) |
| **Rp** | \(|Z|^2 / R\) (экв. форма через \(\mathrm{Re}(1/Z)\)) | `0x20004a88` (`+0x40`) |

**Порядок записи**

- **`+3 == 0`:** сначала цепочка в `+0x98` (C) и связанные промежуточные/Rp; при прохождении порогов — `+0x90` (L).
- **`+3 != 0`:** сначала `+0x40` (Rp), затем `+0x90` (L); при прохождении порогов — `+0x98` (C).

То есть формулы L/C **одни и те же**; байт `+3` меняет порядок и акцент series/parallel (UI), а не другую физическую модель. Знак \(X\) и «показывать L или C» дополнительно режутся сравнениями с порогами (`FUN_08002958` / `FUN_08002988`).

Флаги готовности/качества: `0x20004a4c` (`+4`), `0x20004a4d` (`+5`, уже известный ready из 6.12).

#### 6.25.5 Связь с `FUN_0800dbf8`

`FUN_0800dbf8` считает комплексный/модульный результат в соседние поля и **не** пишет Rs/Rp/L/C в `+0x30/+0x40/+0x90/+0x98`.  
`FUN_0800d578` — следующий шаг: физические величины для UI (`FUN_08009618` lerp → display-shadow `+0x50…`, раздел 6.22).

#### 6.25.6 Цепочка end-to-end (обновлённая)

```text
ADC1/ADC2 → DMA → CCM-коррелятор → буфер 0x20007840
  → FUN_0800dbf8 (комплекс / |Z|-путь)
  → FUN_0800d578 (Rs/Rp/L/C)
  → 0x20004a48 +0x30/+0x40/+0x90/+0x98
  → FUN_08009618 (сглаживание → +0x50…)
```

Частота возбуждения для ω: TIM2 → DMA1_CH5 → DAC, структура `0x200049A8` (см. отчёт по генератору / RCC).

#### 6.25.7 Статус уверенности

| Утверждение | Оценка |
|---|---|
| Адреса записи Rs/Rp/L/C | CONFIRMED (6.23 + `.hex`) |
| `12a04` = ×, `129e2` = ÷ комплексных | CONFIRMED |
| \(f\) из `FUN_080006a8` / `0x200049A8` | CONFIRMED |
| scale `15de0` = N·32767·0.5 | CONFIRMED |
| Z = U/I | CONFIRMED |
| Rs = Re(Z) → `+0x30` | CONFIRMED |
| L = X/ω, C = −1/(ω·X) | CONFIRMED (по операциям и константам) |
| Rp = \|Z\|²/R | CONFIRMED (по структуре ×/÷) |
| `+3` = селектор порядка series/parallel UI | CONFIRMED как селектор ветки |
| Какая из двух L/C-формул **первая** пишется в `+0x98` при `+3==0` | PROBABLE (на карту адресов и физику не влияет; закрывается одним численным тестом) |

#### 6.25.8 Итог для дорожной карты

Раздел 11 / приоритет «где и как считается R/L/C» — **ЗАКРЫТ** по месту записи и по основным тождествам.  
Необязательный хвост: unit-тест на известных Re/Im/\(f\) для снятия последнего PROBABLE по порядку записи в ветке `+3==0`.

---

**Связанные символы (таблица §10 — дополнение)**

| Адрес | Имя | Роль | Статус |
|---|---|---|---|
| `0x0800d578` | `FUN_0800d578` | writer Rs/Rp/L/C | CONFIRMED |
| `0x08012a04` | complex mul | \((a+jb)(c+jd)\) | CONFIRMED |
| `0x080129e2` | complex div | \((a+jb)/(c+jd)\) | CONFIRMED |
| `0x080006a8` | freq getter | \(f\) из `0x200049A8` | CONFIRMED |
| `0x08015de0` | corr scale | N·32767·0.5 | CONFIRMED |
